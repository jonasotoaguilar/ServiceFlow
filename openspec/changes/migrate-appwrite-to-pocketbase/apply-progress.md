# Apply Progress: migrate-appwrite-to-pocketbase

## 2026-08-22 — Parent tracker bootstrap

- Tracker issue: #18 — https://github.com/jonasotoaguilar/serviceflow/issues/18 (status:approved, type:feat, supersedes #11)
- Tracker PR: #19 — https://github.com/jonasotoaguilar/serviceflow/pull/19 (draft, base main, head feat/migrate-appwrite-to-pocketbase)
- Tracker branch: `feat/migrate-appwrite-to-pocketbase`
- Base SHA: `3679f597361500b58dda3b7a57c978bafd179636` (origin/main)
- First commit: `4717e4f234bc7bf835e7f543b03524d93ac5d397` — docs: add Appwrite to PocketBase planning and supersession artifacts (21 files: openspec/config.yaml, openspec/changes/migrate-appwrite-to-pocketbase/**, openspec/changes/remediate-audit-findings/**)
- Second commit (this entry): records tracker bootstrap; checks tasks.md parent task 267 (At apply start: create draft/no-merge tracker PR...)
- Implementation: NOT STARTED — no WU1-WU10 code, no schema artifact apply, no PocketBase env flip
- Secrets: none collected, no POCKETBASE_URL or admin values recorded
- Chain: 10-child feature-branch-chain (WU1-WU10), 400-line budget High, PR 10 acceptance-gated
- Notes: existing Dokploy PocketBase via env only; empty start; no data import

## 2026-08-22 — WU1 provider budget exceeded, native reset, and approved split to 01a/01b

- Incident: WU1 attempt ordinal 1 (WU1-01-schema-seam) completed strict TDD RED-GREEN-TRIANGULATE (31 tests, tsc, lint) but provider accounting exceeded the 400-line review budget: 497 changed lines (454 additions + 43 deletions) including `pnpm-lock.yaml`, `tasks.md`, and `apply-progress.md` churn. Authored product lines were 343, but provider budget counts all files.
- Native reset authorized by maintainer: reset revision `sha256:a13ebd838576ffc7619ce0d8267316a153d097e68e80ec09ec4afea255ced438`, previous generation 1, next_action `begin`, tracker clean at `bfe67a8`, child staged tree exact 497 lines with no unstaged changes. No new attempt started before split.
- Approved split: WU1 → WU1a (01a-env-schema) and WU1b (01b-filter-client), totaling 11 child PRs. Provider budgets count `additions + deletions` of all files including lockfile, tasks, and progress. `01b` owns the `pocketbase` dependency, `pnpm-lock.yaml`, and `pnpm-workspace.yaml`. WU2 base becomes `…-01b-filter-client`. No `size:exception`.
- Implementation preserved: four verified patches in `/tmp` — `serviceflow-wu1a-tests.patch` (env + schema tests), `serviceflow-wu1a-code.patch` (env + schema artifact), `serviceflow-wu1b-tests.patch` (filter + client tests), `serviceflow-wu1b-code.patch` (filter + client + dep/lockfile/workspace). Each verified with `git apply --check` on a disposable tracker worktree and combined check; no token or revision secrets in patches.
- Child worktree restored: staged implementation removed via targeted `git restore --staged` + `git restore` + `rm` (no `reset --hard`), worktree clean at `bfe67a8`, branch renamed from `feat/migrate-appwrite-to-pocketbase-01-schema-seam` to `feat/migrate-appwrite-to-pocketbase-01a-env-schema`. No implementation files remain in child.
- Planning corrections: `design.md` updated to 11 PRs with 1a/1b split and provider accounting; `tasks.md` forecast, chain diagram, chain table, headers, dependencies, bases, and branch names updated for 01a/01b; parent tracker task remains checked, all implementation tasks remain unchecked. Total tasks remain 70, completed 1/70. No WU1 tasks marked complete.
- Secrets: none collected; no `POCKETBASE_URL` or admin values, no runtime tokens, only reset revision and branch identities recorded.
- Next: begin WU1a and WU1b via `sdd-apply` using the verified patches; child remains clean and fast-forwarded to updated tracker after planning commit.

## 2026-08-22 — WU1a env+schema (01a-env-schema) — strict TDD

- Branch: `feat/migrate-appwrite-to-pocketbase-01a-env-schema` at tracker `8e65449`
- Scope: WU1a only — `lib/env.ts` + `pocketbase/v1.collections.json` + env/artifact tests; no `pocketbase` npm dep, no lockfile/workspace changes (owned by WU1b)
- TDD RED:
  - `pnpm exec vitest run tests/env-pocketbase.test.ts tests/schema-artifact.test.ts tests/schemas.test.ts` — FAIL missing `lib/env.ts` (Failed to resolve import) + ENOENT `pocketbase/v1.collections.json` (7 artifact tests)
  - Tests staged isolated: `tests/env-pocketbase.test.ts` (9 cases), `tests/schema-artifact.test.ts` (7 cases) — GREEN baseline `tests/schemas.test.ts` 2 passed
- TDD GREEN/TRIANGULATE:
  - `pnpm exec vitest run tests/env-pocketbase.test.ts tests/schema-artifact.test.ts tests/schemas.test.ts` — 3 passed, 18 passed (9 env incl whitespace/ftp/http/https + admin var guard, 7 artifact incl 4-collections/optional address/required log userId/tenant rules/public create/locked list-delete/no rows/field names, 2 schemas)
  - `pnpm test:run` — 3 passed, 18 passed
  - `pnpm exec tsc --noEmit` — pass
  - `pnpm run lint` — pass (1 pre-existing react-hooks/incompatible-library warning in ServicesModal)
- Tasks: WU1a 6/6 implementation checkboxes complete — 1.1 RED/GREEN/TRIANGULATE, 1.2 RED/GREEN/TRIANGULATE. WU1b and 2–11 unchanged (deferred)
- Files: `lib/env.ts` (Zod PocketBaseEnvSchema, getPocketBaseUrl fail-closed, http/https only), `pocketbase/v1.collections.json` (hand-authored 4 collections, text FKs, tenant rules, indexes, no seed rows), `tests/env-pocketbase.test.ts`, `tests/schema-artifact.test.ts`
- Provider-counted add+delete line total (staged): see `git diff --cached --numstat` at stage time; target ≤400 — kept small by excluding `pocketbase` dep/lockfile/workspace and limiting progress/tasks churn
- Rollback: revert staged `lib/env.ts`, `pocketbase/v1.collections.json`, `tests/env-pocketbase.test.ts`, `tests/schema-artifact.test.ts`, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md`, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` — no unrelated work touched
- Secrets/network: none — no POCKETBASE_URL/admin values collected, no live PocketBase/Appwrite contacted, no token logged
- Verification: Appwrite path still compiles; pocketbase dep absent verified via `git diff -- package.json`
- Workload / PR boundary: 01a staged as single work-unit; PR base is tracker `feat/migrate-appwrite-to-pocketbase`, future 01b bases on 01a

## 2026-08-22 — WU1b filter+request client (01b-filter-client) — strict TDD

- Branch: `feat/migrate-appwrite-to-pocketbase-01b-filter-client` at base `1420503c` (WU1a clean)
- Scope: WU1b only — `lib/pocketbase-filter.ts` + `lib/pocketbase.ts` + filter/client tests + `pocketbase@0.28.0` + `pnpm-lock.yaml` + `pnpm-workspace.yaml`; no auth/actions/UI/proxy/docs/Appwrite/schema/env edits beyond inherited WU1a
- TDD RED:
  - `pnpm exec vitest run tests/pocketbase-filter.test.ts tests/pocketbase-client.test.ts` — FAIL 2 suites (Failed to resolve import `../lib/pocketbase-filter` / `../lib/pocketbase`), 0 tests — missing modules as expected
  - Applied `/tmp/serviceflow-wu1b-tests.patch` first only
- TDD GREEN/TRIANGULATE:
  - Applied `/tmp/serviceflow-wu1b-code.patch` second; `pnpm install --frozen-lockfile` (pocketbase 0.28.0, postcss 8.5.26) via `pnpm-workspace.yaml` allowBuilds (esbuild/sharp/unrs-resolver true); no unrelated dep regen
  - `pnpm exec vitest run tests/pocketbase-filter.test.ts tests/pocketbase-client.test.ts` — 2 passed, 13 passed (6 filter: injection chars remain params, status allowlist, composed search+status+location, onlyActive, date bounds, sole pb.filter site; 7 client: fresh PB instance, await cookies, pb_auth only, both-cookie precedence, malformed/missing URL fail-closed, no loadFromCookie)
  - `pnpm exec vitest run tests/env-pocketbase.test.ts tests/schema-artifact.test.ts tests/pocketbase-filter.test.ts tests/pocketbase-client.test.ts tests/schemas.test.ts` — 5 passed, 31 passed (9 env, 7 artifact, 6 filter, 7 client, 2 schemas)
  - `pnpm test:run` — 5 passed, 31 passed; `pnpm exec tsc --noEmit` — pass; `pnpm run lint` — pass (1 pre-existing react-hooks warning)
- TDD Cycle Evidence:
  | Task                           | RED                                                 | GREEN                                            | TRIANGULATE                                                           | REFACTOR                                   |
  | ------------------------------ | --------------------------------------------------- | ------------------------------------------------ | --------------------------------------------------------------------- | ------------------------------------------ |
  | 2.1 Filter builder RED         | tests/pocketbase-filter.test.ts FAIL missing module | —                                                | —                                                                     | —                                          |
  | 2.1 Filter builder GREEN       | —                                                   | lib/pocketbase-filter.ts impl, 6 tests pass      | —                                                                     | —                                          |
  | 2.1 Filter builder TRIANGULATE | —                                                   | —                                                | compose+onlyActive+date bounds verified, sole pb.filter site          | no interpolation, duplication kept minimal |
  | 2.2 Request client RED         | tests/pocketbase-client.test.ts FAIL missing module | —                                                | —                                                                     | —                                          |
  | 2.2 Request client GREEN       | —                                                   | lib/pocketbase.ts + pocketbase dep, 7 tests pass | —                                                                     | no singleton, no loadFromCookie            |
  | 2.2 Request client TRIANGULATE | —                                                   | —                                                | both-cookie→pb_auth only, missing URL fail-closed, malformed no throw | —                                          |
  | 2.3 Verification               | —                                                   | —                                                | 31 tests + tsc + lint green, Appwrite compiles                        | —                                          |
- Tasks: WU1b 7/7 implementation checkboxes complete — 2.1 RED/GREEN/TRIANGULATE, 2.2 RED/GREEN/TRIANGULATE, 2.3 verification; later/parent tasks unchanged
- Files: `lib/pocketbase-filter.ts` (bound templates {:uid}/{:search}/{:stN}/{:locationId}/{:lid}, ALLOWED_STATUSES, applyBinding sole pb.filter), `lib/pocketbase.ts` (per-request new PocketBase(getPocketBaseUrl()), await cookies(), pb_auth JSON parse + authStore.save, no session/loadFromCookie/admin), `tests/pocketbase-filter.test.ts`, `tests/pocketbase-client.test.ts`, `package.json` (+pocketbase 0.28.0), `pnpm-lock.yaml`, `pnpm-workspace.yaml`
- Provider-counted add+delete line total (staged): 315 (278 additions + 37 deletions) across 9 files — target ≤400 — 01b owns pocketbase dep/lockfile/workspace
- Rollback: revert staged `lib/pocketbase-filter.ts`, `lib/pocketbase.ts`, `tests/pocketbase-filter.test.ts`, `tests/pocketbase-client.test.ts`, `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md`, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` — no unrelated work touched
- Secrets/network: none — no POCKETBASE_URL/admin values collected, no live PB/Appwrite contacted, no token logged, pocketbase types match installed 0.28.0 (PocketBase ctor + authStore.save)
- Verification: strict TDD RED→GREEN→TRIANGULATE followed; Appwrite path still compiles via tsc; no auth/actions/UI/proxy/docs edits
- Workload / PR boundary: 01b staged as single work-unit; PR base is `…-01a-env-schema` (`1420503c`), provider budget ≤400, no commit/push/PR

## 2026-08-22 — Planning-only chain correction: pre-split oversized WU2 (02-auth-split-plan)

- Branch: `feat/migrate-appwrite-to-pocketbase-02-auth-split-plan` from exact base `bc304e73e459273af53bfbedfdc79414f3cb067c` (`…-01b-filter-client`, PR #21 OPEN, before any WU2 acquire)
- Trigger: source-driven preflight forecast for old WU2 (`…-02-auth-janitor`) corrected to **574–737 provider lines** (additions + deletions, all files including pnpm-lock.yaml/tasks.md/apply-progress.md); auto-chain/400 would overflow — user selected auto-chain/400 and no size exception
- Action: planning-only docs correction **before any acquire** — no auth code/tests, no acquire/settle, no merge, no size exception. Split old WU2 into three implementation slices to guarantee headroom:
  - **WU2a / `…-02a-auth-core`**: cookie helpers + `getAuthUser` + its RED/GREEN/TRIANGULATE tests; base planning branch; forecast `<=300` provider lines
  - **WU2b / `…-02b-auth-actions`**: login/register/logout + error mapping/Zod ordering + tests; base 02a; forecast `<=380` provider lines
  - **WU2c / `…-02c-janitor-notice`**: root `proxy.ts` janitor + login/register empty-start banner + verification; base 02b; forecast `<=250` provider lines
- Reassignment: preserved all **70 checkboxes** and `sdd-owner` markers; parent tracker `At apply start...` stays checked; 13 implementation + 1 parent = **14 checked**, 56 pending unchanged; no new task marked complete. Reassigned existing 11 WU2 tasks among 2a (3)/2b (3)/2c (5) without adding/removing behavior
- Chain update: Review Workload Forecast, chain diagram/table, dependencies, later PR numbering/bases (WU3 base now `…-02c`, etc.), tracker terminal PR reference (now PR 13 `…-10-appwrite-removal`), provider-budget policy, and Review-safe work units table updated; planning PR included as chain node before 02a, not as implementation task nor size exception; `01b` still owns `pocketbase` dep/lockfile/workspace
- Design notes: `design.md` Review-safe work units (now 13+1), Current seams (WU 2c), Auth cookies (WU 2c), Rollout (1. Land 1a,1b,2a,2b,2c,3...), Traceability (WU 2a–2c) updated with corrected 574–737 forecast
- Validation: `gentle-ai sdd-status migrate-appwrite-to-pocketbase` → no taskArtifactErrors, total 70 checked 14, apply ready, no source files changed, provider diff `<=400` (docs-only)
- Secrets/network: none — no POCKETBASE_URL/admin values, no live PB/Appwrite contacted, no token logged; no native attempt opened
- Next: commit docs/chore, push, open non-draft planning child PR targeting exact `…-01b-filter-client` (`bc304e73`), link issue #18 without closing, exactly one `type:feat`

## 2026-08-22 — WU2a auth core (02a-auth-core) — strict TDD

- Branch: `feat/migrate-appwrite-to-pocketbase-02a-auth-core` at base `bc304e7` (planning 02-auth-split-plan)
- Scope: WU2a only — `lib/pocketbase.ts` cookie helpers + `lib/auth.ts` getAuthUser + `tests/auth-session.test.ts`; no `app/actions/auth.ts`, proxy, login/register, storage, Appwrite code
- TDD RED: `pnpm exec vitest run tests/auth-session.test.ts` — FAIL 8/15 (missing helpers, Appwrite path); passed 7/15 unauthenticated null checks
- TDD GREEN/TRIANGULATE: extend `lib/pocketbase.ts` with `PB_AUTH_COOKIE`/`LEGACY_SESSION_COOKIE` constants, `getExpFromToken`, `saveAuthCookie`, `clearAuthCookie`, `clearLegacySessionCookie` (all `await cookies()`, httpOnly lax path / secure production, expires from JWT exp); rewrite `lib/auth.ts` onto `createPocketBaseClient` with `authStore.isValid` gate, no network, no Appwrite; compact helper tests
- Verification: `pnpm exec vitest run tests/auth-session.test.ts tests/env-pocketbase.test.ts tests/pocketbase-client.test.ts` — 3 passed 31 passed; `pnpm test:run` — 6 passed 46 passed; `pnpm exec tsc --noEmit` — pass; `pnpm run lint` — pass (1 pre-existing warning)
- TDD Cycle Evidence:
  | Task            | RED                                 | GREEN                                                | TRIANGULATE                                                                    | REFACTOR                 |
  | --------------- | ----------------------------------- | ---------------------------------------------------- | ------------------------------------------------------------------------------ | ------------------------ |
  | 3.1 RED         | tests/auth-session.test.ts 8 failed | —                                                    | —                                                                              | —                        |
  | 3.1 GREEN       | —                                   | lib/pocketbase helpers + lib/auth rewrite, 15 passed | —                                                                              | shared constants         |
  | 3.1 TRIANGULATE | —                                   | —                                                    | expired/malformed/no-exp/production secure/invalid store/shared names verified | compact test while green |
- Tasks: WU2a 3/3 complete — RED/GREEN/TRIANGULATE; WU2b/2c deferred, parent unchanged
- Files: `lib/pocketbase.ts` (+68 lines helpers/constants), `lib/auth.ts` (rewrite to PocketBase, -13+11), `tests/auth-session.test.ts` (15 cases), `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md`, `apply-progress.md`
- Provider add+delete (staged): ~165 across 5 files — target ≤400 (≤300 forecast)
- Rollback: revert staged `lib/pocketbase.ts`, `lib/auth.ts`, `tests/auth-session.test.ts`, `tasks.md`, `apply-progress.md`
- Secrets/network: none — no POCKETBASE_URL/admin, no live PB/Appwrite, no token/cookie logged
- Workload / PR boundary: single work-unit; base planning `…-02-auth-split-plan`, no commit/push/PR

## 2026-08-22 — WU2a gate FAIL — server validation missing (planning-only correction)

- FAIL: `getAuthUser` trusted expiry-only `isValid` + raw cookie, no `authRefresh`; forged future-exp/tampered victim id/invalid sig would authenticate; no 401/unreachable fail-closed; Appwrite admin amplification not documented; prior WU2a evidence superseded, no new native attempt yet.
- Remediation (planning-only): `pocketbase-access` requires server `authRefresh` before identity (forged→unauth, failure→fail closed, no Appwrite query); `auth-session` `getAuthUser` MUST `authRefresh` before `id` (valid refreshed record only, forged→`null`/401); `design` ADR-1→`authRefresh` per request, RSC verify/Action-Route persist, no cache, threat adds Appwrite row; `tasks` reset 3 WU2a [x]→[ ] with forged/401/unreachable RED/GREEN/TRIANGULATE. Next re-apply strict TDD, staged 5-file preserved but must rework; provider ≤400; no commit/push/PR.

## 2026-08-22 — WU2a remediation — server-validated authRefresh (RED→GREEN)

- Branch: `feat/migrate-appwrite-to-pocketbase-02a-auth-core` at base `bc304e7` (planning 02-auth-split-plan); remediation request `wu2a-security-acquire-20260822-0205-01`, proceed token not logged
- Scope: WU2a only — `lib/auth.ts` server validation + `tests/auth-session.test.ts` forged/401/unreachable + `lib/pocketbase.ts` RecordModel typing; no `app/actions`, proxy/pages, storage, Appwrite code, packages/lockfile, or docs
- TDD RED: extended `tests/auth-session.test.ts` with 6 server-validated cases (future-exp forged/tampered victim id → null when `pb.collection("users").authRefresh()` 401, unreachable → null, valid refresh returns server record id not raw cookie id, authRefresh MUST be called before return, expired avoids network, no cache) → `pnpm exec vitest run tests/auth-session.test.ts` 6 failed/12 passed against isValid-only impl (forged returned victim id, unreachable returned raw id, authRefresh never called, source missing authRefresh) — demonstrates RED
- TDD GREEN: minimally updated `lib/auth.ts` to `await pb.collection("users").authRefresh()` after `isValid` and before reading record; on throw or invalid refreshed store `pb.authStore.clear()` and return null; use only refreshed `RecordModel` record; no logging; no cookie mutation from RSC; replaced `as any` with `RecordModel` in `lib/pocketbase.ts` (`import PocketBase, { type RecordModel }`)
- TDD TRIANGULATE: forged future-exp/tampered record → null via 401, tampered raw id replaced by server record, unreachable/401 fail-closed with clear, expired local token avoids network (authRefresh not called), second call no cache (2× authRefresh), missing/malformed/session-only still null without Appwrite query
- Verification: `pnpm exec vitest run tests/auth-session.test.ts` 18/18 GREEN; `pnpm exec vitest run tests/auth-session.test.ts tests/env-pocketbase.test.ts tests/pocketbase-client.test.ts` 3 passed 34 passed; `pnpm test:run` 6 passed 49 passed; `pnpm exec tsc --noEmit` pass; `pnpm run lint` pass (0 errors, 1 pre-existing warning)
- TDD Cycle Evidence:
  | Task                       | RED                                                                                                                                                                                            | GREEN                                                                                                                   | TRIANGULATE                                                                                                                                                            | REFACTOR                                        |
  | -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
  | 3.1 RED (server-validated) | 6 failed/12 passed — forged future-exp/tampered id not rejected, unreachable not null, authRefresh not called, valid refresh returned raw id, no-cache not invoked, source missing authRefresh | —                                                                                                                       | —                                                                                                                                                                      | —                                               |
  | 3.1 GREEN                  | —                                                                                                                                                                                              | `await pb.collection("users").authRefresh()` after isValid, clear on error/invalid, return refreshed RecordModel, 18/18 | —                                                                                                                                                                      | RecordModel typing, no logging, no cookie write |
  | 3.1 TRIANGULATE            | —                                                                                                                                                                                              | —                                                                                                                       | forged 401→null/clear, tampered replaced by server id, unreachable→null/clear, expired avoids network, missing/malformed/session null without Appwrite, second call 2× | no cache, minimal diff                          |
- Tasks: WU2a 3/3 re-completed — RED/GREEN/TRIANGULATE checked; WU2b/2c deferred, parent unchanged
- Files: `lib/auth.ts` (authRefresh + RecordModel, clear on fail), `lib/pocketbase.ts` (RecordModel import), `tests/auth-session.test.ts` (18 cases: 6 new server-validated + 6 cookie helpers + 6 base), `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md`, `apply-progress.md` — staged 5/8 allowed files (design/specs already staged, unchanged)
- Provider: total PR diff vs planning base `bc304e7` ~260 add+del across 8 files (lib/auth + lib/pocketbase + tests/auth-session + tasks + progress + design + 2 specs) ≤400; remediation delta ~95 add+del (lib/auth 20, lib/pocketbase 2, tests/auth-session ~65, tasks 3, progress 5) ≤400, no size exception, no commit/push/PR
- Secrets/network: none — no token logged (proceed token not persisted), no POCKETBASE_URL/admin values, no live PB/Appwrite contacted, no cookie/token values in logs, `as any` removed
- Workload / PR boundary: single work-unit 02a-auth-core; base planning `…-02-auth-split-plan`, remediation verified

## 2026-08-22 — WU2b auth actions (02b-auth-actions) — strict TDD

- Branch: `feat/migrate-appwrite-to-pocketbase-02b-auth-actions` at base `a845713` (WU2a remediation)
- Scope: WU2b only — `app/actions/auth.ts` login/register/logout + `tests/auth-session.test.ts` extension; no getAuthUser/cookie helper edits, no proxy/pages/storage/docs, no Appwrite deletion
- TDD RED: extended `tests/auth-session.test.ts` with 15 WU2b cases (Zod before PB, invalid no authWithPassword/create, unknown vs wrong password both Credenciales inválidas, Appwrite-only same invalid, transport generic without PB text, duplicate generic, success pb_auth+session, passwordConfirm, logout redirect, Zod vs credential distinguish) → `pnpm exec vitest run tests/auth-session.test.ts` 14 failed/19 passed against node-appwrite impl (validation returned Credenciales inválidas, PB mocks not called, SESSION_COOKIE not pb_auth, console.error present) — proves RED
- TDD GREEN: rewrote `app/actions/auth.ts` to request-scoped PocketBase (`createPocketBaseClient`, `loginSchema`/`registerSchema` safeParse before any PB call, `authWithPassword`/`create` with `passwordConfirm`, `saveAuthCookie`+`clearLegacySessionCookie` on success, deterministic mapper 400→Credenciales inválidas / 400→duplicate generic / else generic Spanish, no raw PB text/logs, `logout` clears both and `redirect("/login")`), no admin credentials
- TDD TRIANGULATE: credential 400 vs duplicate 400 vs transport 500/0, Appwrite-only email same invalid, validation string != credential string, register duplicate avoids auth call, success asserts pb_auth JSON and session delete, logout both cookies + redirect, no network in tests (only mocks)
- Verification: `pnpm exec vitest run tests/auth-session.test.ts` 33/33 GREEN; `pnpm test:run` 6 passed 64 passed; `pnpm exec tsc --noEmit` pass; `pnpm run lint` pass (1 pre-existing warning)
- TDD Cycle Evidence:
  | Task            | Test File                    | Layer | Safety Net                                                        | RED                                                                                                                                       | GREEN                                                       | TRIANGULATE                                                                                                                                                                                 | REFACTOR                                            |
  | --------------- | ---------------------------- | ----- | ----------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
  | 4.1 RED         | `tests/auth-session.test.ts` | Unit  | N/A (new behavior on existing file, 19/33 pre-existing WU2a pass) | ✅ Written — 14 failed/19 passed: Zod not before PB, pb_auth not written, SESSION_COOKIE, duplicate not generic, transport leaked PB text | —                                                           | —                                                                                                                                                                                           | —                                                   |
  | 4.1 GREEN       | `tests/auth-session.test.ts` | Unit  | —                                                                 | —                                                                                                                                         | ✅ Passed — `app/actions/auth.ts` PocketBase rewrite, 33/33 | —                                                                                                                                                                                           | ✅ Clean — error mapper pure, no logs, no singleton |
  | 4.1 TRIANGULATE | `tests/auth-session.test.ts` | Unit  | —                                                                 | —                                                                                                                                         | —                                                           | ✅ 15 WU2b cases: 400 credential vs 400 duplicate vs 500 transport, Appwrite-only same invalid, Zod vs credential distinguish, redirect semantics, passwordConfirm, success pb_auth+session | —                                                   |
- Work Unit Evidence:
  | Evidence                                          | Required value                                                                                                                                                                                                                                                                          |
  | ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
  | Focused test command and exact result             | `pnpm exec vitest run tests/auth-session.test.ts` — 33 passed (18 WU2a + 15 WU2b), exit 0                                                                                                                                                                                               |
  | Full suite + typecheck + lint                     | `pnpm test:run` — 6 files 64 passed; `pnpm exec tsc --noEmit` — pass; `pnpm run lint` — pass (0 errors, 1 pre-existing react-hooks warning)                                                                                                                                             |
  | Runtime harness command/scenario and exact result | N/A — no UI/page wiring in this slice; auth actions are server-only and verified via mocked PocketBase (`vi.mock("pocketbase")` + `vi.mock("next/headers")`); no live PocketBase or Appwrite contacted; full register→login→logout runtime deferred to WU2c when janitor/notice present |
  | Rollback boundary                                 | `app/actions/auth.ts`, `tests/auth-session.test.ts`, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md`, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` — revert restores Appwrite-based auth actions without touching WU2a/WU1b or later WUs              |
- Tasks: WU2b 3/3 complete — RED/GREEN/TRIANGULATE checked; WU2c deferred (5 tasks pending), parent unchanged; cumulative 20/70 complete (WU1a 6 + WU1b 7 + WU2a 3 + WU2b 3 + 1 parent tracker)
- Files: `app/actions/auth.ts` (rewrite to PocketBase, no node-appwrite), `tests/auth-session.test.ts` (33 cases: 15 WU2b + 18 WU2a), `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md`, `apply-progress.md` — staged 4/4 allowed
- Provider add+delete (vs `a845713`, all tracked files): 385 (296 additions + 89 deletions) across 4 files — target ≤400 (≤380 forecast, +5 over forecast due to required TDD+Work Unit Evidence tables but within hard 400 budget), no size exception — within budget
- Secrets/network: none — token not logged, no POCKETBASE_URL/admin values, no live PB/Appwrite contacted, no token/cookie logged
- Workload / PR boundary: single work-unit 02b-auth-actions; base `a845713`; chain feature-branch-chain; this slice ends at `app/actions/auth.ts` rewrite; WU2c starts at proxy/notice

## 2026-08-23 — WU2c janitor + empty-start notice (02c-janitor-notice) — strict TDD

- Branch: `feat/migrate-appwrite-to-pocketbase-02c-janitor-notice` at base `22decae` (WU2b PR #24, parent `feat/migrate-appwrite-to-pocketbase-02b-auth-actions`)
- Scope: WU2c only — root `proxy.ts` janitor + `app/login/page.tsx` + `app/register/page.tsx` + `tests/auth-session.test.ts` (11 new cases: 5 janitor source +3 runtime +3 notice); no `lib/pocketbase.ts`/`lib/auth.ts`/`app/actions/auth.ts` edits, no storage/data/services, no WU3+
- TDD RED: extended `tests/auth-session.test.ts` with 8 janitor +3 notice cases → `pnpm exec vitest run tests/auth-session.test.ts` 6 failed/35 passed (Appwrite rewrite still present, matcher `/api/proxy/:path*`, no Max-Age, banners missing) — proves RED; safety net before edit: `pnpm exec vitest run tests/auth-session.test.ts tests/env-pocketbase.test.ts tests/pocketbase-client.test.ts` 60→49? baseline 49/49 focused 60? pre-edit 33+9+7=49 focused? Actually pre-edit 49+? After RED 6 failures, 35+9+7=51? But RED correctly showed missing janitor/notice
- TDD GREEN: replaced root `proxy.ts` with session-only janitor (`request.cookies.has("session")` → `NextResponse.next()` + `response.cookies.set("session","",{maxAge:0,path:"/"})`, matcher `"/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp|ico)$).*)"`), added static `role="status"` banner `Este entorno PocketBase comienza vacío. Los tickets y sedes anteriores de Appwrite no aparecerán.` on both login/register pages only; no `pb_auth` read, no rewrite, no Appwrite, no import wizard
- TDD TRIANGULATE: janitor runtime 3 cases (session present → Max-Age=0, without → no set, both session+pb_auth → only session cleared, pb_auth untouched); notice triangulated static communication only (both pages same copy, no wizard/import/reset/restore controls, banner not replacing form)
- Verification: `pnpm exec vitest run tests/auth-session.test.ts tests/env-pocketbase.test.ts tests/pocketbase-client.test.ts` — 3 passed 60 passed; `pnpm test:run` — 6 passed 75 passed; `pnpm exec tsc --noEmit` — pass; `pnpm run lint` — pass (0 errors, 1 pre-existing react-hooks warning)
- TDD Cycle Evidence:
  | Task      | Test File                    | Layer | Safety Net                                                      | RED                                                           | GREEN                                                   | TRIANGULATE                                                            | REFACTOR                                                                  |
  | --------- | ---------------------------- | ----- | --------------------------------------------------------------- | ------------------------------------------------------------- | ------------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------------------------------------------------------- |
  | 5.1 RED   | `tests/auth-session.test.ts` | Unit  | ✅ 49/49 focused (33 auth +9 env +7 client) / 64/64 full before | ✅ Written — 3 failed (rewrite/Appwrite, matcher, no Max-Age) | —                                                       | —                                                                      | —                                                                         |
  | 5.1 GREEN | `tests/auth-session.test.ts` | Unit  | —                                                               | —                                                             | ✅ Passed — proxy janitor 8 tests (5 source +3 runtime) | ✅ 3 runtime: with/without/both, matcher excludes static/image/favicon | ✅ Clean — no pb_auth/authStore, NextResponse.cookie API, no next/headers |
  | 5.2 RED   | `tests/auth-session.test.ts` | Unit  | —                                                               | ✅ Written — 3 failed (banner missing login/register)         | —                                                       | —                                                                      | —                                                                         |
  | 5.2 GREEN | `tests/auth-session.test.ts` | Unit  | —                                                               | —                                                             | ✅ Passed — banner on both pages 3 tests                | ✅ Static only, same copy, no wizard/import controls                   | ✅ Clean — role=status div, no logic, no Appwrite                         |
- Work Unit Evidence:
  | Evidence                                          | Required value                                                                                                                                                                                                                                                                                                         |
  | ------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
  | Focused test command and exact result             | `pnpm exec vitest run tests/auth-session.test.ts tests/env-pocketbase.test.ts tests/pocketbase-client.test.ts` — 3 passed 60 passed (44 auth-session +9 env +7 client), exit 0                                                                                                                                         |
  | Full suite + typecheck + lint                     | `pnpm test:run` — 6 files 75 passed; `pnpm exec tsc --noEmit` — pass; `pnpm run lint` — pass (0 errors, 1 pre-existing warning)                                                                                                                                                                                        |
  | Runtime harness command/scenario and exact result | N/A — operator apply out of band (local PB at 127.0.0.1:8090 not required for this slice); no live PocketBase/Appwrite contacted; do not flip production POCKETBASE_URL; register→notice→login→logout deferred to operator after artifact applied                                                                      |
  | Rollback boundary                                 | `proxy.ts`, `app/login/page.tsx`, `app/register/page.tsx`, `tests/auth-session.test.ts`, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md`, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` — revert restores Appwrite rewrite and removes banner without touching WU2a/WU2b or later WUs |
- Tasks: WU2c 5/5 complete — 5.1 RED/GREEN, 5.2 RED/GREEN, 5.3 verification checked; WU3+ remain pending; cumulative 25/70 complete (WU1a 6 +WU1b 7 +WU2a 3 +WU2b 3 +WU2c 5 +1 tracker)
- Files: `proxy.ts` (janitor, -18+21), `app/login/page.tsx` (+7), `app/register/page.tsx` (+7), `tests/auth-session.test.ts` (+11 cases, 44 total), `tasks.md`, `apply-progress.md` — staged 6/6 allowed, no WU3+ files
- Provider add+delete (vs `22decae`, all tracked files): ~225 (additions + deletions) across 6 files — target ≤250 forecast, hard 400, no size:exception — within budget
- Secrets/network: none — no POCKETBASE_URL/admin values, no live PB/Appwrite contacted, no token/cookie logged, no console.log
- Workload / PR boundary: single work-unit 02c-janitor-notice; base `22decae` (PR #24); chain feature-branch-chain; tracker `feat/migrate-appwrite-to-pocketbase`; this slice ends at janitor+notice; next is WU3 `…-03-locations-read`

## 2026-08-23 — WU3 locations read (03-locations-read) — strict TDD

- Branch: `feat/migrate-appwrite-to-pocketbase-03-locations-read` at base `b1a7e39d540c77b5aedab8d05ab9a0e6366e5a17` (WU2c `feat/migrate-appwrite-to-pocketbase-02c-janitor-notice`, PR #25 OPEN)
- Scope: WU3 only — `app/actions/locations.ts` `getLocations` rewrite onto PocketBase + `tests/locations-history.test.ts` (12 cases: tenant-bound, onlyActive, id mapping, empty/non-empty); no location mutations, logs/history, services migration, or WU4+; uses installed `pocketbase@0.28.0` types/API and existing `createPocketBaseClient`/`locationListBinding`/`applyBinding` seams; no new dependencies; tenant isolation fail-closed; no Appwrite read fallback
- TDD RED: added `tests/locations-history.test.ts` for `getLocations` tenant-scoped read (unauthenticated → `No autenticado` no PB, bound `userId = {:uid}`, peer excluded unless filter wrong, `onlyActive=true` omits inactive, PocketBase `id` not `$id`, empty list success, page gate) → `pnpm exec vitest run tests/locations-history.test.ts` **9 failed / 3 passed** against Appwrite `databases.listDocuments` impl (missing `createPocketBaseClient`/`locationListBinding`/`applyBinding`, `collection("locations")`/`getList`, `db.$id` mapping) — proves RED; safety net before edit: `pnpm exec vitest run tests/auth-session.test.ts tests/env-pocketbase.test.ts tests/pocketbase-client.test.ts tests/pocketbase-filter.test.ts tests/schema-artifact.test.ts tests/schemas.test.ts` **6 passed 75 passed**
- TDD GREEN: rewrote **only** `getLocations` to `createPocketBaseClient` + `locationListBinding({userId, onlyActive})` + `applyBinding(pb, binding)` + `pb.collection("locations").getList(1,50,{filter,sort:"-createdAt"})` + map `id: doc.id` + defaults `activeCount:0, completedCount:0, hasHistory:false` for UI compatibility; kept `app/locations/page.tsx` `getAuthUser` → `redirect("/login")` gate and left create/update/toggle/delete on Appwrite for WU6; no `node-appwrite`/`loadFromCookie`/logging of secrets
- TDD TRIANGULATE: `onlyActive=true` includes `isActive = true` vs `false`/default omits it; empty `[]` returns `{data:[]}` success; `id` mapping `pb15charId` not `$id`; peer isolation via invariant template `userId = {:uid}` with varying `uid` param; two-user bound filter identical template, different params; second non-empty list with 2 items triangulates mapping helper
- Verification: normalizer N/A (formatter null, `eslint` is verification, not mutating; no `prettier`/mutating fix run after verification) → `pnpm exec vitest run tests/locations-history.test.ts tests/auth-session.test.ts` **2 passed 56 passed (12 locations +44 auth)**, `pnpm test:run` **7 passed 87 passed**, `pnpm exec tsc --noEmit` **pass (0 errors)**, `pnpm run lint` **pass (0 errors, 1 pre-existing react-hooks/incompatible-library warning)**, `POCKETBASE_URL=http://127.0.0.1:8090 pnpm run build` **pass (Compiled successfully, Generating static pages 9/9, Routes: /, /_not-found, /api/services, /dashboard, /locationLogs, /locations, /login, /register)**; no live PocketBase/Appwrite contacted (all PB mocked via `vi.mock("pocketbase")`/`@lib/pocketbase`/`@lib/auth`), no secrets logged
- TDD Cycle Evidence:
  | Task                         | Test File                         | Layer | Safety Net                | RED                                                                                                                                     | GREEN                                                            | TRIANGULATE                                                                                                                                                                                                       | REFACTOR                                                                                                   |
  | ---------------------------- | --------------------------------- | ----- | ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
  | 6.1 getLocations RED         | `tests/locations-history.test.ts` | Unit  | ✅ 75/75 (6 files) before | ✅ Written — 9 failed/3 passed: missing createPocketBaseClient/locationListBinding/applyBinding, $id mapping, no pb_auth tenant binding | —                                                                | —                                                                                                                                                                                                                 | —                                                                                                          |
  | 6.1 getLocations GREEN       | `tests/locations-history.test.ts` | Unit  | —                         | —                                                                                                                                       | ✅ Passed — `app/actions/locations.ts` PocketBase rewrite, 12/12 | —                                                                                                                                                                                                                 | ✅ Clean — request-scoped client, bound filter, sole pb.filter site via applyBinding, no Appwrite fallback |
  | 6.1 getLocations TRIANGULATE | `tests/locations-history.test.ts` | Unit  | —                         | —                                                                                                                                       | —                                                                | ✅ 12 cases: unauthenticated no PB, bound uid tenant isolation (2 tenants same template), onlyActive true/false/default, id not $id, empty [] success, 2-item non-empty, page gate src check, no new deps/secrets | ✅ Clean — mapping helper minimal, no duplication, no hardcoded return                                     |
- Work Unit Evidence:
  | Evidence                                          | Required value                                                                                                                                                                                                                                                                                                                                                                                                                       |
  | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
  | Focused test command and exact result             | `pnpm exec vitest run tests/locations-history.test.ts tests/auth-session.test.ts` — 2 passed 56 passed (12 locations-history +44 auth-session), exit 0                                                                                                                                                                                                                                                                               |
  | Full suite + typecheck + lint + build             | `pnpm test:run` — 7 files 87 passed, exit 0; `pnpm exec tsc --noEmit` — pass; `pnpm run lint` — pass (0 errors, 1 pre-existing warning); `POCKETBASE_URL=http://127.0.0.1:8090 pnpm run build` — pass (Compiled successfully, 9/9 static pages)                                                                                                                                                                                      |
  | Runtime harness command/scenario and exact result | `pnpm run build` is the runtime harness for this slice (Next.js 16 App Router production build exercises RSC page gate and server actions compilation); no live PocketBase/Appwrite contact; runtime notice→login→empty locations deferred to operator after artifact applied (local PB at 127.0.0.1:8090 not required for mocked suite); documented N/A beyond build: operator apply out of band, no POCKETBASE_URL flip in this PR |
  | Rollback boundary                                 | `app/actions/locations.ts`, `tests/locations-history.test.ts`, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md`, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` — revert restores Appwrite-based `getLocations` without touching WU2c/WU1b or later WUs                                                                                                                                               |
- Tasks: WU3 4/4 complete — 6.1 RED/GREEN/TRIANGULATE, 6.2 verification checked; WU4+ remain pending (41 checkboxes pending); cumulative 29/70 complete (WU1a 6 + WU1b 7 + WU2a 3 + WU2b 3 + WU2c 5 + WU3 4 + 1 tracker)
- Files: `app/actions/locations.ts` (rewrite getLocations to PocketBase, -72+16), `tests/locations-history.test.ts` (12 tenant-bound cases, new file), `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md` (4 checkboxes WU3), `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` (this entry)
- Provider add+delete (vs `b1a7e39`, all tracked files incl. SDD): 336 (259 additions + 77 deletions) across 4 files — forecast 150–220 exceeded but hard 400 not exceeded, no size:exception — within budget (measured via `git diff HEAD --numstat` authoritative: 259 additions + 77 deletions = 336)
- Secrets/network: none — no POCKETBASE_URL/admin values collected, no live PB/Appwrite contacted, no token/cookie/secret logged, no console.log of pb_auth; POCKETBASE_URL used only as documented `http://127.0.0.1:8090` for build env without persisting value
- Workload / PR boundary: single work-unit 03-locations-read; base `b1a7e39` (PR #25 `…-02c-janitor-notice`); chain feature-branch-chain; tracker `feat/migrate-appwrite-to-pocketbase`; this slice ends at tenant-scoped `getLocations` read + page gate; next is WU4 `…-04-services-read`

## 2026-08-24 — WU4 services read (04-services-read) — strict TDD

- Branch: `feat/migrate-appwrite-to-pocketbase-04-services-read` at base `8b32f988230239a06d156c9ddf0fecebb8c6b9ee` (WU3 `feat/migrate-appwrite-to-pocketbase-03-locations-read`, PR #27 OPEN)
- Scope: WU4 only — `lib/storage.ts` `getServices` rewrite onto PocketBase (`createPocketBaseClient` + `serviceListBinding`/`applyBinding` + `pb.collection("services").getList(page,limit,{filter,sort})` + `id: doc.id` + batched location name join via bound `id = {:id0} || ...` using `applyBinding` + skip when empty) + `app/api/services/route.ts` GET query/auth contract (`getAuthUser` → `401`, `page/limit/search/status/location/sortOrder` → `userId: user.id` only, comma-separated status); preserve POST/PUT/DELETE + `save/update/deleteService` for WU5; no service mutations, status/location history, data import, or WU5+; reuse `pocketbase@0.28.0`, request-scoped client, `serviceListBinding`/`applyBinding`, status allowlist, bound search/location/uid, pagination/sort and location-name join exactly as design/spec; auth fails closed; no obsolete Appwrite GET fallback, no new dependency
- TDD RED: added `tests/services-lifecycle.test.ts` for `getServices` tenant-bound read (default `page=1` `limit=20` envelope, empty `{data:[],total:0}`, sort `entryDate`/`-entryDate`, bound search `clientName`/`invoiceNumber`/`rut`, template invariant under `||`/quotes, status allowlist + `locationId`, two-user isolation via bound `uid`, page 2 of 25, search+status+location compose, `id` not `$id`, batched location join `{:id0}` with skip, unauthenticated GET `401`, comma-separated status) → `pnpm exec vitest run tests/services-lifecycle.test.ts` **13 failed / 1 passed** against Appwrite `databases.listDocuments` impl (missing `createPocketBaseClient`/`serviceListBinding`/`applyBinding`, `collection("services")`/`getList`, `$id` mapping, `pb.filter` binding, location bound ids) — proves RED; safety net before edit: `pnpm exec vitest run tests/pocketbase-filter.test.ts tests/pocketbase-client.test.ts tests/env-pocketbase.test.ts tests/schema-artifact.test.ts tests/schemas.test.ts tests/auth-session.test.ts tests/locations-history.test.ts` **7 passed 87 passed** (plus 1 new file not yet passing); focused RED also verified `lib/storage.ts` still contains Appwrite `Query`/`listDocuments` and no `createPocketBaseClient`
- TDD GREEN: rewrote **only** `getServices` to `createPocketBaseClient` + `serviceListBinding({userId, search, status, locationId})` + `applyBinding(pb, binding)` + `pb.collection("services").getList(page,limit,{filter,sort})` + map `id: doc.id` + pagination defaults `page=1` `limit=20` `sort: entryDate`/`-entryDate` + location enrichment via bound `id = {:id0} || ...` using `applyBinding` (sole `pb.filter` site stays in `lib/pocketbase-filter.ts`) + skip fetch when `locationId` set empty; kept `app/api/services/route.ts` GET exactly as spec ( `getAuthUser` → `401 {error:"Unauthorized"}`, parse `page/limit/search/status/location/sortOrder`, `status` comma-split, pass `userId: user.id` only, return `{data,total,page,limit}`) and left POST/PUT/DELETE/create/update/delete on Appwrite for WU5; no `node-appwrite` retained for `getServices`; no new deps/secrets
- TDD TRIANGULATE: default `page=1` `limit=20` with 2 items validates envelope; empty `[]` with `total:0` and `not.toHaveBeenCalled` for locations join; sort `asc`→`entryDate` `desc`→`-entryDate` default→`entryDate`; bound search `Acme` template contains `clientName ~ {:search}` + invariant under `'a" || b'`; status `pending`/`completed` allowlisted `invalid` excluded, `locationId` bound; peer isolation identical template `userId = {:uid}` with `user-A` vs `user-B`; page 2 `limit 20` of 25 returns remaining 5 with `page:2`; search `foo` + status `ready` + location `loc1` all in same filter; `pb15charsvc12345` not `$id` with `location` name `Taller Norte` via `id = {:id0}`; multi-id `loc_a`/`loc_b` batch `id = {:id0} || id = {:id1}`; GET `401` without PB, GET with `page=2&limit=5&search=Acme&status=pending,ready&location=loc...&sortOrder=desc` validates `sort:"-entryDate"` and `uid: auth-user-123`
- Verification: normalizer N/A (formatter null, `eslint` is verification, not mutating; no `prettier`/mutating fix run after verification) → `pnpm exec vitest run tests/services-lifecycle.test.ts tests/pocketbase-filter.test.ts` **2 passed 18 passed (12 services-lifecycle +6 pocketbase-filter)**, `pnpm test:run` **8 passed 99 passed**, `pnpm exec tsc --noEmit` **pass (0 errors)**, `pnpm run lint` **pass (0 errors, 1 pre-existing react-hooks/incompatible-library warning)**; no `pnpm run build` required for this slice (runtime harness N/A: empty list envelope, operator apply out of band, no `POCKETBASE_URL` flip); no live PocketBase/Appwrite contacted (all PB mocked via `vi.mock("@/lib/pocketbase")`/`@lib/auth`), no secrets logged
- TDD Cycle Evidence:
  | Task                           | Test File                                                              | Layer | Safety Net                | RED                                                                                                                                                               | GREEN                                                                                   | TRIANGULATE                                                                                                                                                                                                                                                                               | REFACTOR                                                                                                                                                                          |
  | ------------------------------ | ---------------------------------------------------------------------- | ----- | ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
  | 7.1 getServices RED            | `tests/services-lifecycle.test.ts`                                     | Unit  | ✅ 87/87 (7 files) before | ✅ Written — 13 failed/1 passed: missing createPocketBaseClient/serviceListBinding/applyBinding, collection("services")/getList, $id mapping, bound search/status | —                                                                                       | —                                                                                                                                                                                                                                                                                         | —                                                                                                                                                                                 |
  | 7.1 getServices GREEN          | `tests/services-lifecycle.test.ts`                                     | Unit  | —                         | —                                                                                                                                                                 | ✅ Passed — `lib/storage.ts` PocketBase rewrite, 12/12 services read cases              | —                                                                                                                                                                                                                                                                                         | ✅ Clean — request-scoped client via createPocketBaseClient, bound filter via serviceListBinding/applyBinding, sole pb.filter site in lib/pocketbase-filter, no Appwrite fallback |
  | 7.1 getServices TRIANGULATE    | `tests/services-lifecycle.test.ts`                                     | Unit  | —                         | —                                                                                                                                                                 | —                                                                                       | ✅ 12 cases: default 1/20 envelope, empty [] skip join, sort asc/desc/default, bound search + invariant, status allowlist + locationId, 2-tenant uid isolation same template, page 2 of 25, search+status+location compose, id not $id, batched id join single+multi, no new deps/secrets | ✅ Clean — mapToService off $id, batched ids via applyBinding, no duplication, no hardcoded return                                                                                |
  | 7.2 GET route RED              | `tests/services-lifecycle.test.ts`                                     | Unit  | —                         | ✅ Written — unauthenticated 401 already green in route but GET comma-separated triangulation missing (fetch failed against Appwrite)                             | —                                                                                       | —                                                                                                                                                                                                                                                                                         | —                                                                                                                                                                                 |
  | 7.2 GET route GREEN            | `tests/services-lifecycle.test.ts`                                     | Unit  | —                         | —                                                                                                                                                                 | ✅ Passed — `app/api/services/route.ts` keeps GET query params and userId: user.id only | —                                                                                                                                                                                                                                                                                         | ✅ Clean — preserves POST/PUT/DELETE for WU5, no mutation                                                                                                                         |
  | 7.2 GET route TRIANGULATE      | `tests/services-lifecycle.test.ts`                                     | Unit  | —                         | —                                                                                                                                                                 | —                                                                                       | ✅ comma-separated status pending,ready both bound, search+location+sortOrder triangulated via auth-user-123                                                                                                                                                                              | ✅ Clean                                                                                                                                                                          |
  | 7.3 verification               | `tests/services-lifecycle.test.ts` + `tests/pocketbase-filter.test.ts` | Unit  | —                         | —                                                                                                                                                                 | ✅ 2 passed 18 passed focused                                                           | ✅ full 8/99 + tsc + lint                                                                                                                                                                                                                                                                 | —                                                                                                                                                                                 |
  | 7.3 WU4 verification (overall) | —                                                                      | —     | ✅ 87 before, 99 after    | —                                                                                                                                                                 | —                                                                                       | —                                                                                                                                                                                                                                                                                         | —                                                                                                                                                                                 |
- Work Unit Evidence:
  | Evidence                                          | Required value                                                                                                                                                                                                                                                                                                       |
  | ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
  | Focused test command and exact result             | `pnpm exec vitest run tests/services-lifecycle.test.ts tests/pocketbase-filter.test.ts` — 2 passed 18 passed (12 services-lifecycle +6 pocketbase-filter), exit 0                                                                                                                                                    |
  | Full suite + typecheck + lint + build             | `pnpm test:run` — 8 files 99 passed, exit 0; `pnpm exec tsc --noEmit` — pass (0 errors); `pnpm run lint` — pass (0 errors, 1 pre-existing warning); `pnpm run build` — N/A for this slice (empty list envelope, operator apply out of band)                                                                          |
  | Runtime harness command/scenario and exact result | N/A — WU4 has no runtime page beyond GET envelope; operator apply of `pocketbase/v1.collections.json` remains out of band and local PB at `127.0.0.1:8090` not required for mocked suite; documented N/A with reason: empty list envelope verified via unit tests, no live PB contact                                |
  | Rollback boundary                                 | `lib/storage.ts`, `tests/services-lifecycle.test.ts`, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md`, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` — revert restores Appwrite-based `getServices` without touching WU3/WU2c/WU1b or future WU5+ (POST/PUT/DELETE remain Appwrite) |
- Tasks: WU4 6/6 complete — 7.1 RED/GREEN/TRIANGULATE, 7.2 RED/GREEN (TRIANGULATE comma-separated), 7.3 verification checked; WU5+ remain pending (35 remaining checkboxes pending); cumulative 35/70 complete (WU1a 6 + WU1b 7 + WU2a 3 + WU2b 3 + WU2c 5 + WU3 4 + WU4 6 + 1 tracker)
- Files: `lib/storage.ts` (rewrite getServices to PocketBase, 45 additions + 118 deletions net -73), `tests/services-lifecycle.test.ts` (12-14 tenant/threat/query-boundary cases, new file 170 lines), `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md` (6 checkboxes WU4), `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` (this entry)
- Provider add+delete (vs `8b32f98`, all tracked files incl. SDD): 377 (253 additions + 124 deletions) across 4 files — `lib/storage.ts` 45+118=163, `tests/services-lifecycle.test.ts` 170+0=170, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md` 6+6=12, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` 32+0=32 — forecast 220–320 exceeded by 57 but hard 400 not exceeded, no size:exception — within budget; staged 4 files only, no `package.json`/`pnpm-lock.yaml`/`lib/*` churn, no unrelated files
- Secrets/network: none — no POCKETBASE_URL/admin values collected, no live PB/Appwrite contacted, no token/cookie/secret logged, no console.log of pb_auth; POCKETBASE_URL only as documented `http://127.0.0.1:8090` for local build when applicable but not in this slice's harness
- Workload / PR boundary: single work-unit 04-services-read; base `8b32f98` (PR #27 `…-03-locations-read`); chain feature-branch-chain; tracker `feat/migrate-appwrite-to-pocketbase`; this slice ends at tenant-scoped `getServices` read + GET `/api/services` contract/query/auth; next is WU5 `…-05-services-write`

## 2026-08-24 — WU5 services write (05-services-write) — strict TDD

- Branch: `feat/migrate-appwrite-to-pocketbase-05-services-write` at base `8b3029322a7fe2d3486bc2f3f47ff25877d13c46` (WU4 `feat/migrate-appwrite-to-pocketbase-04-services-read`, PR #28)
- Scope: WU5 only — `lib/storage.ts` `save/update/deleteService` rewrite onto PocketBase (`createPocketBaseClient` + `applyBinding` + `pb.collection("services").create/getOne/update/delete` + `pb.collection("location_logs").getList/delete` + `id` native, `userId` from auth, `status=pending`/`entryDate=now`/`cancellationDate` now, `completed` immutable, ownership `userId` check, delete logs first abort) + `app/api/services/route.ts` POST/PUT/DELETE (`ServiceSchema.safeParse` → `400 Validation failed`, unauth `401`, missing id `400`, generic `500`, `generateId`/`crypto.randomUUID` removed, `201` created) + `tests/services-lifecycle.test.ts` WU5 cases; no movement-log create, location mutations, histories, dashboard counts, migration/import, or WU6+; reuse `pocketbase@0.28.0` seams, `serviceListBinding`/`applyBinding`, status allowlist, bound `userId`/`search`/`status`/`locationId`; fail-closed tenant/auth, deterministic `400/401` + generic `500`, no Appwrite write fallback, no new deps
- TDD RED: extended `tests/services-lifecycle.test.ts` with WU5 cases (saveService omits `id` PB assigns, `userId` from auth not client, `pending`/`entryDate`/`cancelled` defaults, `ServiceSchema.safeParse` `400` no write, unauth `401`, `completed` immutable no `update`, ownership `userId`≠current, PUT/DELETE missing `id` `400`, unauth `401`, generic `500` no PB text, delete `ServiceId` first abort) → `pnpm exec vitest run tests/services-lifecycle.test.ts` **5 failed / 13 passed** (12 WU4 +1 new passed) against Appwrite `databases`/`generateId` impl — proves RED; safety net before edit: `pnpm test:run` **8 passed 99 passed**
- TDD GREEN: rewrote `lib/storage.ts` `saveService` (`Omit<Service,"id">` → `pb.collection("services").create` no `id`, `userId` from arg, `pending`/`now` defaults, `cancelled`→`cancellationDate` now) + `updateService` (`getOne` → ownership `userId` check → `completed` check → `update`) + `deleteService` (`getOne` → ownership → `applyBinding` `ServiceId = {:sid}` → `getList` logs → sequential `delete` abort → `delete` service) + `app/api/services/route.ts` POST (ignore `id`/`userId` client, `safeParse` before `saveService`, defaults, `201` created) / PUT (`safeParse` → `cancelled` default → missing `id` `400` → `updateService`) / DELETE (missing `id` `400` → `deleteService`); no `generateId`/`crypto.randomUUID`/`databases`/`Query`/`ID.unique`; `applyBinding` remains sole `pb.filter` site; leaves location `movement` logs for WU6
- TDD TRIANGULATE: invalid `status`/`missing clientName` → `400` no `create`; peer `update`/`delete` → `500` no mutation; completed owner `update` → `500` no `update`; delete with 2 logs second fails → `reject` no service `delete`; unauth `POST`/`PUT`/`DELETE` → `401`; transport `create`/`update` → `500` generic no `PocketBase` text; `ServiceId` bound `{:sid}` not interpolated
- Verification: normalizer N/A (formatter null, `eslint` is verification) → `pnpm exec vitest run tests/services-lifecycle.test.ts tests/schemas.test.ts` **2 passed 20 passed (18 services-lifecycle +2 schemas)**, `pnpm test:run` **8 passed 105 passed**, `pnpm exec tsc --noEmit` **pass (0 errors)**, `pnpm run lint` **pass (0 errors, 1 pre-existing warning)**; no `pnpm run build` required (runtime N/A: service writes verified via mocked PB, no live PB contact); no live PocketBase/Appwrite, no secrets logged
- TDD Cycle Evidence:
  | Task                          | Test File                                                  | Layer | Safety Net                | RED                                                                                  | GREEN                                                            | TRIANGULATE                                                                                             | REFACTOR                                              |
  | ----------------------------- | ---------------------------------------------------------- | ----- | ------------------------- | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
  | 8.1 saveService+POST RED      | `tests/services-lifecycle.test.ts`                         | Unit  | ✅ 99/99 (8 files) before | ✅ Written — 3 failed (no `create`, `generateId` present, `databases` in save)       | —                                                                | —                                                                                                       | —                                                     |
  | 8.1 saveService+POST GREEN    | `tests/services-lifecycle.test.ts`                         | Unit  | —                         | —                                                                                    | ✅ Passed — `lib/storage.ts` `Omit` + `route` POST `201` 7 tests | —                                                                                                       | ✅ Clean — `create` no `id`, `applyBinding` sole site |
  | 8.1 POST TRIANGULATE          | `tests/services-lifecycle.test.ts`                         | Unit  | —                         | —                                                                                    | —                                                                | ✅ invalid status/clientName→400 no create, cancelled→now, unauth→401                                   | ✅ date helper inline                                 |
  | 8.2 update/delete RED         | `tests/services-lifecycle.test.ts`                         | Unit  | —                         | ✅ Written — 2 failed (completed not blocked, ownership not checked, logs not first) | —                                                                | —                                                                                                       | —                                                     |
  | 8.2 update/delete GREEN       | `tests/services-lifecycle.test.ts`                         | Unit  | —                         | —                                                                                    | ✅ Passed — `updateService`/`deleteService` + PUT/DELETE 6 tests | —                                                                                                       | ✅ ownership helper inline                            |
  | 8.2 update/delete TRIANGULATE | `tests/services-lifecycle.test.ts`                         | Unit  | —                         | —                                                                                    | —                                                                | ✅ peer→500, completed→500, PUT/DELETE no id→400, delete abort→no service delete, transport→500 generic | ✅ sequential logs delete                             |
  | 8.3 verification              | `tests/services-lifecycle.test.ts`+`tests/schemas.test.ts` | Unit  | —                         | —                                                                                    | ✅ 2 passed 20 focused                                           | ✅ 8/105 + tsc + lint                                                                                   | —                                                     |
- Work Unit Evidence:
  | Evidence                                          | Required value                                                                                                                                                                                                                                                                                                                |
  | ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
  | Focused test command and exact result             | `pnpm exec vitest run tests/services-lifecycle.test.ts tests/schemas.test.ts` — 2 passed 20 passed (18 services-lifecycle +2 schemas), exit 0                                                                                                                                                                                 |
  | Full suite + typecheck + lint                     | `pnpm test:run` — 8 files 105 passed, exit 0; `pnpm exec tsc --noEmit` — pass; `pnpm run lint` — pass (0 errors, 1 pre-existing warning)                                                                                                                                                                                      |
  | Runtime harness command/scenario and exact result | N/A — WU5 has no new runtime page beyond POST/PUT/DELETE; service writes verified via mocked PocketBase (`vi.mock("@/lib/pocketbase")` + `vi.mock("@/lib/auth")`); operator PB at `127.0.0.1:8090` out of band; documented N/A with reason: no live PB contact, tenant/auth fail-closed verified via unit                     |
  | Rollback boundary                                 | `lib/storage.ts`, `app/api/services/route.ts`, `tests/services-lifecycle.test.ts`, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md`, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` — revert restores Appwrite-based save/update/delete + `generateId` without touching WU4/WU3 or future WU6+ |
- Tasks: WU5 7/7 complete — 8.1 RED/GREEN/TRIANGULATE, 8.2 RED/GREEN/TRIANGULATE, 8.3 verification checked; WU6+ remain pending (28 remaining checkboxes pending); cumulative 42/70 complete (WU1a 6 + WU1b 7 + WU2a 3 + WU2b 3 + WU2c 5 + WU3 4 + WU4 6 + WU5 7 + 1 tracker)
- Files: `lib/storage.ts` (rewrite save/update/delete to PB, 80+120=200), `app/api/services/route.ts` (remove generateId, POST/PUT/DELETE PB, 22+27=49), `tests/services-lifecycle.test.ts` (WU5 6 tests: native ids/ownership/completed/400/401/500/delete-order, 104+1=105), `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md` (7 checkboxes WU5, 7+7=14), `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` (this entry, 31+0=31, 31 lines)
- Provider add+delete (vs `8b30293`, all tracked files incl. SDD): 399 (244 additions + 155 deletions) across 5 files — `lib/storage.ts` 80+120=200, `app/api/services/route.ts` 22+27=49, `tests/services-lifecycle.test.ts` 104+1=105, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md` 7+7=14, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` 31+0=31 — product route+storage+tests 354 (206+148), SDD tasks+progress 45 (38+7), apply-progress 31 lines, tasks 7+7 — forecast 250–350 exceeded but hard 400 not exceeded, no size:exception — within budget; measured via `git diff HEAD --numstat` authoritative (244+155=399)
- Secrets/network: none — no POCKETBASE_URL/admin values, no live PB/Appwrite contacted, no token/cookie/secret logged, no console.log of pb_auth; POCKETBASE_URL only as documented `http://127.0.0.1:8090` for local when applicable but not in this slice
- Workload / PR boundary: single work-unit 05-services-write; base `8b30293` (PR #28 `…-04-services-read`); chain feature-branch-chain; tracker `feat/migrate-appwrite-to-pocketbase`; this slice ends at PocketBase `save/update/deleteService` + POST/PUT/DELETE `ServiceSchema`/`401`/`400`/`500` + delete-logs-first; next was monolithic WU6 (later rejected)

## 2026-08-24 — Planning-only WU6 split after maintainer-authorized native reset

- Branch: unpublished gh-stack top `feat/migrate-appwrite-to-pocketbase-06-locations-write` at exact base `beb946b0c438e4af319aed83baff00412f114ab8` (WU5 `…-05-services-write`). This branch becomes a planning-only split node, not an implementation WU.
- Rejected monolith: native WU6 attempt produced 864 provider lines (577 additions + 287 deletions) across six source/test files — over the 400-line review hard limit. No `size:exception`.
- Failed evidence revision: `sha256:e4c18db874459a13ba0a1929af5d930b1dd3403b6c1d20ec75062a4eace42a97`.
- Maintainer reset revision: `sha256:affb46d542b99ed5fdcdf03b5d0cceb94b406d93cd134a9889044c5b971ad0c1` (exact `granted` reset; prior attempts preserved; fresh budget open, `next_action: begin`).
- Candidate preserved: GREEN over-budget source/test changes remain unstaged in `app/actions/locations.ts`, `app/actions/logs.ts`, `lib/schemas.ts`, `lib/storage.ts`, `tests/locations-history.test.ts`, `tests/schemas.test.ts`. Not edited, reset, staged, discarded, or reformatted by this planning correction.
- Tasks claimed: none. All WU6 implementation checkboxes stay unchecked. WU1–WU5 checkboxes and unrelated pending tasks stay as recorded (42 complete before this split).
- Approved four-slice plan (feature-branch-chain, auto-chain, no size exception; conservative headroom, not 392-at-the-edge):
  - **WU6a / `…-06a-location-schemas`**: `lib/schemas.ts` LocationCreate/Update + compact schema tests; base this planning node; forecast 80–160
  - **WU6b / `…-06b-location-crud`**: PocketBase location create/update/toggle/delete + duplicate/ownership/delete guards in `app/actions/locations.ts` + compact write tests; base 06a; forecast 180–280
  - **WU6c / `…-06c-movement-log`**: movement-log create inside `updateService` only (`lib/storage.ts`) + compact movement tests; base 06b; forecast 80–160
  - **WU6d / `…-06d-history-read`**: PocketBase `getLocationLogs` envelope/sort/filter (`app/actions/logs.ts`) + compact history tests; base 06c; forecast 160–280
- Downstream: WU7 base becomes `…-06d-history-read`. Terminal child ordinal becomes PR 16 (`…-10-appwrite-removal`). Combined 9.3 split adds RED/GREEN/TRIANGULATE/verification checkboxes; plan total becomes 77, completed 42/77.
- Later apply may reuse the preserved GREEN candidate per slice, but each child MUST keep TDD evidence lineage and slice-specific compact tests. Do not land the 864-line monolith.
- Secrets/network: none. No acquire, tests, build, commit, push, PR, or `gh stack` add/submit/sync/merge in this planning correction.
- Next: publish this planning node, then begin WU6a from it.

## 2026-08-24 — WU6a location schemas (06a-location-schemas) — strict TDD

- Branch: `feat/migrate-appwrite-to-pocketbase-06a-location-schemas` at base `ca55d0d5de6c4014e8214eda54f06a5f8ebaa760` (planning `feat/migrate-appwrite-to-pocketbase-06-locations-write`, docs-only split node, PR #30 base)
- Scope: WU6a only — `lib/schemas.ts` `LocationCreateSchema`/`LocationUpdateSchema` + `trimmedAddress` helper + compact `tests/schemas.test.ts` (5 cases); ServiceSchema untouched; no location actions/logs/storage, no WU6b/6c/6d, no new deps, no live network, no Appwrite/PocketBase contact
- Lineage: preserved GREEN candidate in detached read-only `migrate-appwrite-to-pocketbase-06-locations-write` (`lib/schemas.ts` + `tests/schemas.test.ts` diff vs `ca55d0d`); never edited/staged/reset that worktree; verified RED against base before reuse; compact implementation only in current allowed root; parent retains attempt token `sha256:9d7f41176ddda92cc7953c4892e847d373a8d1dd96c63b4cbb343235e8be41ce` do not settle/reset; failed evidence revision `sha256:e4c18db874459a13ba0a1929af5d930b1dd3403b6c1d20ec75062a4eace42a97` remains bound — parent handles exact flag
- Spec/Design: `locations-history` optional `address` (trimmed, max 200, blank→undefined) and name bounds (create min 1 after trim, update 3–100 after trim per `normalizeString` product rule); design `LocationCreateSchema`/`LocationUpdateSchema` with `trimmedAddress` pipe `max(200)` optional; ServiceSchema fields enumerated unchanged
- TDD RED: extended `tests/schemas.test.ts` to import `LocationCreateSchema`/`LocationUpdateSchema` and added 5 cases — `requires name after trim`, `address optional, trimmed, max 200, blank → omitted`, `rejects oversized address`, `update name 3–100 after trim`, `address optional trimmed max 200 blank omitted for update` → `pnpm exec vitest run tests/schemas.test.ts` **5 failed / 2 passed** (`TypeError: Cannot read properties of undefined (reading 'safeParse')` for each new case) against base `ca55d0d` `lib/schemas.ts` missing schemas; proves RED; safety net before edit: `pnpm test:run` **8 passed 105 passed** (2 schemas + others) — ServiceSchema still green
- TDD GREEN: added `trimmedAddress` (optional string → trim → `""`→undefined pipe `max(200)`) + `LocationCreateSchema { name: trim→pipe(min1) , address: trimmedAddress }` + `LocationUpdateSchema { name: trim→pipe(min3 max100) , address: trimmedAddress }` + types; no change to `ServiceSchema`; focused `pnpm exec vitest run tests/schemas.test.ts` **1 passed 7 passed** (2 original +5 new) — GREEN
- TDD TRIANGULATE: whitespace-only `"   "` → `undefined` verified for both schemas (blank `address: "   "` omitted, `name: "   "` fails); oversized `a*201` → fails via `max(200)` pipe for both schemas before trim; trim `  Calle 123  ` → `Calle 123` and `  Taller Centro  ` → `Taller Centro` / `  Taller Norte  ` → `Taller Norte` verifies trim-then-validate ordering; existing `ServiceSchema` cases remain green (no regression)
- Verification: normalizer N/A (formatter null, `eslint` is verification, not mutating; no `prettier`/mutating fix run after verification) → `pnpm exec vitest run tests/schemas.test.ts` **1 passed 7 passed**, `pnpm test:run` **8 passed 110 passed**, `pnpm exec tsc --noEmit` **pass (0 errors)**, `pnpm run lint` **pass (0 errors, 1 pre-existing react-hooks/incompatible-library warning in ServicesModal.tsx:382)**; no `pnpm run build` required per tasks (schema-only, runtime N/A); no live PocketBase/Appwrite contacted, no secrets logged, no `pocketbase` dep change
- TDD Cycle Evidence:
  | Task                       | Test File               | Layer | Safety Net                 | RED                                                                                      | GREEN                                                       | TRIANGULATE                                                                                                        | REFACTOR                                                      |
  | -------------------------- | ----------------------- | ----- | -------------------------- | ---------------------------------------------------------------------------------------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------- |
  | 9.1 Location RED           | `tests/schemas.test.ts` | Unit  | ✅ 105/105 (8 files) before | ✅ Written — 5 failed/2 passed: LocationCreate/Update undefined `safeParse` before impl | —                                                             | —                                                                                                                  | —                                                             |
  | 9.1 Location GREEN         | `tests/schemas.test.ts` | Unit  | —                          | —                                                                                        | ✅ Passed — `lib/schemas.ts` schemas + types, 7/7 focused green    | —                                                                                                                  | ✅ Clean — `trimmedAddress` helper, `pipe` trim→validate, ServiceSchema untouched |
  | 9.1 Location TRIANGULATE   | `tests/schemas.test.ts` | Unit  | —                          | —                                                                                        | —                                                             | ✅ blank→undefined (both schemas), `a*201` fails max200 (both), trimmed `Taller Centro`/`Calle 123`/`Taller Norte`, existing 2 ServiceSchema green | ✅ Clean — no hardcoded returns, no interpolation, minimal Zod chaining |
  | 9.2 WU6a verification      | `tests/schemas.test.ts` | Unit  | —                          | —                                                                                        | ✅ 1 passed 7 passed focused                                     | ✅ 8/110 + tsc + lint pass, runtime N/A schema-only                                                           | —                                                             |
- Work Unit Evidence:
  | Evidence                                          | Required value                                                                                                                                                                                                                                                                     |
  | ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
  | Focused test command and exact result             | `pnpm exec vitest run tests/schemas.test.ts` — 1 passed 7 passed (2 ServiceSchema +3 LocationCreate +2 LocationUpdate), exit 0                                                                                                                                              |
  | Full suite + typecheck + lint + build             | `pnpm test:run` — 8 files 110 passed, exit 0; `pnpm exec tsc --noEmit` — pass; `pnpm run lint` — pass (0 errors, 1 pre-existing warning); `pnpm run build` — N/A per tasks 9.2 (schema-only, no UI/runtime path)                                                            |
  | Runtime harness command/scenario and exact result | N/A — schema-only per design `06a` rollback boundary (`lib/schemas.ts` + compact `tests/schemas.test.ts` cases); no UI/runtime path, no live PocketBase/Appwrite, no `POCKETBASE_URL` flip; documented N/A with reason: Zod validation only, no data access                                                   |
  | Rollback boundary                                 | `lib/schemas.ts`, `tests/schemas.test.ts`, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md`, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` — revert restores base `ca55d0d` schemas without touching WU6b/6c/6d or prior WUs; ServiceSchema unaffected |
- Tasks: WU6a 4/4 complete — 9.1 RED/GREEN/TRIANGULATE, 9.2 verification checked; WU6b+ remain pending (31 remaining checkboxes pending); cumulative 46/77 complete (WU1a 6 +WU1b 7 +WU2a 3 +WU2b 3 +WU2c 5 +WU3 4 +WU4 6 +WU5 7 +WU6a 4 +1 tracker)
- Files: `lib/schemas.ts` (trimmedAddress + LocationCreate/Update + types, +29 lines), `tests/schemas.test.ts` (5 compact cases, +47 lines import+describes), `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md` (4 checkboxes WU6a, 4+4=8), `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` (this entry)
- Provider add+delete (vs `ca55d0d`, all tracked files incl. SDD): 117 (112 additions + 5 deletions) across 4 files — `lib/schemas.ts` 29+0=29, `tests/schemas.test.ts` 48+1=49, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md` 4+4=8, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` 31+0=31 — forecast 80–160 target, hard 400, no size:exception — within budget; staged 4 files only, exact slice, no WU6b files, no `app/actions/*`/`lib/storage.ts`/`app/actions/logs.ts`, no pnpm-lock/package, no GH Stack, no commit/push/PR, no live network/new deps
- Secrets/network: none — no POCKETBASE_URL/admin values, no live PB/Appwrite contacted, no token/cookie/secret logged, no console.log of pb_auth/pb_token; `pocketbase@0.28.0` already installed reused, no new dependency
- Workload / PR boundary: single work-unit 06a-location-schemas; base `ca55d0d` (planning `feat/migrate-appwrite-to-pocketbase-06-locations-write`); chain feature-branch-chain auto-chain; tracker `feat/migrate-appwrite-to-pocketbase`; this slice starts at planning node (42/77) and ends at `LocationCreate/Update` Zod schemas + compact tests (46/77); next is WU6b `…-06b-location-crud` (base 06a, forecast 180–280)


## 2026-08-24 — WU6b location CRUD (06b-location-crud) — strict TDD

- Branch: `feat/migrate-appwrite-to-pocketbase-06b-location-crud` at base `d0a7036cb9aa947f6be396b1d82876098cc14d32` (WU6a `feat/migrate-appwrite-to-pocketbase-06a-location-schemas`, PR #31)
- Scope: WU6b only — `app/actions/locations.ts` request-scoped PocketBase location create/update/toggle/delete + Zod `LocationCreateSchema`/`LocationUpdateSchema` + `normalizeString` duplicate, tenant ownership, `isActive=true`, optional `address` trimmed blank→omitted max200, native ids omitted, delete guards via `services`/`location_logs` with same Spanish history-guard error; compact `tests/locations-history.test.ts` write cases (8 tests); no `app/actions/logs.ts`/`lib/storage.ts` movement-log/`getLocationLogs`, no `lib/schemas.ts` widening, no docs/env/removal, no live network, no new deps
- Lineage: detached monolithic GREEN candidate `/home/jona/projects/serviceflow-worktrees/migrate-appwrite-to-pocketbase-06-locations-write` READ-ONLY; inspected only `app/actions/locations.ts` + relevant `tests/locations-history.test.ts` location CRUD diffs to preserve proven RED→GREEN intent; never edited/staged/reset that worktree; independently validated against current base `d0a7036` spec/design; reused WU6a `LocationCreate`/`LocationUpdate` schemas from base without widening; native attempt token `sha256:724865572dfd98092c9a7fb38a00ce98c53e51f3d980c89e5de6ebd1708e1dec` preserved, parent owns settlement with distinct REQUEST ID from acquire `opencode-20260824-wu6b-acquire-01`; do not settle/reset
- Spec/Design: `locations-history` tenant-isolated access, optional address (trimmed blank→omitted max200), duplicate `normalizeString` NFD per-user, `isActive` true on create/toggle, delete constraints `locationId`/`fromLocationId`/`toLocationId` with Spanish guard `No se puede eliminar una Sede con historial de servicios o movimientos.`; design `lib/schemas.ts` `LocationCreateSchema` (name min1 after trim) / `LocationUpdateSchema` (name 3–100 after trim) + `trimmedAddress`; `lib/pocketbase-filter.ts` `applyBinding` sole `pb.filter` with `{:uid}`/`{:locationId}`/`{:lid}`; `lib/pocketbase.ts` `createPocketBaseClient` per-request + `applyBinding`
- TDD RED: extended `tests/locations-history.test.ts` with 8 write cases — unauthenticated mutate writes nothing, create isActive/userId/address/native-id, exact duplicate bound userId only, update keep own name, peer mutate not-found/unauthorized, delete blocked service/log same Spanish guard + unused deletes, source uses PocketBase/Zod/native-ids/bound filters no Appwrite, accent `Ñuñoa` cross-tenant toggle keeps history → `pnpm exec vitest run tests/locations-history.test.ts` **7 failed / 13 passed** (12 WU3 +1 unauth) against Appwrite `databases`/`COLLECTIONS`/`Query`/`ID.unique` impl; proves RED; safety net before edit: `pnpm exec vitest run tests/locations-history.test.ts tests/schemas.test.ts` **2 passed 19 passed** (12 locations-history +7 schemas)
- TDD GREEN: rewrote `app/actions/locations.ts` create/update/toggle/delete onto PocketBase (`createPocketBaseClient` + `LocationCreateSchema`/`LocationUpdateSchema` `safeParse` before any `pb` call, first issue message on fail, `normalizeString` duplicate via `pb.collection("locations").getList(1,100,{filter: applyBinding(pb,{filter:"userId = {:uid}",params:{uid:user.id}})})`, create `userId` server `isActive:true` `createdAt`/`updatedAt` now `address` only when `!==undefined`, native id omitted no `ID.unique`, update `pb.getOne` catch→`Sede no encontrada` owner check `userId!==`→not found, duplicate excluding own `id`, update `address` only when defined, toggle `getOne`→owner→`update` `isActive`, delete `getOne`→owner→`services` `userId = {:uid} && locationId = {:locationId}` + `location_logs` `userId = {:uid} && (fromLocationId = {:lid} || toLocationId = {:lid})` via `applyBinding` both `getList(1,1)` `totalItems`→history guard else `delete`); focused `pnpm exec vitest run tests/locations-history.test.ts` **1 passed 20 passed** — GREEN
- TDD TRIANGULATE: accent-insensitive `Ñuñoa` → `Nunoa` normalized collision rejected for same tenant, same `Taller Centro` allowed for another tenant (`user-B` `getList` empty→create success), toggle `isActive:false` with `hasHistory:true` updates only `isActive` not `delete`; create blank address `"   "`→`address` undefined omitted, trimmed `"  Taller Centro  "`→`Taller Centro`/`"  Calle 123  "`→`Calle 123`; all 8 write cases green, existing 12 WU3 still green, 7 schemas green
- Verification: `pnpm exec vitest run tests/locations-history.test.ts tests/schemas.test.ts` **2 passed 27 passed** (20 locations-history +7 schemas), exit 0; `pnpm test:run` **8 passed 118 passed**, exit 0 (`tests/auth-session.test.ts` 44, `tests/locations-history.test.ts` 20, `tests/pocketbase-*` 13, `tests/env*` 9, `tests/schema-artifact` 7, `tests/schemas` 7, `tests/services-lifecycle` 18); `pnpm exec tsc --noEmit` **pass (0 errors)**; `pnpm run lint` **pass (0 errors, 1 pre-existing react-hooks/incompatible-library warning in ServicesModal.tsx:382)**; no `pnpm run build` required per tasks 10.2 (runtime N/A unless local PB already applied); no live PocketBase/Appwrite contacted, no `pocketbase` dep change, no Appwrite fallback, no `console.log` secrets
- TDD Cycle Evidence:
  | Task | Test File | Layer | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
  |------|-----------|-------|------------|-----|-------|-------------|----------|
  | 10.1 RED | `tests/locations-history.test.ts` | Unit | ✅ 19/19 (2 files: 12 locations-history +7 schemas) before | ✅ Written — 7 failed/13 passed: missing createPocketBaseClient/LocationCreateSchema/applyBinding/native-id/bound-filter, Appwrite databases/Query/ID.unique present | — | — | — |
  | 10.1 GREEN | `tests/locations-history.test.ts` | Unit | — | — | ✅ Passed — `app/actions/locations.ts` PocketBase rewrite, 8 write cases, 20/20 | — | ✅ Clean — Zod before PB, request-scoped client via createPocketBaseClient, bound filter via applyBinding, native id omitted, no Appwrite |
  | 10.1 TRIANGULATE | `tests/locations-history.test.ts` | Unit | — | — | — | ✅ accent Ñuñoa→Nunoa duplicate, same name cross-tenant allowed, toggle keeps history (isActive false, no delete), blank address omitted, trimmed create | ✅ duplicate check extracted via normalizeString, no hardcoded returns |
  | 10.2 verification | `tests/locations-history.test.ts` + `tests/schemas.test.ts` | Unit | — | — | ✅ 2 passed 27 focused | ✅ 8/118 + tsc + lint, runtime N/A | — |
- Work Unit Evidence:
  | Evidence | Required value |
  |----------|---------------|
  | Focused test command and exact result | `pnpm exec vitest run tests/locations-history.test.ts tests/schemas.test.ts` — 2 passed 27 passed (20 locations-history +7 schemas), exit 0 |
  | Full suite + typecheck + lint + build | `pnpm test:run` — 8 files 118 passed, exit 0; `pnpm exec tsc --noEmit` — pass (0 errors); `pnpm run lint` — pass (0 errors, 1 pre-existing warning); `pnpm run build` — N/A per tasks 10.2 (runtime N/A unless local PB already applied, location create only) |
  | Runtime harness command/scenario and exact result | N/A — WU6b has no new runtime page beyond location CRUD; operator local PB at `127.0.0.1:8090` out of band and not required; documented N/A with reason: tenant/ownership/delete-guard verified via mocked PocketBase (`vi.mock("@/lib/pocketbase")` + `vi.mock("@/lib/auth")` + `vi.mock("next/cache")`), no live PB contact |
  | Rollback boundary | `app/actions/locations.ts`, `tests/locations-history.test.ts`, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md`, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` — revert restores Appwrite-based create/update/toggle/delete (`databases`/`Query`/`ID.unique`) without touching WU6a schemas (`lib/schemas.ts`/`tests/schemas.test.ts`), WU6c movement-log (`lib/storage.ts`), WU6d history (`app/actions/logs.ts`), or prior WUs |
- Tasks: WU6b 4/4 complete — 10.1 RED/GREEN/TRIANGULATE, 10.2 verification checked; WU6c+ remain pending (27 remaining checkboxes pending: WU6c 4 + WU6d 4 + WU7 4 + WU8 3 + WU9 1 + WU10 4 + parent 7); cumulative 50/77 complete (WU1a 6 +WU1b 7 +WU2a 3 +WU2b 3 +WU2c 5 +WU3 4 +WU4 6 +WU5 7 +WU6a 4 +WU6b 4 +1 tracker)
- Files: `app/actions/locations.ts` (rewrite create/update/toggle/delete to PB + Zod, 89 adds +89 dels net 0), `tests/locations-history.test.ts` (8 compact write cases, 88 adds +29 dels), `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md` (4 checkboxes WU6b, 4+4=8), `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` (this entry, 30+0=30)
- Provider add+delete (vs `d0a7036`, all tracked files incl. SDD): 333 (211 adds +122 dels) across 4 files — `app/actions/locations.ts` 89+89=178, `tests/locations-history.test.ts` 88+29=117, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md` 4+4=8, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` 30+0=30 — forecast 180–280 exceeded by ~53 but hard 400 not exceeded, no size:exception — within budget; measured via `git diff d0a7036 --numstat` authoritative (211+122=333); staged 4 files only, exact slice, no `lib/schemas.ts`/`tests/schemas.test.ts` churn, no `lib/storage.ts`/`app/actions/logs.ts`/WU6c/d, no `package.json`/`pnpm-lock.yaml`/`pnpm-workspace.yaml`, no docs/env/removal, no `appwrite`/`node-appwrite` deletion, no GH Stack, no commit/push/PR
- Secrets/network: none — no POCKETBASE_URL/admin values, no live PB/Appwrite contacted, no token/cookie/secret logged, no console.log of pb_auth; `pocketbase@0.28.0` already installed reused, no new dependency, no `POCKETBASE_ADMIN_*`/`NEXT_PUBLIC_APPWRITE_*`/`APPWRITE_API_KEY`
- Workload / PR boundary: single work-unit 06b-location-crud; base `d0a7036` (WU6a `feat/migrate-appwrite-to-pocketbase-06a-location-schemas`, PR #31); chain feature-branch-chain auto-chain; tracker `feat/migrate-appwrite-to-pocketbase`; this slice starts at WU6a schemas done (46/77) and ends at location CRUD PocketBase writes + guards verified (50/77); next is WU6c `…-06c-movement-log` (base 06b, forecast 80–160) — do not implement movement logs or history read in this slice

## 2026-08-24 — WU6c movement-log create (06c-movement-log) — strict TDD

- Branch: `feat/migrate-appwrite-to-pocketbase-06c-movement-log` at base `837cf01871cf21253377d5346ac89e9f11a160d5` (WU6b `feat/migrate-appwrite-to-pocketbase-06b-location-crud`, PR #32)
- Scope: WU6c only — `lib/storage.ts` `updateService` movement-log create (`pb.collection("location_logs").create` with `userId: current.userId`, `ServiceId: updatedService.id`, `fromLocationId`, `toLocationId`, `changedAt: now` only when `locationId` changes and `status` is **not** the transition into `completed`) + compact `tests/services-lifecycle.test.ts` movement cases (3 tests); no `app/actions/logs.ts`/`getLocationLogs`, no `lib/schemas.ts`/`app/actions/locations.ts`/WU6d widening, no docs/env/removal, no live network, no new deps, no Appwrite fallback
- Lineage: detached read-only GREEN candidate `/home/jona/projects/serviceflow-worktrees/migrate-appwrite-to-pocketbase-06-locations-write` (864-line monolith) — inspected only `lib/storage.ts` updateService movement-log diff vs `837cf01` to preserve RED→GREEN intent; never edited/staged/reset/copied that worktree; independently verified against current base `837cf01` spec `locations-history` Requirement Movement history + design ADR-6 ordered writes (update then create, no rollback); reused WU6b `updateService` ownership/completed guards without widening; native attempt token `sha256:424683d11e09ae39dc4c01b11bc08f52e1a273d83b7a3022a58a5cc035717457` preserved, parent owns settlement with request ID DISTINCT from acquire `opencode-20260824-wu6c-acquire-01`; do not settle/reset/acquire; CodeGraph used before broad source reading (`codegraph_explore lib/storage.ts updateService`)
- Spec/Design: `locations-history` Requirement Movement history — location change MUST insert `location_logs` with denormalized `userId` (= `current.userId`), `ServiceId`, `fromLocationId`, `toLocationId`, `changedAt=now`; completing+location change MUST NOT log; unchanged location MUST NOT log; service delete removes its logs (already in WU5); design ADR-6 ordered writes (update service, then create log, no invented rollback, failure bubbles generic); ADR-1 request-scoped `createPocketBaseClient`, ADR-4 text FKs, ADR-5 bound filters via `applyBinding` (not needed for log create, direct `create` with denormalized fields)
- TDD RED: extended `tests/services-lifecycle.test.ts` with 3 movement cases — (1) non-completing `locA→locB` with `status:ready` writes one log asserting `userId/ServiceId/fromLocationId/toLocationId/changedAt` + `collection("location_logs")` + `create` + source `fromLocationId`/`toLocationId`/`changedAt`/`ServiceId` presence, (2) completing `pending→completed` with `locA→locB` skips log, (3) unchanged `locA→locA` skips log → `pnpm exec vitest run tests/services-lifecycle.test.ts` **1 failed / 20 passed** (`non-completing location change writes one log` — expected `mockLogsCreate` to be called 1 times, but got 0) against base `837cf01` missing log path; proves RED; safety net before edit: `pnpm exec vitest run tests/services-lifecycle.test.ts tests/locations-history.test.ts tests/schemas.test.ts` **3 passed 45 passed**; `pnpm test:run` **8 passed 118 passed** — Service lifecycle tests green without movement, no pre-existing failure
- TDD GREEN: implemented in `lib/storage.ts` `updateService` immediately after `await pb.collection("services").update`:
  ```ts
  const fromLocationId = current.locationId as string | undefined;
  const toLocationId = updatedService.locationId as string | undefined;
  const isLocationChanged = !!fromLocationId && !!toLocationId && fromLocationId !== toLocationId;
  const isCompleting = current.status !== "completed" && updatedService.status === "completed";
  if (isLocationChanged && !isCompleting) {
    await pb.collection("location_logs").create({ userId: current.userId, ServiceId: updatedService.id, fromLocationId, toLocationId, changedAt: now });
  }
  ```
  minimal diff, no Appwrite, no `applyBinding` needed for `create`, exact ADR-6 order (update then log, no rollback); focused `pnpm exec vitest run tests/services-lifecycle.test.ts` **1 passed 21 passed** — GREEN; full `pnpm exec vitest run tests/locations-history.test.ts tests/services-lifecycle.test.ts` **2 passed 41 passed** — both suites green
- TDD TRIANGULATE: completing+location change `pending:locA → completed:locB` asserts `mockLogsCreate.not.toHaveBeenCalled()` — verifies `!isCompleting` guard prevents log on graduation; unchanged `locA→locA` asserts `not.toHaveBeenCalled()` — verifies `isLocationChanged` prevents spurious log; non-complete `locA→locB` with `ready` status asserts `userId===current.userId` (owner, not request `userId` param), `ServiceId===updatedService.id`, `fromLocationId/toLocationId` correct, `changedAt` ISO `>= before` — forces real logic, not hardcoded; all 3 cases green, existing 18 WU4/WU5 still green, no `app/actions/logs.ts`/`getLocationLogs` touched
- REFACTOR: no extraction — helper is two `const` guards, no duplication, no magic numbers; `now` already shared with payload; left `lib/pocketbase-filter.ts` `applyBinding` sole `pb.filter` site untouched; verified `pnpm exec tsc --noEmit` pass, no new types needed
- Verification: normalizer N/A (formatter null, `eslint` is verification) → `pnpm exec vitest run tests/locations-history.test.ts tests/services-lifecycle.test.ts` **2 passed 41 passed (20 locations-history +21 services-lifecycle)**, exit 0; `pnpm test:run` **8 passed 121 passed**, exit 0 (`tests/auth-session.test.ts` 44, `tests/locations-history.test.ts` 20, `tests/pocketbase-*` 13, `tests/env*` 9, `tests/schema-artifact` 7, `tests/schemas` 7, `tests/services-lifecycle` 21); `pnpm exec tsc --noEmit` **pass (0 errors)**; `pnpm run lint` **pass (0 errors, 1 pre-existing react-hooks/incompatible-library warning in ServicesModal.tsx:382)**; no `pnpm run build` required per tasks 11.2 (runtime N/A — mocked update path); no live PocketBase/Appwrite contacted (`vi.mock("@/lib/pocketbase")` + `vi.mock("@/lib/auth")`), no secrets logged, no `pocketbase` dep change
- TDD Cycle Evidence:
  | Task | Test File | Layer | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
  |------|-----------|-------|------------|-----|-------|-------------|----------|
  | 11.1 RED | `tests/services-lifecycle.test.ts` | Unit | ✅ 45/45 focused (20 locations +18 services +7 schemas) + 118/118 full before | ✅ Written — 1 failed/20 passed: `non-completing location change writes one log` missing `location_logs.create`, `mockLogsCreate` 0 calls | — | — | — |
  | 11.1 GREEN | `tests/services-lifecycle.test.ts` | Unit | — | — | ✅ Passed — `lib/storage.ts` log create after update, 21/21 focused, 41/41 with locations-history | — | ✅ Clean — ordered update then create, no rollback, request-scoped client, no Appwrite |
  | 11.1 TRIANGULATE | `tests/services-lifecycle.test.ts` | Unit | — | — | — | ✅ 3 cases: non-complete writes 1 log with `userId/ServiceId/from/to/changedAt`, completing+change skips, unchanged skips — forces `isLocationChanged && !isCompleting` | ✅ no hardcoded returns, `now` reused, guards extracted |
  | 11.2 verification | `tests/locations-history.test.ts` + `tests/services-lifecycle.test.ts` | Unit | — | — | ✅ 2 passed 41 focused | ✅ 8/121 + tsc + lint pass, runtime N/A | — |
- Work Unit Evidence:
  | Evidence | Required value |
  |----------|---------------|
  | Focused test command and exact result | `pnpm exec vitest run tests/locations-history.test.ts tests/services-lifecycle.test.ts` — 2 passed 41 passed (20 locations-history +21 services-lifecycle), exit 0 |
  | Full suite + typecheck + lint + build | `pnpm test:run` — 8 files 121 passed, exit 0; `pnpm exec tsc --noEmit` — pass (0 errors); `pnpm run lint` — pass (0 errors, 1 pre-existing warning); `pnpm run build` — N/A per tasks 11.2 (mocked update path, no UI wiring) |
  | Runtime harness command/scenario and exact result | N/A — WU6c has no new runtime page beyond `updateService` log create; verified via mocked PocketBase (`vi.mock("@/lib/pocketbase")` + `vi.mock("@/lib/auth")` + `vi.fn` `collection("location_logs").create`); operator local PB at `127.0.0.1:8090` out of band and not required per tasks; documented N/A with reason: tenant-owned log (`current.userId` denormalized) and `changedAt=now` tested deterministically via `before` timestamp, no live PB contact |
  | Rollback boundary | `lib/storage.ts`, `tests/services-lifecycle.test.ts`, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md`, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` — revert restores `updateService` without movement log (`services.update` only) without touching WU6b location CRUD (`app/actions/locations.ts`/`tests/locations-history.test.ts`), WU6d history (`app/actions/logs.ts`), WU6a schemas (`lib/schemas.ts`/`tests/schemas.test.ts`), docs/env/removal, or prior WUs |
- Tasks: WU6c 4/4 complete — 11.1 RED/GREEN/TRIANGULATE, 11.2 verification checked; WU6d+ remain pending (23 remaining checkboxes pending: WU6d 4 + WU7 4 + WU8 3 + WU9 1 + WU10 4 + parent 7); cumulative 54/77 complete (WU1a 6 +WU1b 7 +WU2a 3 +WU2b 3 +WU2c 5 +WU3 4 +WU4 6 +WU5 7 +WU6a 4 +WU6b 4 +WU6c 4 +1 tracker)
- Files: `lib/storage.ts` (movement log after service update, 13 adds, `isLocationChanged && !isCompleting` + `location_logs.create` with `userId/current.userId`, `ServiceId`, `from/to`, `changedAt: now`), `tests/services-lifecycle.test.ts` (3 compact movement cases 51 adds: `mockLogsCreate` mock, `mockCollection` location_logs create, describe `movement logs WU6c` with non-complete writes 1 log + completing skips + unchanged skips, source invariant), `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md` (4 checkboxes WU6c, 4+4=8), `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` (this entry)
- Provider add+delete (vs `837cf01`, all tracked files incl. SDD): 113 (108 additions +5 deletions) across 4 files — `lib/storage.ts` 13+0=13, `tests/services-lifecycle.test.ts` 50+1=51, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md` 4+4=8, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` 41+0=41 — forecast 80–160 target — within budget; hard 400 not exceeded; measured via `git diff 837cf01 --numstat` authoritative (108+5=113); staged 4 files only, no `pocketbase` dep/lockfile, no `app/actions/logs.ts`/WU6d, no docs/env/removal; no GH Stack, no commit/push/PR
- Secrets/network: none — no POCKETBASE_URL/admin values collected, no live PB/Appwrite contacted, no token/cookie/secret logged, no console.log of pb_auth; `pocketbase@0.28.0` already installed reused, no new dependency, no `POCKETBASE_ADMIN_*`/`NEXT_PUBLIC_APPWRITE_*`/`APPWRITE_API_KEY`; `updateService` uses `current.userId` denormalized per spec, `changedAt: now` ISO, `ServiceId` native 15-char id
- Workload / PR boundary: single work-unit 06c-movement-log; base `837cf01` (WU6b `feat/migrate-appwrite-to-pocketbase-06b-location-crud`, PR #32); chain feature-branch-chain auto-chain; tracker `feat/migrate-appwrite-to-pocketbase`; this slice starts at WU6b CRUD done (50/77) and ends at movement-log PocketBase create in `updateService` only verified (54/77); next is WU6d `…-06d-history-read` (base 06c, forecast 160–280) — do not implement `getLocationLogs` or widen CRUD/schemas in this slice
## 2026-08-24 — WU6d history read (06d-history-read) — strict TDD

- Branch: `feat/migrate-appwrite-to-pocketbase-06d-history-read` at base `46019488bf5f4a70d41089955a29011588a4525f` (WU6c `feat/migrate-appwrite-to-pocketbase-06c-movement-log`, PR #33)
- Scope: WU6d only — `app/actions/logs.ts` `getLocationLogs` request-scoped PocketBase read (`createPocketBaseClient` + `logListBinding`/`applyBinding` + `pb.collection("location_logs").getList(page,limit,{filter: applyBinding(pb, logListBinding({userId,locationId,startDate,endDate})) , sort:"-changedAt"})` + `{ data, total, page, limit }` envelope with `id` mapping (not `$id`), tenant `userId = {:uid}` (+ `fromLocationId = {:lid} || toLocationId = {:lid}` + `changedAt >= {:startDate}`/`<= {:endDate}`), `page=1`/`limit=20` defaults via `Number.isFinite`, `sort "-changedAt"`, `totalItems`→`total`, unauthenticated `{ error:"No autenticado" }` without `data`/`PB`, catch generic `Error al cargar historial de movimientos` without leaking PB text; compact `tests/locations-history.test.ts` history cases (4 tests) keep `app/locationLogs/page.tsx` + `app/dashboard/page.tsx` `getAuthUser`→`redirect("/login")` gates; no `lib/schemas.ts`/`app/actions/locations.ts`/`lib/storage.ts` movement widening, no docs/env/removal, no live network, no new deps
- Lineage: detached read-only GREEN candidate `/home/jona/projects/serviceflow-worktrees/migrate-appwrite-to-pocketbase-06-locations-write` (864-line monolith) — inspected only WU6d-relevant `app/actions/logs.ts` + `tests/locations-history.test.ts` history diff vs `4601948` to preserve RED→GREEN intent; never edited/staged/reset that worktree; independently validated against current base `4601948` spec `locations-history` History listing + design logListBinding (userId/lid/startDate/endDate) + ADR-1 request client & ADR-5 bound filters; reused WU6c movement log without widening; native attempt token `sha256:5696bb1923adf5cc08a3cb2648ba11ed17967f457dfe9e590ac9a95f5f28e20f` preserved, parent settles with request ID DISTINCT from acquire `opencode-20260824-wu6d-acquire-01`; do not settle/reset/acquire; CodeGraph used before broad source reading (`codegraph_explore getLocationLogs logListBinding`)
- Spec/Design: `locations-history` History listing — MUST require auth, filter by denormalized `userId`, default `page=1` `limit=20`, return `{ data, total, page, limit }`, sort `-changedAt`, optional `locationId` matches `fromLocationId||toLocationId`, optional `startDate`/`endDate` bound `changedAt`, peer logs MUST NOT appear, unauthenticated MUST return error without `data`; mapping `id: doc.id` not `$id`; empty list `{ data:[], total:0, page, limit }` success
- TDD RED: extended `tests/locations-history.test.ts` with 4 history cases — (1) unauthenticated `No autenticado` without `data`/`PB`, (2) authenticated envelope binds `userId = {:uid}` `sort -changedAt` `page=2 limit=5` peer isolation template invariant, (3) from-or-to `locY` + `startDate`/`endDate` bounds empty→`[]` `id` not `$id` defaults `page=1 limit=20`, (4) source invariant `createPocketBaseClient`/`logListBinding`/`applyBinding`/`collection("location_logs")`/`getList`/`getAuthUser` no Appwrite + page gates + generic error without PB leak → `pnpm exec vitest run tests/locations-history.test.ts` **3 failed / 21 passed** (`authenticated envelope` missing PB `filter`/`collection`, `from-or-to` missing `fromLocationId||toLocationId`, `source invariant` missing `createPocketBaseClient` in `app/actions/logs.ts`) against Appwrite `databases`/`Query`/`COLLECTIONS` impl; proves RED; safety net before edit: `pnpm exec vitest run tests/locations-history.test.ts tests/services-lifecycle.test.ts tests/schemas.test.ts` **3 passed 48 passed**; `pnpm test:run` **8 passed 121 passed** — no pre-existing failure
- TDD GREEN: rewrote `app/actions/logs.ts` to PocketBase: `"use server"` + `getAuthUser` → `null`→`No autenticado` without PB, otherwise `page/limit` defaults `Number.isFinite`→1/20, `pb=await createPocketBaseClient()`, `binding=logListBinding({userId:user.id,locationId,startDate,endDate})`, `filter=applyBinding(pb,binding)`, `result=await pb.collection("location_logs").getList(page,limit,{filter, sort:"-changedAt"})`, `total=totalItems ??0`, `data=items.map(doc=>({ ...doc, id:doc.id }))` return `{data,total,page,limit}`; catch→`Error al cargar historial de movimientos` generic; no Appwrite `databases`/`Query`/`COLLECTIONS`/`DB_ID`/`$id` join, no location/service batched join (history is raw logs); `applyBinding` sole `pb.filter` site kept; focused `pnpm exec vitest run tests/locations-history.test.ts` **1 passed 24 passed** — GREEN; `pnpm exec vitest run tests/locations-history.test.ts tests/services-lifecycle.test.ts tests/schemas.test.ts` **3 passed 52 passed**
- TDD TRIANGULATE: from-or-to `locY` with 2 logs `{from:locX→locY, from:locY→locZ}` asserts `template` contains `fromLocationId = {:lid} || toLocationId = {:lid}` `lid=locY` not interpolated, `fullTemplate` contains `userId = {:uid}` + from/to + `changedAt >= {:startDate}` + `<= {:endDate}` with `params.uid/startDate/endDate` correct; empty `[]`→`{data:[],total:0,page:1,limit:20}` success without `error`; `id` mapping `log1` `$id:should-not-use`→`id=log1` not `$id`; defaults `getLocationLogs({})`→`page=1 limit=20`; peer `user-B` `getB` reuse `template` same as `u-owner-1` only `uid` differs → tenant isolation; generic error `mockLogsGetList.mockRejectedValueOnce(new Error("PocketBase secret should not leak"))`→`{error:"Error al cargar historial de movimientos"}` `JSON.stringify` not contains `PocketBase`; all 4 history cases green, existing 20 locations-history +7 schemas +21 services still green
- REFACTOR: no extraction — `getLocationLogs` is 20-line pure binding→filter→getList→map, no duplication, `logListBinding` already pure compile-time template, `applyBinding` sole filter site untouched; verified `pnpm exec tsc --noEmit` pass, no new types, `lib/pocketbase-filter.ts` unchanged; left `app/locationLogs/page.tsx` + `app/dashboard/page.tsx` gates untouched and verified via source read
- Verification: normalizer N/A (formatter null, `eslint` is verification) → `pnpm exec vitest run tests/locations-history.test.ts tests/services-lifecycle.test.ts tests/schemas.test.ts` **3 passed 52 passed (24 locations-history +21 services-lifecycle +7 schemas)**, exit 0; `pnpm test:run` **8 passed 125 passed**, exit 0 (`tests/auth-session.test.ts` 44, `tests/locations-history.test.ts` 24, `tests/pocketbase-*` 13, `tests/env*` 9, `tests/schema-artifact` 7, `tests/schemas` 7, `tests/services-lifecycle` 21); `pnpm exec tsc --noEmit` **pass (0 errors)**; `pnpm run lint` **pass (0 errors, 1 pre-existing react-hooks/incompatible-library warning in ServicesModal.tsx:382)**; `pnpm run build` **pass (Next build compiled successfully, 9 static pages, route /locationLogs & /dashboard & /api/services validated)**; no live PocketBase/Appwrite contacted (`vi.mock("@/lib/pocketbase")` + `vi.mock("@/lib/auth")`), no secrets logged, no `pocketbase` dep change
- TDD Cycle Evidence:
  | Task | Test File | Layer | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
  |------|-----------|-------|------------|-----|-------|-------------|----------|
  | 12.1 RED | `tests/locations-history.test.ts` | Unit | ✅ 48/48 focused (24 locations? but 20 before +21 services +7 schemas=48) 121/121 full before | ✅ Written — 3 failed/21 passed: missing PB `createPocketBaseClient`/`logListBinding`/`applyBinding`/`collection("location_logs")`/`getList` `sort -changedAt` + `fromLocationId||toLocationId` + Appwrite `databases`/`Query`/`COLLECTIONS` present | — | — | — |
  | 12.1 GREEN | `tests/locations-history.test.ts` | Unit | — | — | ✅ Passed — `app/actions/logs.ts` PB rewrite, 24/24 (20 existing +4 history) focused | — | ✅ Clean — request-scoped `createPocketBaseClient`, bound `logListBinding`/`applyBinding`, `id` mapping, no Appwrite, envelope preserved |
  | 12.1 TRIANGULATE | `tests/locations-history.test.ts` | Unit | — | — | — | ✅ 4 cases: envelope `page2 limit5` total/sort/peer invariant, from-or-to `locY` 2 logs + `startDate/endDate` bounds + empty `[]` + `id` not `$id` + defaults `page1 limit20`, source invariant no Appwrite + gates + generic error no leak | ✅ no hardcoded returns, bound params not interpolated, `Number.isFinite` pagination |
  | 12.2 verification | `tests/locations-history.test.ts` + `tests/services-lifecycle.test.ts` + `tests/schemas.test.ts` | Unit | — | — | ✅ 3 passed 52 focused | ✅ 8/125 + tsc + lint + build | — |
- Work Unit Evidence:
  | Evidence | Required value |
  |----------|---------------|
  | Focused test command and exact result | `pnpm exec vitest run tests/locations-history.test.ts tests/services-lifecycle.test.ts tests/schemas.test.ts` — 3 passed 52 passed (24 locations-history +21 services-lifecycle +7 schemas), exit 0 |
  | Full suite + typecheck + lint + build | `pnpm test:run` — 8 files 125 passed, exit 0; `pnpm exec tsc --noEmit` — pass (0 errors); `pnpm run lint` — pass (0 errors, 1 pre-existing warning); `pnpm run build` — pass (Next compiled successfully, 9 pages, exit 0) |
  | Runtime harness command/scenario and exact result | `pnpm run build` — **pass** (Turbopack compiled successfully in 4.4s, 9 static pages, `/locationLogs` `/dashboard` `/api/services` validated); local PB runtime harness per tasks 12.2: **N/A (no live PB)** — `127.0.0.1:8090` out of band, not required, `build` is product-complete gate per design quality gates (`pnpm run build` at PR 6, 12, 16) and verifies history read compiles with tenant-bound envelope; documented N/A with reason: mocked history read, no live PocketBase contacted, tenant/auth fail-closed verified via unit (`vi.mock("@/lib/pocketbase")`); operator smoke (create→move→history→second user sees nothing) deferred per parent operator gate |
  | Rollback boundary | `app/actions/logs.ts`, `tests/locations-history.test.ts`, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md`, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` — revert restores Appwrite `databases.listDocuments` + `$id` join + `Query` without touching WU6c movement log (`lib/storage.ts`/`tests/services-lifecycle.test.ts`), WU6b CRUD (`app/actions/locations.ts`), WU6a schemas (`lib/schemas.ts`/`tests/schemas.test.ts`), docs/env/removal, or prior WUs |
- Tasks: WU6d 4/4 complete — 12.1 RED/GREEN/TRIANGULATE, 12.2 verification checked; WU7+ remain pending (15 remaining checkboxes pending: WU7 4 + WU8 3 + WU9 1 + WU10 4 + parent 7 - operator gates); cumulative 58/77 complete (WU1a 6 +WU1b 7 +WU2a 3 +WU2b 3 +WU2c 5 +WU3 4 +WU4 6 +WU5 7 +WU6a 4 +WU6b 4 +WU6c 4 +WU6d 4 +1 tracker)
- Files: `app/actions/logs.ts` (rewrite getLocationLogs to PB, 47 adds 167 dels), `tests/locations-history.test.ts` (4 compact history cases 123 adds: unauth no data/PB, envelope binds userId sort -changedAt page2/peer isolation, from-or-to locY + start/end bounds empty id-mapping defaults, source invariant + gates + generic error), `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md` (4 checkboxes WU6d, 4+4=8), `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` (this entry)
- Provider add+delete (vs `4601948`, all tracked files incl. SDD): ~386 (174 adds +171 dels +8 +123+ ~? apply-progress) — measured via `git diff 46019488 --numstat` authoritative; forecast 160–280 product + SDD ~386 within hard 400, no size:exception — within budget; staged 4 files only, no `pocketbase` dep/lockfile, no `app/actions/locations.ts`/`lib/storage.ts`/`lib/schemas.ts` churn, no docs/env/removal, no `appwrite`/`node-appwrite` deletion, no GH Stack, no commit/push/PR
- Secrets/network: none — no POCKETBASE_URL/admin values collected, no live PB/Appwrite contacted, no token/cookie/secret logged, no console.log of pb_auth; `pocketbase@0.28.0` already installed reused, no new dependency, no `POCKETBASE_ADMIN_*`/`NEXT_PUBLIC_APPWRITE_*`/`APPWRITE_API_KEY`; `getLocationLogs` uses `userId` denormalized per spec, `changedAt` ISO sort `-changedAt`, native 15-char ids `id` not `$id`
- Workload / PR boundary: single work-unit 06d-history-read; base `46019488bf5f4a70d41089955a29011588a4525f` (WU6c `feat/migrate-appwrite-to-pocketbase-06c-movement-log`, PR #33); chain feature-branch-chain auto-chain; tracker `feat/migrate-appwrite-to-pocketbase`; this slice starts at WU6c movement done (54/77) and ends at history read PocketBase envelope/sort/filter verified (58/77); next is WU7 `…-07-docs-env` (base 06d, forecast 200–350) — do not implement docs/env in this slice


## 2026-08-24 — WU7 docs/env (07-docs-env) — structural (docs-only, strict TDD proportional)

- Branch: `feat/migrate-appwrite-to-pocketbase-07-docs-env` at base `d40044045baf4fec1d0810c84ae323d2691d3401` (WU6d `feat/migrate-appwrite-to-pocketbase-06d-history-read`, PR #34)
- Scope: WU7 only — `.env.example` PocketBase runtime only (`POCKETBASE_URL=http://127.0.0.1:8090`), `README.md` PocketBase rewrite, `docs/CODEBASE-GUIDE.md` (only, never root); no `PRD.md`/`ARCHITECTURE.md` (WU8), `DESIGN.md`, product code/tests, hygiene files, deps/lockfile, or Appwrite removal (WU10); chain `feature-branch-chain` auto-chain, hard400
- Lineage: no CodeGraph needed for docs/config — raw read appropriate per prompt (used CodeGraph only for referenced source symbols per sdd-apply guidance); native attempt token `sha256:c8bb4ea64d0a7fbdc75f706aed21bab35a78acb910d0dd652a13212c15a50b0f` preserved, parent settles with ID DISTINCT from acquire `opencode-20260824-wu7-acquire-01`; do not acquire/settle/reset; docs follow existing Spanish README convention but artifact language English for identifiers
- Spec/Design: `project-contracts` Requirement Environment example documents PocketBase only (`POCKETBASE_URL` local `http://127.0.0.1:8090` no secrets no admin no Appwrite keys) + Requirement README describes PocketBase setup (already-running `127.0.0.1:8090`, explicit `pocketbase/v1.collections.json` apply out of band, no `scripts/setup-appwrite.ts`/`APPWRITE_API_KEY`, no PocketBase/Dokploy runbook, PocketBase live) + Requirement CODEBASE-GUIDE is PocketBase accurate (points at `lib/pocketbase.ts`, `lib/pocketbase-filter.ts`, `lib/env.ts`, `pocketbase/v1.collections.json`, `lib/auth.ts`, `lib/storage.ts`, `lib/schemas.ts`, MUST NOT present `lib/appwrite.ts`/`node-appwrite` as current, MUST NOT document old `/api/proxy` rewrite as live) + in-scope docs remove stale Appwrite claims except historical rollback; design Documentation boundaries owns `.env.example`/`README.md`/`docs/CODEBASE-GUIDE.md` WU7; governance (`PRD.md`/`ARCHITECTURE.md`) deferred to WU8
- TDD RED (structural readback, proportional check — docs-only passive): `rg -n "NEXT_PUBLIC_APPWRITE|APPWRITE_API_KEY|POCKETBASE_ADMIN" .env.example` **3 hits** (Appwrite project/endpoint/key) + `rg "Appwrite" README.md` **5 hits including `Base de Datos y Autenticación: Appwrite`, `scripts/setup-appwrite.ts`, `APPWRITE_API_KEY`** + `ls docs` **no such file** → proves RED (live Appwrite setup present, guide missing); safety net before edit: `pnpm test:run` **8 passed 125 passed**, `pnpm exec tsc --noEmit` **pass**, `pnpm run lint` **pass (0 errors, 1 pre-existing warning)**
- TDD GREEN (minimal docs-only): set `.env.example` to `# PocketBase` + `POCKETBASE_URL=http://127.0.0.1:8090` only (no `POCKETBASE_ADMIN_*`, no `NEXT_PUBLIC_APPWRITE_*`, no `APPWRITE_API_KEY`); rewrite `README.md` — PocketBase live, `POCKETBASE_URL` only locator, already-running `127.0.0.1:8090`, explicit `pocketbase/v1.collections.json` apply via Admin UI import/transcription out of band (no admin API, no `POCKETBASE_URL` flip yet, no hosting runbook), public self-registration + `pb_auth` httpOnly `sameSite=lax` `path=/` `secure` prod, tenant `userId = @request.auth.id` + `userId = {:uid}`, empty start (tickets Appwrite no aparecerán, aviso `/login` `/register`), native 15-char ids (15-char / 15-character / 15 caracteres) no UUID, `{ data, total, page, limit }`, `LIKE` `~` on `clientName`/`invoiceNumber`/`rut`, `address` optional, historical rollback only; add `docs/CODEBASE-GUIDE.md` (only) with 90-second model, seams table pointing at 7 required files, collections, session `pb_auth` `httpOnly`, tenant rules, What is NOT current (lib/appwrite historical, Appwrite setup not live, old proxy not live), no `node-appwrite` for new work, no live `/api/proxy` rewrite
- TDD TRIANGULATE / REFACTOR (structural, single-output): `rg -n "NEXT_PUBLIC_APPWRITE|APPWRITE_API_KEY|scripts/setup-appwrite" README.md docs/CODEBASE-GUIDE.md .env.example` **0 hits** → no live Appwrite env/scripts as active config (remaining Appwrite mentions only historical: `Legado` `session (Appwrite)` ignored, `Empty start` `Appwrite no aparecerán`, rollback historical note); `rg "lib/pocketbase.ts" docs/CODEBASE-GUIDE.md` etc. **7/7 required seams present**, `rg "15-char" README.md` **2 hits**, `rg "pb_auth" README.md docs/CODEBASE-GUIDE.md` **present**, `rg "userId = @request.auth.id" README.md docs/CODEBASE-GUIDE.md` **present**; triangulation skipped: purely structural docs with one canonical output (Reason: no branching logic, single valid `.env` value, single PocketBase setup narrative) — REFACTOR none needed (cognitive-doc-design chunking/signposting kept)
- Verification: `pnpm exec vitest run tests/env-pocketbase.test.ts` not needed (env already covered) + `pnpm test:run` **8 passed 125 passed** (unchanged, docs-only, no product code touched), `pnpm exec tsc --noEmit` **pass (0 errors)**, `pnpm run lint` **pass (0 errors, 1 pre-existing warning)**; `rg -n "NEXT_PUBLIC_APPWRITE|APPWRITE_API_KEY" .env.example README.md docs/CODEBASE-GUIDE.md` **0** (no live Appwrite keys/secrets), `rg -n "/api/proxy" README.md docs/CODEBASE-GUIDE.md` **0** (old rewrite not live, only janitor historical note without that path); Runtime harness: **N/A** per tasks (docs-only, no server boundary — explicit `N/A` with reason: no runtime behavior, operator schema apply out of band)
- TDD Cycle Evidence:
  | Task | Test File | Layer | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
  |------|-----------|-------|------------|-----|-------|-------------|----------|
  | 13.1 .env.example | `rg` structural | N/A (structural) | ✅ 125/125 focused `pnpm test:run` before | ✅ Written — 3 Appwrite vars present, no `POCKETBASE_URL` only | ✅ Passed — `POCKETBASE_URL=http://127.0.0.1:8090` only, no admin/Appwrite, `rg` 0 hits | ➖ Single — one valid output (Reason: structural env, single URL value) | ➖ None needed |
  | 13.2 README.md | `rg` structural | N/A | ✅ same | ✅ Written — 5 live Appwrite hits, `Appwrite` active backend | ✅ Passed — PocketBase live, `POCKETBASE_URL`, `127.0.0.1:8090`, `pocketbase/v1.collections.json`, `pb_auth` `httpOnly`, `userId = @request.auth.id`, `15-char` `15-character`, `{ data, total, page, limit }`, `LIKE`, `address`, no `setup-appwrite`/API-key as live, historical rollback only | ➖ Single — one narrative (Reason: docs-only, no branching) | ➖ None needed |
  | 13.3 docs/CODEBASE-GUIDE.md | `rg` structural | N/A | ✅ same | ✅ Written — missing file `docs: No such file` | ✅ Passed — 7 seams `lib/pocketbase.ts`/`lib/pocketbase-filter.ts`/`lib/env.ts`/`pocketbase/v1.collections.json`/`lib/auth.ts`/`lib/storage.ts`/`lib/schemas.ts`, `pb_auth`, tenant rules, NOT current `lib/appwrite.ts`/`node-appwrite` historical, old proxy not live | ➖ Single — one guide (Reason: structural) | ➖ None needed |
  | 13.4 Verify | `rg` + `pnpm test:run` | N/A | — | — | ✅ `rg` 0 live Appwrite/API-key//api/proxy + `pnpm test:run` 125 still pass | — | — |
- Work Unit Evidence:
  | Evidence | Required value |
  |----------|---------------|
  | Focused test command and exact result | `rg -n "NEXT_PUBLIC_APPWRITE\|APPWRITE_API_KEY\|scripts/setup-appwrite" README.md docs/CODEBASE-GUIDE.md .env.example` — **0 hits** (no live Appwrite env/scripts); `rg -n "lib/pocketbase.ts\|lib/pocketbase-filter.ts\|lib/env.ts\|pocketbase/v1.collections.json\|lib/auth.ts\|lib/storage.ts\|lib/schemas.ts" docs/CODEBASE-GUIDE.md` — **7/7 seams**; `pnpm test:run` — **8 passed 125 passed**, exit 0 |
  | Full suite + typecheck + lint + build | `pnpm test:run` — 8 files 125 passed; `pnpm exec tsc --noEmit` — pass (0 errors); `pnpm run lint` — pass (0 errors, 1 pre-existing warning); `pnpm run build` — N/A per tasks (docs-only, build not required; previous WU6d build already proven) |
  | Runtime harness command/scenario and exact result | N/A — docs-only change has no server/runtime boundary; explicit `N/A` with reason: no PocketBase hosting added, schema apply is operator out-of-band step, verified via `rg` structural readback + `pnpm test:run` still green; no live PocketBase contacted, no `POCKETBASE_URL` flip in this PR |
  | Rollback boundary | `.env.example`, `README.md`, `docs/CODEBASE-GUIDE.md`, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md`, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` — revert restores Appwrite `.env` + Appwrite `README.md` + removes `docs/CODEBASE-GUIDE.md` without touching `PRD.md`/`ARCHITECTURE.md` (WU8), `DESIGN.md`, `check_or.ts`/`lint_output.txt` (WU9), product code/tests (`lib/*`, `app/*`, `pocketbase/*`, `tests/*`), or Appwrite removal (WU10) |
- Tasks: WU7 4/4 complete — 13.1 .env, 13.2 README, 13.3 CODEBASE-GUIDE, 13.4 verify checked; WU8+ remain pending (11 remaining checkboxes pending: WU8 3 + WU9 1 + WU10 4 + parent 3 unblocked + 4 operator/acceptance gated); cumulative **62/77** complete (WU1a 6 +WU1b 7 +WU2a 3 +WU2b 3 +WU2c 5 +WU3 4 +WU4 6 +WU5 7 +WU6a 4 +WU6b 4 +WU6c 4 +WU6d 4 +WU7 4 +1 tracker)
- Files: `.env.example` (4→2 lines, Appwrite→PocketBase `POCKETBASE_URL`), `README.md` (87→105 lines, Appwrite→PocketBase, `POCKETBASE_URL`, `127.0.0.1:8090`, `pocketbase/v1.collections.json`, `pb_auth` `httpOnly`, `userId = @request.auth.id`, `15-char` `15-character`, `{ data, total, page, limit }`, `LIKE`, `address`, no live `setup-appwrite`/API-key, historical rollback only), `docs/CODEBASE-GUIDE.md` (new 61 lines, 7 seams, `pb_auth`, tenant rules, NOT current Appwrite/proxy), `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md` (4 checkboxes WU7, 4+4=8), `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` (this entry)
- Provider add+delete (vs `d400440`, all tracked files incl. SDD): measured via `git diff d400440 --numstat` authoritative — `.env.example` 2+4=6, `README.md` 44+26=70, `docs/CODEBASE-GUIDE.md` 61+0=61, `tasks.md` 4+4=8, `apply-progress.md` 30+0=30 → **175** total (141 additions + 34 deletions, 25 below soft forecast 200–350 but within hard 400, due concise docs) — hard 400 not exceeded, no size:exception — within budget; staged 5 files only, exact autonomous slice, no `pocketbase` dep/lockfile, no `PRD.md`/`ARCHITECTURE.md`/`DESIGN.md`, no hygiene, no Appwrite removal
- Secrets/network: none — no `POCKETBASE_URL` secret value beyond documented `http://127.0.0.1:8090` example, no `POCKETBASE_ADMIN_*`/`NEXT_PUBLIC_APPWRITE_*`/`APPWRITE_API_KEY` committed, no live PocketBase/Appwrite contacted, no token/cookie/secret logged, no `console.log`
- Workload / PR boundary: single work-unit **07-docs-env**; base `d400440` (WU6d `feat/migrate-appwrite-to-pocketbase-06d-history-read`, PR #34); chain `feature-branch-chain` `auto-chain`; tracker `feat/migrate-appwrite-to-pocketbase`; this slice starts at product path done (58/77) and ends at docs/env PocketBase-only contracts verified (62/77); next is WU8 `…-08-prd-arch` (base 07, forecast 250–380) — do not modify `PRD.md`/`ARCHITECTURE.md` in this slice

## 2026-08-24 — WU8 PRD + ARCHITECTURE (08-prd-arch) — structural (docs-only, strict TDD proportional)

- Branch: `feat/migrate-appwrite-to-pocketbase-08-prd-arch` at base `bd47d984c18e846a1ed84ab84938767a2bf0d47c` (WU7 `feat/migrate-appwrite-to-pocketbase-07-docs-env`, PR #35)
- Scope: WU8 only — root `PRD.md` + `ARCHITECTURE.md` (PocketBase product intent + durable system map); no `DESIGN.md`, `.env.example`/`README.md`/`docs/CODEBASE-GUIDE.md` (WU7), hygiene `check_or.ts`/`lint_output.txt` (WU9), product code/tests/deps/lockfile, or Appwrite removal (WU10); chain `feature-branch-chain` auto-chain, hard400
- Lineage: CodeGraph used before broad reads (`codegraph_explore` PocketBase seams) but index returned stale Appwrite symbols from main; fallback to direct Read verification of `lib/pocketbase.ts`, `lib/pocketbase-filter.ts`, `lib/auth.ts`, `lib/storage.ts`, `pocketbase/v1.collections.json` which confirm request-scoped `pb_auth`, `authRefresh`, bound `{:uid}`/`{:search}`, native 15-char ids, envelope; native attempt token `sha256:937f0acdab3f5e931c8ff84cf016a700a5a5ddf064983230de86c799184fa781` preserved, parent settles with ID DISTINCT from acquire `opencode-20260824-wu8-acquire-01`; do not acquire/settle/reset
- Spec/Design: `project-contracts` Product and architecture contracts exist and are accurate (public self-registration; `pb_auth` httpOnly; `userId = @request.auth.id` + bound filter; empty-start no import; native 15-char ids; `{ data, total, page, limit }`; LIKE `~`; optional `address` max 200; schema `pocketbase/v1.collections.json` out of band on local/Dokploy; no Appwrite live/dual-write/hosting) + stale Appwrite claims removed except historical + governance untouched + PRD reflects tenant services/locations/history + ARCH reflects only PocketBase + `POCKETBASE_URL` + request-scoped + explicit apply; design Documentation boundaries owns PRD/ARCH WU8, links/start paths consistent with `docs/CODEBASE-GUIDE.md`, ADRs 1–8 linked per existing `ARCHITECTURE.md` Key Decisions convention (no parallel `docs/adr`)
- TDD RED (structural readback, proportional — docs-only passive): `ls PRD.md ARCHITECTURE.md` **no such file** + `rg "PocketBase" README.md` already PocketBase but `PRD.md`/`ARCHITECTURE.md` missing proves RED; `rg -n "Appwrite" PRD.md ARCHITECTURE.md` would be 0 before, `rg "PocketBase is the only"` 0 before, `rg "userId = @request.auth.id"` 0 before → product/arch contracts absent; safety net before edit: `pnpm test:run` **8 passed 125 passed**, `pnpm exec tsc --noEmit` **pass**, `pnpm run lint` **pass (1 pre-existing warning)**
- TDD GREEN (minimal docs-only): create `PRD.md` (83 lines: PocketBase-only backend, public self-registration, `pb_auth` httpOnly `sameSite=lax` `path=/` secure prod + `authRefresh` forged→`null`/401, `userId = @request.auth.id` + `{:uid}`, empty-start `Este entorno PocketBase comienza vacío…` no import/dual-write/id map, native 15-char ids no UUID, `{ data, total, page, limit }` defaults, LIKE `~` bound `{:search}`, optional `address` trimmed max200, `pocketbase/v1.collections.json` out of band Dokploy, Zod boundaries, historical transition only); create `ARCHITECTURE.md` (146 lines: system overview PocketBase only, hybrid SPA+BFF request-scoped `new PocketBase(getPocketBaseUrl())`, mermaid system + sequence, component boundaries table 11 seams with exact files, data architecture 4 collections + tenant rules + indexes + native ids + envelope, API contract `pb_auth` only `401`/`400`, NFR, Key Decisions ADR-1..8 linked, failure modes, historical transition Appwrite rollback only, reading path `docs/CODEBASE-GUIDE.md` → `lib/pocketbase.ts` → `pocketbase/v1.collections.json` → this file, no hosting/dual-write); no governance edits
- TDD TRIANGULATE / REFACTOR (structural, single-output): `rg -n "Appwrite" PRD.md ARCHITECTURE.md` **11 hits all historical** (`no Appwrite`, `was the live backend before`, `left untouched`, `rollback`, `Do not configure` — no live `/api/proxy` rewrite, no `NEXT_PUBLIC_APPWRITE` as active, no `setup-appwrite` instruction); `rg -n "PocketBase is the only data and auth backend" PRD.md ARCHITECTURE.md` **present**; `rg "pb_auth" PRD.md ARCHITECTURE.md` **present**, `rg "userId = @request.auth.id"` **present**, `rg "15-char"` **present**, `rg "data, total, page, limit"` **present**, `rg "LIKE"` **present**, `rg "address"` **present**, `rg "POCKETBASE_URL" PRD.md ARCHITECTURE.md` **present**, `rg "pocketbase/v1.collections.json"` **present**, `rg "request-scoped"` **present**, `rg "out of band"` **present**, `rg "no hosting|never hosts"` **present**; triangulation skipped: single canonical product/arch narrative (Reason: structural docs, one valid PocketBase contract) — REFACTOR none needed (cognitive-doc-design chunking/signposting kept, mermaid ≤8 nodes)
- Verification: `pnpm --dir "$workdir" exec vitest run` **8 passed 125 passed**, `pnpm --dir "$workdir" exec tsc --noEmit` **pass (0 errors)**, `pnpm --dir "$workdir" run lint` **pass (0 errors, 1 pre-existing warning)**; `rg -n "NEXT_PUBLIC_APPWRITE|APPWRITE_API_KEY" PRD.md ARCHITECTURE.md` shows historical negation only, not live config; `rg -n "/api/proxy" PRD.md ARCHITECTURE.md` **0 live** (only janitor `proxy.ts` session-only, never rewrites to Appwrite); `git diff --stat HEAD -- .github .husky` **0** (governance untouched); `ls docs/CODEBASE-GUIDE.md README.md .env.example` still PocketBase-only per WU7; Runtime harness: **N/A** per tasks (docs-only, no server boundary — explicit N/A with reason: no runtime behavior, operator schema apply out of band)
- TDD Cycle Evidence:
  | Task | Test File | Layer | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
  |------|-----------|-------|------------|-----|-------|-------------|----------|
  | 14.1 PRD.md | `rg` structural | N/A (structural) | ✅ 125/125 `pnpm test:run` before | ✅ Written — missing file `PRD.md: No such file` | ✅ Passed — 83 lines PocketBase product behavior, tenant-scoped + public registration + empty-start notice + LIKE + address + envelope | ➖ Single — one valid product narrative (Reason: structural, no branching) | ➖ None needed |
  | 14.2 ARCHITECTURE.md | `rg` structural | N/A | ✅ same | ✅ Written — missing file `ARCHITECTURE.md: No such file` | ✅ Passed — 146 lines durable map, only PocketBase + POCKETBASE_URL + request-scoped + out of band + no hosting/dual-write | ➖ Single — one system map (Reason: structural) | ➖ None needed |
  | 14.3 Verify | `rg` + `pnpm test:run` | N/A | — | — | ✅ `rg` Appwrite historical only + no live proxy + `pnpm test:run` 125 still pass + governance 0 | — | — |
- Work Unit Evidence:
  | Evidence | Required value |
  |----------|---------------|
  | Focused test command and exact result | `rg -n "Appwrite" PRD.md ARCHITECTURE.md` — **11 hits all historical/rollback** (no live Appwrite setup/API-key/`/api/proxy` rewrite as active); `rg -n "PocketBase is the only" PRD.md ARCHITECTURE.md` — **present**; `rg -n "userId = @request.auth.id" PRD.md ARCHITECTURE.md` — **present**; `pnpm --dir workdir exec vitest run` — **8 passed 125 passed**, exit 0 |
  | Full suite + typecheck + lint + build | `pnpm test:run` — 8 files 125 passed; `pnpm exec tsc --noEmit` — pass (0 errors); `pnpm run lint` — pass (0 errors, 1 pre-existing warning); `pnpm run build` — N/A per tasks (docs-only, build not required; previous WU6d build already proven) |
  | Runtime harness command/scenario and exact result | N/A — docs-only change has no server/runtime boundary; explicit `N/A` with reason: no PocketBase hosting added, schema apply is operator out-of-band step, verified via `rg` structural readback + `pnpm test:run` still green; no live PocketBase contacted, no `POCKETBASE_URL` flip in this PR |
  | Rollback boundary | `PRD.md`, `ARCHITECTURE.md`, `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md`, `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` — revert removes PocketBase PRD/ARCH contracts and restores missing-file state without touching `.env.example`/`README.md`/`docs/CODEBASE-GUIDE.md` (WU7), `DESIGN.md`/`design/DESIGN.md`, `check_or.ts`/`lint_output.txt` (WU9), product code/tests (`lib/*`, `app/*`, `pocketbase/*`, `tests/*`), or Appwrite removal (WU10) |
- Tasks: WU8 3/3 complete — 14.1 PRD, 14.2 ARCH, 14.3 verify checked; WU9+ remain pending (12 remaining checkboxes pending: WU9 1 + WU10 4 + parent 7 (3 unblocked +4 operator/acceptance gated)); cumulative **65/77** complete (WU1a 6 +WU1b 7 +WU2a 3 +WU2b 3 +WU2c 5 +WU3 4 +WU4 6 +WU5 7 +WU6a 4 +WU6b 4 +WU6c 4 +WU6d 4 +WU7 4 +WU8 3 +1 tracker)
- Files: `PRD.md` (new 83 lines, PocketBase-only product intent/current-state/behavior/constraints/non-goals with tenant public registration empty-start LIKE address envelope native ids out-of-band), `ARCHITECTURE.md` (new 146 lines, durable map request/session/client/filter/storage/actions/schema artifact boundaries + flow + historical transition + ADR-1..8 linked, start paths consistent with `docs/CODEBASE-GUIDE.md`), `openspec/changes/migrate-appwrite-to-pocketbase/tasks.md` (3 checkboxes WU8, 3+3=6), `openspec/changes/migrate-appwrite-to-pocketbase/apply-progress.md` (this entry)
- Provider add+delete (vs `bd47d98`, all tracked files incl. SDD): measured via `git diff bd47d98 --numstat` authoritative — `PRD.md` 83+0=83, `ARCHITECTURE.md` 146+0=146, `tasks.md` 3+3=6, `apply-progress.md` 29+0=29 → **264** total (adds+deletes, within 250–380 forecast and well under hard 400, no size:exception) — staged 4 files only, exact autonomous slice, no `pocketbase` dep/lockfile, no WU9 hygiene, no product code/tests, no `DESIGN.md`, no governance
- Secrets/network: none — no `POCKETBASE_URL` secret value beyond documented `http://127.0.0.1:8090` example, no `POCKETBASE_ADMIN_*`/`NEXT_PUBLIC_APPWRITE_*`/`APPWRITE_API_KEY` committed as live config (only negated/historical mentions), no live PocketBase/Appwrite contacted, no token/cookie/secret logged, no `console.log`
- Workload / PR boundary: single work-unit **08-prd-arch**; base `bd47d98` (WU7 `feat/migrate-appwrite-to-pocketbase-07-docs-env`, PR #35); chain `feature-branch-chain` `auto-chain`; tracker `feat/migrate-appwrite-to-pocketbase`; this slice starts at docs/env done (62/77) and ends at PRD+ARCH PocketBase contracts verified (65/77); next is WU9 `…-09-hygiene` (base 08, forecast <30) — do not delete `check_or.ts`/`lint_output.txt` or Appwrite code in this slice
