# Apply Progress: Remediate Audit Findings (F1–F8)

## Work Unit: S1 — Removal + Hygiene (F2/F5/F6) — PR #1

- **Delivery**: auto-chain / feature-branch-chain, PR #1 targets feature/tracker branch
- **Mode**: Strict TDD (removal-only unit — no behavioral RED applicable, see evidence)
- **Slice boundary**: tasks 1.1, 1.2, 1.4 only. Task 1.3 was explicitly split out of S1 by Jona and moved to cleanup PR #2 (see reconciliation below). S2+ untouched.
- **Branch**: `docs/project-audit-hardening` (feature branch; no commits made by apply — deferred to parent)

## Task Status (cumulative)

| Task                                                             | Status | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1.1 `git rm proxy.ts` — `/api/proxy/*` gone (F2, SA-4)           | [x]    | `git rm proxy.ts` done; tracked file gone. proxy.ts was an ACTIVE Next 16 auto-loaded proxy entrypoint (root-level `proxy` export + `config.matcher`), so zero imports prove nothing. Post-deletion `pnpm build` exit 0; authoritative `.next` manifests (functions-config-manifest, middleware-manifest, routes-manifest, app-path-routes-manifest) register no proxy and no `/api/proxy` — pre-deletion build did register `/_middleware` matcher `/api/proxy/:path*` (snapshot /tmp/opencode/s1-correction/)                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 1.2 `git rm check_or.ts` (F5, CI-7)                              | [x]    | `git rm check_or.ts` done; tracked file gone; zero refs outside `.git/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 1.3 Commit `design/` + `lint_output.txt` deletions (F5/CI-7)     | [x]    | **PR #2 completed (2026-08-13).** Staged exactly the 14 approved paths (13 `design/` + `lint_output.txt`) as deletions via `git add -- design lint_output.txt`; staged-set delta vs pre-state = exactly these 14 (pre-state staged: `check_or.ts`, `package-lock.json`, `proxy.ts` only — S1 paths, untouched). Text deletions 1,433 (DESIGN.md 39; dashboard code.html 300; gestión code.html 191; historial code.html 335; login code.html 125; modal code.html 196; registro code.html 163; lint_output.txt 84); 6 PNGs binary (`Bin → 0 bytes`). Structural readback: all 14 tracked in HEAD (`git ls-tree -r --name-only HEAD -- design lint_output.txt` = exactly 14), absent from worktree (per-path `test -e`), staged `D` ×14; 0 untracked files under allowed paths. `size:exception` approved by Jona for this PR #2 scope only (14 paths, ~1,433 text deletions). Runtime harness N/A — passive document/image deletion, no executable/runtime boundary. |
| 1.4 `lib/appwrite.ts`: drop debug log + commented warn (F6/SA-5) | [x]    | `console.log("[Appwrite Debug] Session set on client")` and 2 commented warn lines removed (1.4 hunk: 3 deleted lines in `createSessionClient`); pre-existing DB_ID dirty edit preserved untouched in a separate hunk. `pnpm test:run` 17/17 after edit; `tsc --noEmit` clean; grep `Appwrite Debug` → 0 hits.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

Remaining: 3.1–3.6 (PR 4), 4.1–4.3 (PR 5), 5.1–5.3 (PR 6), 6.1–6.4 (PR 7) — 16 tasks, not started.

## Task 1.3 Split Reconciliation (bookkeeping-only, 2026-08-13)

Jona explicitly split task 1.3 out of S1/PR #1. This entry records the reconciliation; no product bytes changed.

- **S1/PR #1 completed tasks**: 1.1, 1.2, 1.4 only. S1 does NOT include 1.3 and does NOT include the ~1,433 deleted lines of the `design/` + `lint_output.txt` cleanup.
- **1.3 now belongs to cleanup PR #2** (tasks.md work-unit table, unit 2). PR #2 is >400 changed lines by deletion size; the later `size:exception` is **not approved**.
- **Index reconciliation performed**: `git restore --staged design lint_output.txt` — the 13 `design/` deletions and the `lint_output.txt` deletion were unstaged ONLY; `proxy.ts`, `check_or.ts`, `package-lock.json` and every other staged path were left untouched. Deletions are preserved in the working tree as unstaged deletions (`D` in `git status --porcelain`).
- **Verified exact sets** (no overlap): staged deletions = `check_or.ts`, `package-lock.json`, `proxy.ts` (3); unstaged deletions = 13 `design/` paths + `lint_output.txt` (14).
- **tasks.md**: checkbox state already matched authoritative values (1.1/1.2/1.4 `[x]`, 1.3 `[ ]`) — no edit made.
- **Next state: blocked** pending an explicit `size:exception` for cleanup PR #2. No apply work on 1.3 may start before approval.
- **Tests: N/A** — bookkeeping-only reconciliation; no product bytes changed, so no test/harness run is applicable.

## Work Unit: S2 — Obsolete Artifact Cleanup (F5/CI-7) — PR #2 (task 1.3)

- **Delivery**: auto-chain / feature-branch-chain, PR #2 base = PR #1 branch (parent constructs branch/PR; apply stages only)
- **Mode**: Strict TDD **N/A per module** — pure passive deletion of documents/images/lint log; no executable or runtime boundary exists. No tests written; none fabricated (a filesystem-absence assertion would be tautological and is banned by assertion-quality rules).
- **`size:exception` scope (approved by Jona 2026-08-13, Engram #6286)**: applies ONLY to the 14 obsolete cleanup paths — 13 under `design/` + `lint_output.txt` (~1,433 text deletions). Does NOT cover functional changes, later slices, or any other PR.
- **Slice boundary**: task 1.3 only. No other task, path, or product byte touched.
- **Branch**: parent-managed (PR #2 branch derives from PR #1 branch).

### Work Unit Evidence (S2 — 14 allowed paths only)

| Evidence                                          | Required value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Focused structural command and exact result       | `git add -- design lint_output.txt` → staged-set delta vs pre-state = exactly 14 paths (`design/DESIGN.md`, 6× `design/*_serviceflow/{code.html,screen.png}`, `lint_output.txt`). `git diff --cached --name-status -- design lint_output.txt` → `D` ×14. `git status --porcelain -- design lint_output.txt` pre-stage → `D` ×14 (unstaged deletions, 0 `??`); post-stage → `D` ×14. Text deletions 1,433 (`--numstat`: DESIGN.md 39, dashboard 300, gestión 191, historial 335, login 125, modal 196, registro 163, lint_output.txt 84); 6 PNGs binary `Bin → 0 bytes`. |
| Runtime harness command/scenario and exact result | **N/A** — passive artifact deletion (HTML mockups, PNG screenshots, design doc, lint log). No executable code, no runtime boundary, nothing to execute. Structural readback is the proportional complete check: (1) all 14 paths tracked in HEAD — `git ls-tree -r --name-only HEAD -- design lint_output.txt` = exactly 14 entries; (2) absent from worktree — per-path `test -e` → absent ×14; (3) staged as deletions — `D` ×14 in cached name-status.                                                                                                               |
| Rollback boundary                                 | `git restore --staged design lint_output.txt` (unstages exactly these 14; returns to the S1-reconciliation pre-state) and, to fully restore files, `git restore --source=HEAD -- design lint_output.txt` (all 14 return from HEAD byte-identical). Covers ONLY these 14 paths — `check_or.ts`, `package-lock.json`, `proxy.ts` (S1 staged) and all other dirty/untracked files are untouched by this rollback.                                                                                                                                                          |

## Work Unit: S3 — pnpm + DB Identity (F3/F4) — PR #3 (tasks 2.1–2.3)

- **Delivery**: auto-chain / feature-branch-chain, PR #3 base = PR #2 branch (parent constructs branch/PR; apply stages only)
- **Mode**: Strict TDD **non-behavior handling per module** — configuration/contract unit (package-manager lockfile governance + a DB identifier constant). No new runtime behavior is specified; a fabricated behavioral RED would violate assertion-quality rules. Safety-net baseline + post-change structural verification recorded as explicit evidence (same precedent as S1/S2).
- **Slice boundary**: tasks 2.1–2.3 only. No other task, path, or product byte touched. `next-env.d.ts` (unrelated dirty), `.github/*`, `.husky/`, `openspec/`, `scripts/migrate-appwrite-permissions.ts`, `tests/permissions.test.ts` (later units) untouched.
- **Branch**: parent-managed (PR #3 branch derives from PR #2 branch).

## Task Status (S3)

| Task                                                                   | Status | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2.1 `pnpm-workspace.yaml`; lockfile churn, appwrite@22 gone (F4, CI-6) | [x]    | `pnpm-workspace.yaml` exists and staged as new (10 lines: `allowBuilds` esbuild/sharp/unrs-resolver — pnpm 11.1.1 gates OK, no `dangerouslyAllowAllBuilds`; `overrides` handlebars/lodash/postcss/undici). Static manifest↔lockfile consistency check (no install run — deterministic from current files, per orchestrator gate): all 17 deps + 19 devDeps present in lockfile importer, specifiers identical, 0 extra entries; only delta = `postcss` (`^8.5.20` vs `>=8.5.10`) explained by the workspace `overrides: postcss` entry (pnpm records override-effective specifier; resolved 8.5.20 satisfies both). `appwrite@22` absent from `package.json` (only `appwrite:*` script names remain) and from `pnpm-lock.yaml` (only `node-appwrite@14.2.0` importer + 2 package entries). lockfileVersion 9.0. Lockfile churn (646+/303− vs HEAD) is the task-mandated F4 content (next 16.2.10, vitest 4.1.0, husky, lint-staged added; appwrite removed). |
| 2.2 `git rm --cached package-lock.json` + `.gitignore` (CI-6)          | [x]    | Tracked lockfile audit: `git ls-files` → only `pnpm-lock.yaml` (package-lock.json already removed from index — staged `D` since before batch 1, verified untouched). Authored edit: `.gitignore` += `package-lock.json` under `# Dependencias`; `git check-ignore -v package-lock.json` → `.gitignore:13:package-lock.json`; `git add -A --dry-run` no longer lists it. Untracked regenerated `package-lock.json` (10,255 lines, on disk) preserved as evidence, now invisible to git — never re-enters commits.                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2.3 `lib/appwrite.ts` DB → `serviceflow-db` (F3/SA-3)                  | [x]    | `DB_ID = "serviceflow-db"` in `lib/appwrite.ts` (DB_ID hunk staged — hunk-split so PR #1's 1.4 hunk stays unstaged), `scripts/setup-appwrite.ts` (staged), `scripts/migrate-appwrite-permissions.ts`, `tests/permissions.test.ts`. `Service-system-db` absent from all product code (only openspec change docs — untracked). Consumers (`app/actions/*`, `lib/storage.ts`) import `DB_ID` — no direct references to fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

## TDD Cycle Evidence (S3)

Configuration/contract unit: strict-tdd `RED` requires a failing behavioral test referencing production code that does not exist yet. No new behavior is specified by tasks 2.1–2.3 (lockfile governance is enforced structurally; `DB_ID` is a constant, not logic). Per orchestrator instruction, non-behavior handling applies: safety-net baseline + post-change structural verification recorded as explicit evidence instead of a fabricated RED.

| Task | Test File              | Layer | Safety Net               | RED                   | GREEN                                                                                              | TRIANGULATE              | REFACTOR       |
| ---- | ---------------------- | ----- | ------------------------ | --------------------- | -------------------------------------------------------------------------------------------------- | ------------------------ | -------------- |
| 2.1  | none (config/contract) | N/A   | ✅ 17/17 (2 files)       | N/A — no new behavior | ✅ static consistency: 17+19 specifiers match, 0 extras, appwrite@22 absent; `tsc --noEmit` exit 0 | ➖ Single (no branching) | ➖ None needed |
| 2.2  | none (config/contract) | N/A   | ✅ 17/17 (same baseline) | N/A — no new behavior | ✅ `git ls-files` → pnpm-lock.yaml only; check-ignore matches; add -A dry-run excludes it          | ➖ Single                | ➖ None needed |
| 2.3  | none (constant value)  | N/A   | ✅ 17/17 (same baseline) | N/A — no new behavior | ✅ grep `Service-system-db` → 0 hits in product code; DB_ID present in 4 files                     | ➖ Single (one constant) | ➖ None needed |

### Test Summary (cumulative)

- **Total tests written**: 0 across all units (removal/config units — none applicable; no fabricated tests)
- **Total tests passing**: 17/17 (2 files) before and after all changes
- **Layers used**: N/A (no behavior added)
- **Approval tests**: None — no behavioral refactor
- **Pure functions created**: 0

## Work Unit Evidence (S3 — tasks 2.1–2.3)

| Evidence                                          | Required value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Focused test command and exact result             | `pnpm exec tsc --noEmit` → exit 0 ("No errors"). `pnpm test:run` → 2 files passed, 17 tests passed (identical before/after). Structural: `git ls-files` lockfiles = `pnpm-lock.yaml` only; manifest↔lockfile importer specifier comparison = 17/17 deps + 19/19 devDeps match (postcss delta explained by workspace override); `appwrite@22` → 0 hits in package.json + pnpm-lock.yaml; `Service-system-db` → 0 hits outside openspec docs; `git check-ignore -v package-lock.json` → `.gitignore:13`; `git add -A --dry-run` → package-lock.json not listed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Runtime harness command/scenario and exact result | **N/A** — configuration/typecheck unit with no runtime boundary: changes are package-manager contract files (workspace config, lockfiles, .gitignore) and one DB identifier constant (`DB_ID`). No executable path is added or altered by this unit; the constant is only READ at Appwrite runtime, which requires the isolated dev environment (S4 unit, PR #5 — out of scope, and no live Appwrite I/O is permitted here). Typecheck + full test suite cover every statically verifiable behavior; lockfile consistency was verified deterministically from current files without any install/network.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Rollback boundary                                 | `git restore --staged pnpm-workspace.yaml .gitignore package.json pnpm-lock.yaml lib/appwrite.ts scripts/setup-appwrite.ts` → unstages exactly this unit's 6 paths, returning the index to the batch-2 state (S1 staged deletions `check_or.ts`/`proxy.ts` and PR #2's 14 deletions untouched). Content restore: `git restore --source=HEAD -- pnpm-workspace.yaml package.json pnpm-lock.yaml` (untracked workspace file disappears; tracked files return byte-identical); `.gitignore`: remove the `package-lock.json` line; `lib/appwrite.ts`: revert ONLY the DB_ID hunk (surgical — NEVER `git checkout -- lib/appwrite.ts`, which would destroy the unstaged 1.4 hunk belonging to PR #1); `package-lock.json`: `git restore --staged package-lock.json` (tracked version returns to index; regenerated untracked copy stays on disk, ignored). No unrelated dirty files (`next-env.d.ts`, `.github/*`, `.husky/`, `openspec/`, `scripts/migrate-appwrite-permissions.ts`, `tests/permissions.test.ts`) are touched by these reverts. |

## Index Set Accounting (S3 — pre/post)

- **PRE (batch-2 state)**: staged = `D` ×19 (`check_or.ts`, `proxy.ts`, `package-lock.json`, 13 `design/` paths, `lint_output.txt`); unstaged `M` = `.gitignore`, `lib/appwrite.ts`, `next-env.d.ts`, `package.json`, `pnpm-lock.yaml`, `scripts/setup-appwrite.ts`; untracked = `.github/*`, `.husky/`, `ARCHITECTURE.md`, `DESIGN.md`, `PRD.md`, `openspec/`, `package-lock.json` (regenerated), `pnpm-workspace.yaml`, `scripts/migrate-appwrite-permissions.ts`, `tests/permissions.test.ts`.
- **POST (after S3 + 2026-08-13 index-attribution correction)**: staged delta = exactly 6 paths — `A pnpm-workspace.yaml`; `M package.json` (2.1 hunks ONLY: name/packageManager/engines, `appwrite` dep removed, next/eslint-config-next/postcss/vitest bumps — 9+/6−); `M .gitignore` (2.2 line ONLY: `package-lock.json` under `# Dependencias` — 1+); `M pnpm-lock.yaml` (full churn, preserved); `M lib/appwrite.ts` (DB_ID hunk ONLY — 1.4 hunk stays unstaged for PR #1); `M scripts/setup-appwrite.ts` (DB_ID hunk ONLY — Role/perms hunk stays unstaged for 3.6). `D package-lock.json` pre-staged, untouched. S1/PR #2 staged sets byte-identical to PRE (verified via cached name-status). Unstaged remainders (worktree-only, bytes preserved): `package.json` prepare/lint-staged/appwrite-scripts hunks; `.gitignore` workspace rename + `lint_output.txt` lines; `scripts/setup-appwrite.ts` import + perms hunks; `lib/appwrite.ts` 1.4 hunk; `next-env.d.ts`. Untracked: `package-lock.json` dropped from status (now ignored; file preserved on disk); all other untracked sets identical to PRE.

## Line Budget (S3 / PR #3 — corrected 2026-08-13)

- **Authored by this apply**: 1 line (`.gitignore` package-lock.json entry). Staging actions only.
- **Staged unit content (pre-existing worktree hardening, unit-3-attributable, corrected)**: `pnpm-workspace.yaml` 10 (+10), `package.json` 15 (9+/6− — 2.1 hunks only), `.gitignore` 1 (1+ — 2.2 line only), `lib/appwrite.ts` 2 (1+/1−), `scripts/setup-appwrite.ts` 2 (1+/1− — DB_ID hunk only). Authored changed lines = **30 ≤ 40** ✓ (correction budget bound).
- **CORRECTED BUDGET INTERPRETATION**: lockfiles are NOT automatically generated goldens — the chained-pr/work-unit-commits carve-out excludes generated goldens only. `pnpm-lock.yaml` churn (949) and `package-lock.json` deletion (10,255) therefore COUNT toward the 400-line review budget. PR #3 raw diff ≈ 11,234 lines (30 authored + lockfile content) → **> 400, NO size exception approved → next state BLOCKED** pending a path-scoped `size:exception` for the PR #3 lockfile diff (parent to ask Jona separately).
- **Removed false rationale (2026-08-13 correction)**: prior wording claimed lockfiles excluded from the authored risk count ("generated lockfiles excluded from authored count... 55 lines ≤ 400 ✓ — no size exception needed") and that the chained-pr skill's "generated-diff carve-out applies" to the PR #3 lockfile diff. Both claims are FALSE: the carve-out is generated-goldens-only; lockfiles are not goldens. Corrected numbers above supersede the prior 55-line authored claim (which also counted future-scope hunks).

## Correction (Index Attribution + Engram Mirror — 2026-08-13, PR #3 slice isolation)

Index-attribution correction only; no product/worktree bytes changed, no commit, no PR, no tests run, no checkbox edits.

- **Method**: for the 3 mixed files, constructed exact isolated blobs from HEAD content (`git show HEAD:path`) in /tmp/opencode/pr3/, wrote them via `git hash-object -w` + `git update-index --cacheinfo 100644,<blob>,<path>`. Worktree bytes verified byte-identical before/after (`sha256sum -c` = OK ×4: package.json, .gitignore, scripts/setup-appwrite.ts, lib/appwrite.ts). No `git add -p` interactive staging, no `git restore` on worktree content.
- **PRE (this correction)**: staged blobs contained future-scope hunks — package.json 21+/7− (whole file), .gitignore 3+/1− (whole file), scripts/setup-appwrite.ts 3+/8− (whole file).
- **POST (exact PR3 attribution)**:
  - `package.json` staged = 2.1 hunks ONLY (9+/6−): name `serviceflow`, `packageManager: pnpm@11.1.1`, `engines.node >=22.22.1`, `appwrite@^22.0.0` removed, next `^16.2.10`, eslint-config-next `^16.2.10`, postcss `^8.5.20`, vitest `^4.1.0` — all consistent with staged pnpm-lock.yaml importer.
  - `.gitignore` staged = 2.2 line ONLY (1+): `package-lock.json` at `.gitignore:13` under `# Dependencias` (`git check-ignore -v` re-verified → `.gitignore:13:package-lock.json`).
  - `scripts/setup-appwrite.ts` staged = 2.3 DB_ID ONLY (1+/1−): `Service-system-db` → `serviceflow-db`.
  - `lib/appwrite.ts` staged = DB_ID ONLY (1+/1−) — preserved, untouched.
  - `pnpm-workspace.yaml` (A), `pnpm-lock.yaml` (M), `package-lock.json` (D), S1 (`check_or.ts`, `proxy.ts` D), PR #2 (13 `design/` + `lint_output.txt` D) — preserved exactly.
- **Deferred unstaged hunks by future PR** (worktree bytes preserved, simply not in the PR #3 index):
  - **PR #7 (CI governance / 6.2)**: `package.json` `prepare: husky` script, `lint-staged` config block, `husky` + `lint-staged` devDeps.
  - **PR #4 (3.x) / PR #5 (4.x)**: `package.json` `appwrite:setup`, `appwrite:migrate-permissions`, `appwrite:migrate-permissions:apply` scripts (3.5 removes the `:apply` alias).
  - **PR #4 / task 3.6**: `scripts/setup-appwrite.ts` `Permission, Role` import line + `createCollection` `Role.any()`→`[]` perms hunk (2+/7−).
  - **PR #1 attribution (unchanged)**: `lib/appwrite.ts` unstaged 1.4 debug-log/warn hunk (3−).
  - **Unattributed/unrelated**: `.gitignore` workspace rename (`Service-system.code-workspace`→`serviceflow.code-workspace`, F3 naming) and `lint_output.txt` ignore line; `next-env.d.ts` dirty (unrelated).
- **Engram mirror sync**: tasks observation (#6272) updated to match authoritative tasks.md — 2.1–2.3 now `[x]`; PR #2 note aligned to tasks.md line 24 ("Jona approved `size:exception` for PR #2 only: 14 paths..."). tasks.md itself NOT edited (checkbox edits forbidden; file already authoritative).
- **Next state: blocked** — PR #3 lockfile-inclusive diff ≈ 11,234 lines > 400; parent must obtain a path-scoped `size:exception` from Jona for the PR #3 lockfile diff before PR construction. No further apply work on PR #3.

## Correction (PR3 Lockfile Regeneration — 2026-08-13, post size-exception approval)

Lockfile-only correction; no product/worktree bytes changed besides the single staged lockfile path, no commit, no PR, no Appwrite access, no lifecycle scripts run, no global config modified.

- **`size:exception` approved (Jona, 2026-08-13)**: scope = PR #3 lockfile diff ONLY — `pnpm-lock.yaml` churn + `package-lock.json` deletion. Does NOT cover functional changes, later slices (PR #4–7), or any other PR. tasks.md updated to record the approval (word count preserved <530; 7/23 checkboxes untouched; PR #2 approval wording and later-slice non-approval preserved).
- **Regeneration method (isolated scratch, no worktree mutation)**: seeded /tmp/opencode/pr3-lockfile-regen/ with the byte-exact staged blobs — `git show :package.json` (blob de1e61f8, sha256 1eb11814…) and `git show :pnpm-workspace.yaml` (sha256 de597fd5…) — then `pnpm install --lockfile-only --ignore-scripts` (pnpm 11.1.1, registry reachable) → exit 0, only 12 deprecation warnings, no errors. No Appwrite access; no global config touched.
- **Result**: regenerated `pnpm-lock.yaml` (sha256 7136f707218585356119c7b6cf77a52bdc5ad1dabd238df48ad815aa156a9025; git blob dea5f43b) staged via `git add pnpm-lock.yaml` — the ONLY path staged in this correction. Worktree + index of every other path byte-preserved (hash table below).
- **Consistency (frozen-lockfile semantics)**: deterministic importer verification on the staged blob: 17/17 dependencies + 17/17 devDependencies match the staged manifest specifiers exactly; 0 extras; sole delta = `postcss` (`^8.5.20` manifest vs `>=8.5.10` importer) — workspace `overrides: postcss` entry; verified identical behavior in the old staged lockfile (same value). husky → 0 hits, lint-staged → 0 hits, appwrite@22 → 0 hits, node-appwrite@14.2.0 present (importer + 2 package entries). Old staged lockfile contamination confirmed and removed: importer carried husky ^9.1.7 + lint-staged ^17.0.8 (19 devDeps) because it had been generated from the dirty worktree manifest.
- **Frozen reproducibility check**: fresh isolated dir /tmp/opencode/pr3-lockfile-frozen/ seeded with regenerated lockfile + staged manifests → `pnpm install --lockfile-only --frozen-lockfile --ignore-scripts` → exit 0 ("ok"), lockfile byte-identical after the check (sha256 unchanged). No install, no lifecycle scripts.
- **`pnpm exec tsc --noEmit`** → exit 0 (run because the lockfile operation changed effective workspace resolution: fresh resolution moves in-range deps to latest — next 16.3.0, react 19.2.8, zod 4.4.3, vitest 4.1.10, postcss 8.5.26, node-appwrite 14.2.0). No Appwrite.
- **Staged line counts (PR #3, corrected)**: `pnpm-lock.yaml` 2150+/1879− vs HEAD (4,029 changed lines — larger than the prior 949 because regeneration resolves all in-range deps to latest); `package-lock.json` −10,255 (unchanged, staged since batch 1). Both inside the approved lockfile exception scope. Other staged paths unchanged (`.gitignore` 1+, `package.json` 9+/6−, `pnpm-workspace.yaml` +10, `lib/appwrite.ts` 1+/1−, `scripts/setup-appwrite.ts` 1+/1−; S1 + PR #2 `D` ×19 byte-identical).
- **Hash preservation (proven)**: worktree `package.json` sha256 199b4b46… before == after; staged `package.json` blob de1e61f8 unchanged; worktree `.gitignore` b1c7698d…, `lib/appwrite.ts` dbb63e5d…, `scripts/setup-appwrite.ts` 7476e6ec…, `next-env.d.ts` 7b550dda… all unchanged; unstaged diff stat identical (17+/13− across 5 files). Evidence at /tmp/opencode/pr3-correction/evidence/.
- **State: READY** — PR #3 index isolated to tasks 2.1–2.3 hunks + regenerated lockfile; exception approved; next action = parent constructs PR #3. PR #4 (3.1–3.6) remains the next apply unit.

## TDD Cycle Evidence (S1)

Removal-only unit: strict-tdd `RED` requires a failing behavioral test referencing production code that does not exist yet. No new behavior is specified by tasks 1.1–1.4; a fabricated test (e.g., asserting a file is absent) would be a tautological filesystem assertion, banned by assertion-quality rules. Per orchestrator instruction, non-behavior handling applies: safety-net baseline + post-change verification recorded as explicit evidence instead of a fabricated RED.

| Task | Test File           | Layer | Safety Net                      | RED                                            | GREEN                                                                                      | TRIANGULATE                                      | REFACTOR       |
| ---- | ------------------- | ----- | ------------------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------ | ------------------------------------------------ | -------------- |
| 1.1  | none (removal-only) | N/A   | ✅ 17/17 (2 files)              | N/A — removal-only, no new behavior to specify | ✅ `pnpm test:run` 17/17 after rm; `tsc --noEmit` clean; post-deletion `pnpm build` exit 0 | ➖ Single (no branching; nothing to triangulate) | ➖ None needed |
| 1.2  | none (removal-only) | N/A   | ✅ 17/17 (same baseline)        | N/A — removal-only                             | ✅ `pnpm test:run` 17/17 after rm; `tsc --noEmit` clean                                    | ➖ Single                                        | ➖ None needed |
| 1.4  | none (removal-only) | N/A   | ✅ 17/17 (baseline before edit) | N/A — removal-only                             | ✅ `pnpm test:run` 17/17 after edit; `tsc --noEmit` clean; grep `Appwrite Debug` → 0 hits  | ➖ Single                                        | ➖ None needed |

(1.3 evidence is recorded in the S2 work-unit section above — Strict TDD N/A per module for pure passive deletion, no fabricated tests.)

### Test Summary

- **Total tests written**: 0 (removal-only unit — none applicable; no fabricated tests)
- **Total tests passing**: 17/17 (2 files) before and after all changes
- **Layers used**: N/A (no behavior added)
- **Approval tests**: None — no behavioral refactor; deletion-only
- **Pure functions created**: 0

## Work Unit Evidence (S1 — proxy.ts, check_or.ts, lib/appwrite.ts only)

| Evidence                                          | Required value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Focused test command and exact result             | `pnpm test:run` → 2 files passed, 17 tests passed (identical before and after). `pnpm exec tsc --noEmit` → "No errors found". `pnpm run lint` → 0 errors (1 pre-existing warning in unrelated form component)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Runtime harness command/scenario and exact result | Post-deletion runtime harness for 1.1: `pnpm build` (next build 16.2.10) → exit 0; generated route table lists only existing app routes (no `/api/proxy`). proxy.ts was an ACTIVE Next 16 auto-loaded proxy entrypoint (root-level file exporting `proxy` + `config.matcher`), NOT dead code: the pre-deletion build's authoritative `.next/server/functions-config-manifest.json` registered `/_middleware` with matcher `/api/proxy/:path*`, i.e. Next compiled it and every matching request was rewritten to `NEXT_PUBLIC_APPWRITE_ENDPOINT` with no auth gate. After deletion, the same manifest paths show `functions: {}`, `middleware-manifest.json` is empty, `routes-manifest.json` has no `/api/proxy` in staticRoutes/dynamicRoutes/rewrites/redirects, `app-path-routes-manifest.json` has no proxy, and grep over `.next/server` + root `.next/*.json` (incl. cache/standalone) → 0 hits for `api/proxy`. Deletion changed runtime semantics: the unauthenticated forwarding path is removed. Before/after manifest bytes snapshotted at /tmp/opencode/s1-correction/ (pre-deletion-functions-config-manifest.json vs post-build manifests). S1 harness covers only the 1.1 deletion; the `design/`/`lint_output.txt` deletions (1.3) are outside this unit. |
| Rollback boundary                                 | S1 rollback covers proxy.ts, check_or.ts, lib/appwrite.ts ONLY. proxy.ts + check_or.ts: `git restore --staged proxy.ts check_or.ts` + `git restore proxy.ts check_or.ts` (files return from HEAD). lib/appwrite.ts: a full `git checkout -- lib/appwrite.ts` would DESTROY the pre-existing DB_ID edit (`Service-system-db` → `serviceflow-db`), so revert only the 1.4 hunk — the removed `console.log("[Appwrite Debug] Session set on client")` line and the 2 commented warn lines in `createSessionClient` (3 deleted lines, first diff hunk) — via a surgical hunk revert, preserving the separate `DB_ID` hunk untouched. The `design/` + `lint_output.txt` deletions are NOT part of S1 rollback — they belong to cleanup PR #2 (1.3) and are left untouched by any S1 revert. No unrelated dirty files (`.gitignore`, `package.json`, `pnpm-lock.yaml`, `scripts/`, `next-env.d.ts`) are touched by these reverts.                                                                                                                                                                                                                                                                                                                                                |

## Deviations

1. **Task 1.3 split out of S1 by Jona** — 1.3 (commit `design/` + `lint_output.txt` deletions) no longer belongs to PR #1; it moved to cleanup PR #2, pending and blocked on an explicit `size:exception`. Deletions remain in the worktree, unstaged. No commit was performed for 1.3 (apply must not commit; deferred to parent at PR construction).
2. **No fabricated RED test** — removal-only unit per orchestrator's non-behavior handling directive; evidence above replaces a test that would violate assertion-quality rules.
3. **Line budget accounting (corrected)** — S1 authored deletions: `proxy.ts` 28, `check_or.ts` 1, `lib/appwrite.ts` 1.4 hunk 3 = ~32 authored deletions, 0 additions (the `DB_ID` 1+/1− is a pre-existing dirty edit, not authored by apply) ≤ 400. The `design/` + `lint_output.txt` deletions (~1,433 text lines, 14 paths) are NOT in S1 scope and NOT counted in PR #1 — they are the PR #2 cleanup (>400 by deletion size, awaiting `size:exception`).
4. **Evidence correction (work unit `S1-runtime-evidence-correction`)** — task 1.1 previously classified `proxy.ts` as dead code with a `N/A` runtime harness. Correction: `proxy.ts` was an active Next 16 auto-loaded proxy entrypoint; runtime harness is now the post-deletion `pnpm build` + authoritative manifest absence (pre/post manifest diff). Product change (deleting the unauthenticated `/api/proxy/:path*` forwarding) is unchanged; only evidence/classification amended. `design.md`/`tasks.md` "dead code"/"N/A (removal only)" wording minimally reworded; task checkboxes 1.1–1.4 unchanged. Proposal/exploration retain the "dead" characterization (not in allowed edit roots) — flagged for parent review.
5. **Bookkeeping-only reconciliation** — no product bytes changed in this batch; no tests run (N/A), only index state (`git restore --staged`) and apply-progress bookkeeping.
6. **PR #2 (1.3) completed under approved `size:exception`** — the 14 obsolete cleanup paths (13 `design/` + `lint_output.txt`, ~1,433 text deletions) are staged as deletions with structural readback proof. Exception scope is path- and PR-scoped (Engram #6286); it does not extend to functional changes or later slices. tasks.md line 24 (later `size:exception`, not approved) was left untouched; per the mark-only-1.3 instruction it is unchanged — flagged for parent to reword if desired.
7. **Index attribution corrected (2026-08-13)** — supersedes the prior "S3 staged content includes future-unit hunks absorbed by the cumulative chain" rationale. That rationale (whole-file absorption: `package.json` carrying PR #7/PR #4-5 hunks "because later PRs build on PR #3's branch") was FALSE for slice isolation and is removed. Corrected state: `package.json` staged 2.1 hunks ONLY; future hunks (`prepare`, lint-staged config, husky/lint-staged devDeps → PR #7; `appwrite:*` scripts + `:apply` alias → PR #4/5) are UNSTAGED worktree-only changes, deferred to their slices.
8. **`lib/appwrite.ts` hunk-split** — only the DB_ID hunk (2.3) is staged for PR #3; the 1.4 debug-log removal hunk remains unstaged, attributable to PR #1 (batch-1 evidence). Parent must include it in PR #1's branch construction.
9. **`.gitignore` staging corrected (2026-08-13)** — supersedes the prior "staged whole" state. Staged = the single `package-lock.json` line (2.2) only. The workspace rename (`Service-system.code-workspace`→`serviceflow.code-workspace`) and `lint_output.txt` ignore lines are unstaged worktree-only hunks, deferred (F3-naming / PR #2-adjacent hygiene — not PR #3-attributable per correction gate).
10. **No lockfile regeneration (superseded 2026-08-13)** — the original orchestrator gate (would mutate/require network) blocked `pnpm install`; consistency was verified statically. SUPERSEDED by the approved lockfile regeneration correction above (2026-08-13, post `size:exception`): the lockfile is now regenerated from the exact staged PR3 manifest in an isolated scratch dir and frozen-verified.
11. **Untracked regenerated `package-lock.json` preserved** — left on disk (evidence), now ignored; contract requires only that it never enter commits, which `.gitignore` now guarantees.
12. **PR #3 budget interpretation corrected (2026-08-13)** — prior wording claimed lockfiles are excluded from the authored risk count as "generated lockfiles" and that the chained-pr "generated-diff carve-out" applies to the PR #3 lockfile diff. FALSE: the carve-out covers generated goldens only; lockfiles are not automatically goldens. Corrected: authored staged lines 30; lockfile-inclusive PR #3 diff ≈ 11,234 lines > 400 → **blocked pending a path-scoped `size:exception` for the PR #3 lockfile diff (parent asks Jona)** — APPROVED 2026-08-13 (lockfile scope only); see the regeneration correction section.
13. **Lockfile contamination corrected (2026-08-13)** — the pre-correction staged `pnpm-lock.yaml` was generated from the dirty worktree manifest and carried husky ^9.1.7 + lint-staged ^17.0.8 in its importer (19 devDeps) although the staged PR3 manifest declares neither. Regeneration from the exact staged manifest removed both (17/17 devDeps) — see the regeneration correction section.

## Status

7/23 tasks complete (1.1–1.4, 2.1–2.3 — PR #1/S1, cleanup PR #2, PR #3/S3). PR #3 index isolated to tasks 2.1–2.3 hunks and structurally verified (worktree byte-identical; S1/PR #2 sets preserved); commit/PR deferred to parent. **Next state: blocked** — PR #3 lockfile-inclusive diff ≈ 11,234 lines > 400, pending Jona's path-scoped `size:exception` for the lockfile diff. After approval: PR #4 — tasks 3.1–3.6 (migration guard, strict TDD with RED tests).

## Status (current, after 2026-08-13 lockfile correction)

7/23 tasks complete (1.1–1.4, 2.1–2.3 — PR #1/S1, cleanup PR #2, PR #3/S3). PR #3 index isolated to tasks 2.1–2.3 hunks; `pnpm-lock.yaml` regenerated from the exact staged PR3 manifest under Jona's approved `size:exception` (lockfile churn + `package-lock.json` deletion, PR #3 only); frozen-lockfile check exit 0; `tsc --noEmit` exit 0; all worktree/index bytes preserved. **State: READY for PR #3 construction (parent).** Next unit: PR #4 — tasks 3.1–3.6 (migration guard, strict TDD with RED tests).

## Work Unit: S4 — Migration Guard (F1) — PR #4 (tasks 3.1–3.6)

- **Delivery**: auto-chain / feature-branch-chain, PR #4 base = PR #3 branch (parent constructs branch/PR; apply stages only)
- **Mode**: **Strict TDD with real RED→GREEN cycles** — behavioral unit. Draft script/tests existed untracked since batch 1; true chronology established by adding failing scenarios before each production change (no fabricated chronology).
- **Slice boundary**: tasks 3.1–3.6 only. No other task, path, or product byte touched. No live Appwrite access; runtime harness N/A (fully mocked admin SDK).
- **Branch**: parent-managed (PR #4 branch derives from PR #3 branch).

### TDD Cycle Evidence (S4)

| Task | Test File                               | Layer | Safety Net            | RED                                                                                             | GREEN                                                                                                                                                                               | TRIANGULATE                                                                                                                                                                                 | REFACTOR                                       |
| ---- | --------------------------------------- | ----- | --------------------- | ----------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| 3.1  | `tests/permissions.test.ts`             | Unit  | ✅ 17/17 (2 files)    | ✅ 10 failed/15 passed — `assertDevTarget is not a function` (import error, code did not exist) | ✅ 25/25 after `assertDevTarget` + `DEV_ENDPOINT_ALLOWLIST` implemented                                                                                                             | ✅ 10 cases: localhost/127.0.0.1/[::1] accept; prod/malformed/missing endpoint, missing project, unset allowlist, project ∉ allowlist, whitespace-padded allowlist                          | ➖ None needed (pure function, single-purpose) |
| 3.2  | `tests/permissions.test.ts`             | Unit  | ✅ 25/25              | ✅ 5 failed/25 passed — `confirmed` missing, `--yes` ignored, argv param rejected               | ✅ 30/30 after `parseArgs(argv = process.argv.slice(2))` → `{dryRun, confirmed}`                                                                                                    | ✅ 5 cases: bare `--apply` ⇒ confirmed:false; `--apply --yes` ⇒ confirmed:true; `--yes` alone keeps dry-run; no flags ⇒ dry-run+unconfirmed; unknown flag via argv throws                   | ➖ None needed                                 |
| 3.3  | `tests/permissions.test.ts`             | Unit  | ✅ 30/30              | ✅ 3 failed/30 passed — `main` not exported                                                     | ✅ 33/33 after `export main` + `assertDevTarget` call + `!dryRun && !confirmed` gate                                                                                                | ✅ 3 scenarios: apply w/o `--yes` exits 1 before Client/Databases construction; prod endpoint rejected on dry-run before any DB access; confirmed apply reaches DB                          | ➖ None needed                                 |
| 3.4  | `tests/permissions.test.ts`             | Unit  | ✅ 30/30              | ✅ (same 3 main-gate tests — plan-before-mutation + mutate-only-when-confirmed assertions)      | ✅ 33/33 — plan lines printed before `updateCollection` (shared event timeline), `updateCollection` called with `[]` perms ×3, `APPLY` header                                       | ✅ 2 behaviors: dry-run never mutates (pre-existing test); confirmed apply prints plan then mutates                                                                                         | ➖ None needed                                 |
| 3.5  | none (package.json script-name removal) | N/A   | ✅ 35/35 (full suite) | N/A — config/contract, no new behavior (precedent S1–S3 non-behavior handling)                  | ✅ `pnpm run appwrite:migrate-permissions:apply` → `[ERR_PNPM_NO_SCRIPT] Missing script`, exit 1; `grep "migrate-permissions:apply" package.json` → 0 hits; other scripts preserved | ➖ Single (one alias)                                                                                                                                                                       | ➖ None needed                                 |
| 3.6  | structural verification only            | N/A   | ✅ 35/35              | N/A — REFACTOR/verify task                                                                      | ✅ `Role.any`/`Permission.read                                                                                                                                                      | write`→ 0 hits in scripts/lib/app/tests;`createCollection(DB_ID, id, name, [])`; admin-SDK-only (`Client`+`.setKey`in both scripts);`tsc --noEmit`exit 0;`eslint` on 3 changed files exit 0 | ➖ Single                                      |

### Test Summary (S4)

- **Total tests written**: 18 (10 assertDevTarget/allowlist + 5 parseArgs + 3 main-gate)
- **Total tests passing**: 35/35 (2 files) — baseline 17 + 18 new
- **Layers used**: Unit (18)
- **Approval tests**: None — no behavior-preserving refactor of existing code (draft behavior was the RED baseline, not an approval target)
- **Pure functions created**: 2 (`assertDevTarget`, rewritten `parseArgs`)

### Work Unit Evidence (S4 — tasks 3.1–3.6)

| Evidence                                          | Required value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Focused test command and exact result             | `pnpm test:run tests/permissions.test.ts` → RED runs: 10 failed/15 passed (3.1), 5 failed/25 passed (3.2), 3 failed/30 passed (3.3/3.4); final GREEN 33/33. Full `pnpm test:run` → 2 files, 35/35 passed. `pnpm exec tsc --noEmit` → exit 0. `pnpm exec eslint scripts/migrate-appwrite-permissions.ts tests/permissions.test.ts scripts/setup-appwrite.ts` → exit 0.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Runtime harness command/scenario and exact result | **N/A** — fully mocked Appwrite/admin SDK (vitest `vi.mock("node-appwrite")`); no network, no live Appwrite access in this unit by design (PR5 owns the isolated dev runtime: tasks 4.1–4.3). The 3 main-gate tests execute the real `main()` control flow with mocked SDK constructors — closest harness to runtime without touching Appwrite.                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Rollback boundary                                 | Worktree: revert `tests/permissions.test.ts`/`scripts/migrate-appwrite-permissions.ts` to batch-3 draft state (`git restore` from /tmp/opencode/pr4/ rollback copies or re-apply inverse edits — files untracked, so `rm` + restore from draft snapshot), `package.json` re-add the `:apply` alias line, `scripts/setup-appwrite.ts` restore `Permission/Role` import + `Role.any()` perms. Index: `git restore --staged scripts/migrate-appwrite-permissions.ts tests/permissions.test.ts scripts/setup-appwrite.ts` (returns to untracked/worktree-only) + `git update-index` package.json back to blob `de1e61f8` (sha256 1eb11814…). S1/PR #2/PR #3 staged sets, `next-env.d.ts`, `.gitignore` workspace-rename hunk, lib/appwrite.ts 1.4 hunk, PR7 package.json hunks — all untouched by any revert. |

### Index Set Accounting (S4 — pre/post)

- **PRE (batch-3 state)**: staged = S1 (`D` check_or.ts, proxy.ts) + PR #2 (`D` ×14) + PR #3 (`D` package-lock.json; `A` pnpm-workspace.yaml; `M` .gitignore, lib/appwrite.ts, package.json 2.1-hunks, pnpm-lock.yaml, scripts/setup-appwrite.ts DB_ID-hunk). Unstaged worktree-only: package.json scripts+prepare+lint-staged hunks, setup-appwrite.ts perms hunk, lib/appwrite.ts 1.4 hunk, .gitignore workspace hunk, next-env.d.ts. Untracked: scripts/migrate-appwrite-permissions.ts, tests/permissions.test.ts, openspec/, .github/, .husky/, docs, etc.
- **POST (after S4)**: staged delta = exactly 4 paths: `A scripts/migrate-appwrite-permissions.ts` (254 lines, final), `A tests/permissions.test.ts` (469 lines, final), `M scripts/setup-appwrite.ts` (cumulative DB_ID + perms hunks, 3+/8−), `M package.json` (blob f663b9aa; cumulative 2.1 hunks + appwrite scripts ×2 — 12+/7−). **No `:apply` alias anywhere in the staged blob.** Worktree-only remainders preserved: package.json PR7 hunks (prepare, lint-staged block, husky+lint-staged devDeps — 8+), lib/appwrite.ts 1.4 hunk, .gitignore workspace hunk, next-env.d.ts. S1/PR #2/PR #3 staged sets byte-identical (verified via `git status --porcelain` + cached diff stats).

### Line Budget (S4 / PR #4)

- **Authored by this apply**: `scripts/migrate-appwrite-permissions.ts` +59 (195→254), `tests/permissions.test.ts` +218 (251→469), `package.json` −1 (alias). **Authored changed lines = 278 ≤ 400 ✓** (parent's budget definition; no size exception requested).
- **PR #4 raw branch diff vs PR #3 branch** (additions+deletions, incl. pre-existing worktree-hardening draft content that predates this apply session): script 254 + tests 469 + setup-appwrite perms 9 + package.json 2 ≈ **734 > 400** — flagged for parent. The drafts (untracked since batch 1) are the S1–S3-documented "later-unit worktree hardening" content, not apply-authored bytes; per the S3 lockfile-correction precedent the raw-diff standard counts them toward review load, so the parent may want a path-scoped size note for PR #4 or an explicit decision. No `size:exception` claimed here.

### Incident Log (S4)

1. **`pnpm run` lockfile contamination (2026-08-13)**: verifying the removed alias via `pnpm run appwrite:migrate-permissions:apply` triggered pnpm 11 auto-install (worktree manifest has PR7-scope husky/lint-staged devDeps not in the staged frozen lockfile) → worktree `pnpm-lock.yaml` rewrote (+51/−10: husky+lint-staged importer entries + vite peer variant). **Fixed**: `git restore --worktree pnpm-lock.yaml` → byte-identical to staged PR3 frozen blob (0 unstaged diff lines; `M` staged-only). Documented here so future slices verify script presence with `pnpm exec`/static checks instead of `pnpm run`.

## Status (current, after S4)

13/23 tasks complete (1.1–1.4, 2.1–2.3, 3.1–3.6 — PR #1/S1, cleanup PR #2, PR #3/S3, PR #4/S4). PR #4 index isolated: migrate script + tests staged `A`, setup-appwrite perms hunk staged, package.json appwrite scripts staged without `:apply` alias (PR7 hunks stay worktree-only). 35/35 tests green, tsc/eslint clean. **State: READY for PR #4 construction (parent).** Next unit: PR #5 — tasks 4.1–4.3 (dev-only migration run, isolated dev runtime + evidence; requires dev endpoint/project/key precondition).

---

# RESET/SPLIT RECONCILIATION (2026-08-13) — supersedes the S4 section above

Jona split the old oversized PR #4 (726-line raw, guard+runner+main in one blob) into three autonomous slices (decision #6299: reviewer focus + 400-line budget; tests stay with each behavior slice). tasks.md was revised: Phase 3 renumbered 3.1–3.9 (old 3.1–3.6 mapping is DEAD), all Phase-3 checkboxes reset to `[ ]`, work-unit table now maps unit 4→PR4 (dev-target guard, ~230, `N/A-pure`, rollback rm 2 files), unit 5→PR5 (runner core, ~340, `N/A-mocked`), unit 6→PR6 (CLI wiring + SA-2, ~190, tsx dry-run no-env → exit 1). The prior S4 batch's staged blob paths (`scripts/migrate-appwrite-permissions.ts`, `tests/permissions.test.ts`, cumulative `scripts/setup-appwrite.ts`, package.json blob f663b9aa) were the OLD PR #4 candidate; this batch reconciles the index to the new PR #4 slice ONLY. Prior genuine RED chronology (10/15, 5/25, 3/30 → 33/33) is preserved in the S4 section above and the S3/S4 evidence for tasks 1.1–2.3 remains valid; the guard/parseArgs test content itself was NOT re-authored — it was relocated verbatim into the new slice's test file (byte-equivalent assertions; see worktree hashes).

## Work Unit: S4-SPLIT — Dev-Target Guard (F1) — PR #4 (revised tasks 3.1–3.3)

- **Delivery**: auto-chain / feature-branch-chain, PR #4 base = PR #3 branch (parent constructs branch/PR; apply stages only)
- **Mode**: **Strict TDD** — behavioral unit. New module boundary introduced by the extraction → a NEW genuine RED was executed for this boundary (module file did not exist); prior RED chronology preserved, nothing fabricated.
- **Slice boundary**: revised tasks 3.1–3.3 ONLY (`DEV_ENDPOINT_ALLOWLIST` + `assertDevTarget` + `parseArgs` contracts in `scripts/dev-target-guard.ts`; focused tests in `tests/dev-target-guard.test.ts`). No Appwrite access; runtime harness N/A (pure functions; no SDK, no network — mocked-boundary tests live in PR5/6).
- **Branch**: parent-managed (PR #4 branch derives from PR #3 branch).

### Index Reconciliation (executed, verified)

| Action                        | Path                                                                                                                                | Result                                                                                                                                                                                                              |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Unstaged (worktree preserved) | `scripts/migrate-appwrite-permissions.ts`, `tests/permissions.test.ts`                                                              | `A` → `??` untracked; worktree bytes intact (sha256 preserved, see hashes below)                                                                                                                                    |
| Index blob restored           | `package.json`                                                                                                                      | f663b9aa (cumulative 12+/7−) → de1e61f8 (PR3 2.1 hunks only, 9+/6−) via `git update-index --cacheinfo`                                                                                                              |
| Index blob restored           | `scripts/setup-appwrite.ts`                                                                                                         | a13d8026 (cumulative 3+/8−) → 3a2aaafa (DB_ID hunk only, 1+/1−); reconstructed in scratch from worktree by reverting only the Permission/Role import + `Role.any()` perms hunk (verified diff vs HEAD = DB_ID only) |
| Staged (new)                  | `scripts/dev-target-guard.ts`, `tests/dev-target-guard.test.ts`                                                                     | `git add` → `A` ×2 — the ONLY PR #4 product paths                                                                                                                                                                   |
| Preserved byte-identical      | S1 (`D` check_or.ts, proxy.ts; lib/appwrite.ts 1.4 hunk unstaged), PR #2 (`D` ×14), PR #3 (all 7 paths incl. frozen pnpm-lock.yaml) | staged-set delta vs pre-state = exactly the 4 paths above (comm -3 on cached name-status); no other index entry touched by construction                                                                             |

### TDD Cycle Evidence (S4-SPLIT)

| Task | Test File                        | Layer | Safety Net            | RED                                                                                                                                                                                                              | GREEN                                                                                                                                     | TRIANGULATE                                                                                                                                                                                                   | REFACTOR                                        |
| ---- | -------------------------------- | ----- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| 3.1  | `tests/dev-target-guard.test.ts` | Unit  | ✅ 35/35 (2 files)    | ✅ **New boundary RED (2026-08-13): `Failed to resolve import "../scripts/dev-target-guard" from "tests/dev-target-guard.test.ts". Does the file exist?`** — module file did not exist (1 file failed, no tests) | ✅ 19/19 after `scripts/dev-target-guard.ts` created with `assertDevTarget` + `DEV_ENDPOINT_ALLOWLIST`                                    | ✅ 9 assertDevTarget cases + 1 allowlist case: localhost/127.0.0.1/[::1] accept; prod/malformed/missing endpoint, missing project, unset allowlist, project ∉ allowlist, whitespace-padded allowlist          | ➖ None needed (pure functions, single-purpose) |
| 3.2  | `tests/dev-target-guard.test.ts` | Unit  | ✅ 19/19 (guard file) | ✅ (same module-boundary RED — parseArgs contract covered by the 9 parseArgs cases imported from the not-yet-existing module; prior chronology 5 failed/25 passed preserved from S4)                             | ✅ 19/19 — `parseArgs(argv = process.argv.slice(2))` → `{dryRun, confirmed}`                                                              | ✅ 9 cases: bare `--apply` ⇒ confirmed:false; `--apply --yes` ⇒ confirmed:true; `--yes` alone keeps dry-run; no flags ⇒ dry-run+unconfirmed; unknown flag via argv throws; explicit `--dry-run`; argv default | ➖ None needed                                  |
| 3.3  | `tests/dev-target-guard.test.ts` | Unit  | ✅ 19/19              | ✅ (module-boundary RED above IS the 3.3 GREEN precondition — no code existed to satisfy it)                                                                                                                     | ✅ 19/19 — `scripts/dev-target-guard.ts` contains ONLY `DEV_ENDPOINT_ALLOWLIST`, `assertDevTarget`, `ParseResult`, `parseArgs` (59 lines) | ✅ covered by 3.1/3.2 triangulation (19 cases)                                                                                                                                                                | ➖ None needed                                  |

### Work Unit Evidence (S4-SPLIT — tasks 3.1–3.3)

| Evidence                                          | Required value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Focused test command and exact result             | `pnpm exec vitest run tests/dev-target-guard.test.ts` → RED: `Failed to resolve import "../scripts/dev-target-guard"` (1 file failed, no tests); GREEN: 1 file passed, **19 tests passed** (19/19). `pnpm exec vitest run` (full) → **3 files, 35/35 passed** (schemas 2 + permissions 14 + guard 19 — total unchanged from baseline 35, no tests lost/added, 19 relocated verbatim). `pnpm exec tsc --noEmit` → exit 0. `pnpm exec eslint` on the 4 touched files → exit 0.                                                                                                                                                                                                                                                |
| Runtime harness command/scenario and exact result | **N/A — pure-module slice**: `assertDevTarget`/`parseArgs` are pure functions (env + argv inputs, thrown errors); the guard module imports NO Appwrite SDK and opens NO network. The runtime boundary (real `main()` flow with mocked SDK, tsx dry-run) belongs to PR5/6 per the split; mocked-boundary validation for this slice = the 19 unit cases above.                                                                                                                                                                                                                                                                                                                                                                |
| Rollback boundary                                 | Worktree: `rm scripts/dev-target-guard.ts tests/dev-target-guard.test.ts` (the 2 new files — rollback "rm 2 files" per unit table); to restore the future blobs' inline guard, re-insert the extracted block (available in this doc's S4 section / git object store) or `git restore` from HEAD where tracked. Index: `git restore --staged scripts/dev-target-guard.ts tests/dev-target-guard.test.ts` returns the index to the pre-slice state (S1/PR2/PR3 byte-identical). Re-adding old blob paths would require the prior staged blobs (ab921b6d, eb197d3f) — available in the object store. `next-env.d.ts`, `.gitignore` workspace hunk, lib/appwrite.ts 1.4 hunk, PR7 package.json hunks — untouched by any revert. |

### Line Budget (S4-SPLIT / PR #4)

- **PR #4 raw staged diff**: `scripts/dev-target-guard.ts` 59 (+59) + `tests/dev-target-guard.test.ts` 162 (+162) = **221 changed lines (221 additions, 0 deletions) ≤ 400 ✓** — matches the unit-4 estimate (~230). No size exception needed or claimed.
- **Old PR #4 candidate (superseded)**: migrate script 254 + tests 469 + setup perms 9 + package.json 2 ≈ 734 > 400 — now split; PR5 (runner core ≈340) and PR6 (CLI wiring + SA-2 ≈190) remain as future slices, unmeasured precisely.

### Future Worktree Preservation (PR5/6 — NOT staged)

- `scripts/migrate-appwrite-permissions.ts` (untracked, worktree): extracted guard block removed; now imports `{ assertDevTarget, parseArgs } from "./dev-target-guard"`; keeps `buildMigrationPlan`, `processCollections`, `main` + entry guard (sha256 46d9d5ec…). Runs green via `tests/permissions.test.ts` (14/14).
- `tests/permissions.test.ts` (untracked, worktree): guard describes removed (moved verbatim to the guard test); keeps import side-effects, buildMigrationPlan, processCollections, main-gate tests (14 tests; sha256 edb0c613…).
- Worktree future hunks preserved: `package.json` PR7 (prepare/lint-staged/husky) + PR6 (`appwrite:setup`, `appwrite:migrate-permissions` scripts — `:apply` alias absent), `scripts/setup-appwrite.ts` perms hunk (PR6/3.8), `.gitignore` workspace rename + lint_output.txt lines, `lib/appwrite.ts` 1.4 hunk (PR1), `next-env.d.ts` (unrelated). `pnpm-lock.yaml` worktree clean (0 unstaged lines — no `pnpm run` executed this batch; only `pnpm exec`).

### Status (current, after S4-SPLIT)

13/26 revised tasks complete (1.1–1.4, 2.1–2.3 = 7; revised 3.1–3.3 = 3; old 3.4–3.6 evidence preserved but those IDs are dead — work re-planned into revised 3.4–3.9). PR #4 index isolated to exactly 2 paths (guard module + test, 221 lines). 35/35 tests green, tsc/eslint clean. **State: READY for PR #4 construction (parent).** Next unit: PR #5 — revised tasks 3.4–3.5 (runner core: `buildMigrationPlan`/`processCollections` GREEN + RED — `tests/permissions.test.ts` 14 tests currently green, runner tests already written in worktree).

## Work Unit: S5 — Runner Core (F1) — PR #5 (revised tasks 3.4–3.5)

- **Delivery**: auto-chain / feature-branch-chain, PR #5 base = PR #4 branch (parent constructs branch/PR; apply stages only, no commit/PR by apply).
- **Mode**: **Strict TDD** — behavioral unit. Module-boundary RED (test references `../scripts/migrate-appwrite-permissions`, module absent) plus the authorized correction's NEW non-404 discrimination RED→GREEN (decision #6325), all executed, nothing fabricated.
- **Slice boundary**: revised tasks 3.4–3.5 ONLY (`buildMigrationPlan`/`processCollections` in `scripts/migrate-appwrite-permissions.ts`; focused tests in `tests/permissions.test.ts`). No CLI/main wiring (PR6/3.6–3.7), no setup-appwrite changes (PR6/3.8), no live Appwrite access; runtime harness N/A-mocked (unit table row 5).
- **Branch/path**: candidate branch `fix/remediate-audit-pr5-core`, worktree `/home/jona/projects/serviceflow-worktrees/remediate-audit-pr5-core` (parent-constructed; HEAD = PR #4 guard commit ed11b49).

### Correction (authorized 2026-08-13 — decision #6325)

Persist revised 3.4–3.5 evidence and fix the reliability false-success path: non-404 Appwrite `getCollection` failures were caught by the bare `catch`, logged as "not found, skipping", and silently dropped from `MigrationResult`. Fix: exported typed predicate `isCollectionNotFound(err)` (checks `code`/`status === 404`); 404 keeps the skip/warn path; any other error is reported `status: "failed"` with the error message — consistent with the existing `updateCollection` failure path and later CLI exit semantics (PR6). No constructor mocking added; no main/CLI wiring; no PR6+ product files touched.

### TDD Cycle Evidence (S5)

| Task | Test File                   | Layer | Safety Net         | RED                                                                                                                                                                                                                                                                                                                                          | GREEN                                                                                                                                        | TRIANGULATE                                                                                                                                                       | REFACTOR                                          |
| ---- | --------------------------- | ----- | ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| 3.4  | `tests/permissions.test.ts` | Unit  | ✅ 32/32 (3 files) | ✅ Module-boundary RED: `Failed to resolve import "../scripts/migrate-appwrite-permissions" … Does the file exist?` (1 file failed, no tests — executed in scratch with the received-candidate test bytes and module absent); NEW non-404 RED: **2 failed \| 12 passed (14)** — non-404 reported failed, mixed 404/non-404 run discriminates | ✅ **14/14 passed** after `isCollectionNotFound` + getCollection catch discrimination                                                        | ✅ 3 cases: all-404 → skipped/empty (approval leg, already passing at RED); all-401 → failed ×3 with error preserved; mixed 404+500 → 404 skipped, non-404 failed | ➖ None needed (single-purpose predicate + catch) |
| 3.5  | `tests/permissions.test.ts` | Unit  | ✅ 32/32           | ✅ (same RED above — runner-core GREEN precondition)                                                                                                                                                                                                                                                                                         | ✅ Full suite **3 files, 35/35 passed** (schemas 2 + guard 19 + permissions 14); `tsc --noEmit` exit 0; eslint on the 2 touched files exit 0 | ✅ covered by 3.4 triangulation                                                                                                                                   | ➖ None needed                                    |

### Test Summary (S5)

- **Total tests written (this correction)**: 3 (non-404 regression, all-404 skip, mixed 404/non-404 discrimination)
- **Total tests passing**: 35/35 (full), 14/14 (focused)
- **Layers used**: Unit (14)
- **Approval tests**: 1 (all-404 skip leg — preserves pre-fix behavior)
- **Pure functions created**: 1 (`isCollectionNotFound`)

### Work Unit Evidence (S5 — revised tasks 3.4–3.5)

| Evidence                                          | Required value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Focused test command and exact result             | `node_modules/.bin/vitest run tests/permissions.test.ts` (binaries invoked directly from source worktree `node_modules` via a temporary symlink, removed after — no install, no `pnpm` run per incident-log rule): SAFETY NET 11/11 (full 32/32); RED **2 failed \| 12 passed (14)**; GREEN **14/14 passed**. Full `vitest run` → **3 files, 35/35 passed**. `tsc --noEmit` exit 0; `eslint scripts/migrate-appwrite-permissions.ts tests/permissions.test.ts` exit 0. Temp artifacts cleaned up (node_modules symlink, `tsconfig.tsbuildinfo`, scratch copy). |
| Runtime harness command/scenario and exact result | **N/A — mocked runtime**: runner core operates on a fully mocked Appwrite admin SDK (`vi.mock("node-appwrite")`); no network, no live Appwrite access by design (unit table row 5 `N/A-mocked`; the live dev Appwrite run is task 4.x / later unit).                                                                                                                                                                                                                                                                                                           |
| Rollback boundary                                 | Worktree: `git restore --staged scripts/migrate-appwrite-permissions.ts tests/permissions.test.ts` and revert both files to the received-candidate state (drop `isCollectionNotFound`, the catch discrimination, and the 3 new tests) → 323-line candidate restored; `git restore` from PR #4 base (HEAD ed11b49) drops the entire slice. Nothing outside the two PR5 files was touched; PR6+ files untouched.                                                                                                                                                 |

### Line Budget (S5 / PR #5)

- **Correction delta vs received candidate**: 74 changed lines (73+/1−) ≤ 77 budget ✓.
- **PR #5 raw staged diff vs PR4 base**: `scripts/migrate-appwrite-permissions.ts` 132 (+132) + `tests/permissions.test.ts` 263 (+263) = **395 changed lines (395 additions, 0 deletions) ≤ 400 ✓**. No size exception needed or claimed.
- Staged paths = exactly the same two PR5 files (`A` ×2); no other index entry touched.

### Status (current, after S5)

15/26 revised tasks complete (1.1–1.4, 2.1–2.3, revised 3.1–3.3, revised 3.4–3.5). PR #5 candidate staged in `fix/remediate-audit-pr5-core` (395 ≤ 400), 35/35 tests green, tsc/eslint clean. **State: READY for PR #5 construction (parent).** Next unit: PR #6 — revised tasks 3.6–3.9 (CLI wiring + SA-2).

## Work Unit: S6 — CLI Wiring + SA-2 (F1) — PR #6 (revised tasks 3.6–3.9)

- **Delivery**: auto-chain / feature-branch-chain, PR #6 base = PR #5 branch (parent constructs branch/PR; apply stages only, no commit/PR by apply).
- **Mode**: **Strict TDD** — behavioral unit. Main-gate RED (tests reference `main` export, absent at 9a949e1) executed, then GREEN from the authoritative revised task drafts; nothing fabricated.
- **Slice boundary**: revised tasks 3.6–3.9 ONLY — `main()` + entry guard + `parseArgs`/`assertDevTarget` wiring + env loading in `scripts/migrate-appwrite-permissions.ts`; 3 main-gate tests in `tests/permissions.test.ts`; SA-2 hunk in `scripts/setup-appwrite.ts` (drop Permission/Role imports, `[]` collection permissions, DB_ID base preserved); `appwrite:migrate-permissions` script in `package.json` (no `:apply` alias). No PR7 hunks (husky/lint-staged/prepare), no docs/CI work, no live Appwrite/network access.
- **Branch/path**: candidate branch `fix/remediate-audit-pr6-cli`, worktree `/home/jona/projects/serviceflow-worktrees/remediate-audit-pr6-cli` (parent-constructed; HEAD = PR #5 core commit 9a949e1).

### TDD Cycle Evidence (S6)

| Task | Test File                   | Layer | Safety Net         | RED                                                                                                                                                                                      | GREEN                                                                                                                                        | TRIANGULATE                                                                                                                                                                                                                      | REFACTOR                              |
| ---- | --------------------------- | ----- | ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- |
| 3.6  | `tests/permissions.test.ts` | Unit  | ✅ 35/35 (3 files) | ✅ `TypeError: main is not a function` — **3 failed \| 14 passed (17)** in focused file (main-gate tests written first, referencing the not-yet-existing export)                         | ✅ **17/17 passed** after `main()` + entry guard + imports added to the migrate script                                                       | ✅ 3 distinct code paths: `--apply` w/o `--yes` → exit(1) pre-Client/Databases; prod endpoint rejected on dry-run pre-Client/Databases; confirmed apply → plan printed before `updateCollection`, permissions `[]`, APPLY banner | ➖ None needed (single-purpose gates) |
| 3.7  | `tests/permissions.test.ts` | Unit  | ✅ 17/17           | ✅ (same RED above — CLI-wiring GREEN precondition)                                                                                                                                      | ✅ Full suite **3 files, 38/38 passed** (schemas 2 + guard 19 + permissions 17); `tsc --noEmit` exit 0; eslint on the 4 touched files exit 0 | ✅ covered by 3.6 triangulation                                                                                                                                                                                                  | ➖ None needed                        |
| 3.8  | (config — no new tests)     | —     | ✅ 38/38           | ➖ Structural SA-2 hunk (drop `Permission`/`Role` import, `[]` perms) — no new behavior to test; `Role.any()` absent verified by grep + tsc/eslint clean                                 | ✅ hunk applied; `tsc --noEmit` exit 0; eslint exit 0                                                                                        | ➖ Single (one literal replacement)                                                                                                                                                                                              | ➖ None needed                        |
| 3.9  | (reconciliation)            | —     | ✅ 38/38           | ➖ REFACTOR/reconciliation: tasks.md count reconciled truthfully — **35 baseline + 3 new = 38** (not the stale 33 from tasks.md; 35 = schemas 2 + guard 19 + permissions 14 at PR5 base) | ✅ suite count 38/38 matches tasks.md; `:apply` alias absent (grep 0 matches in staged diff); no duplicate tests (each behavior tested once) | ➖ N/A                                                                                                                                                                                                                           | ✅ counts reconciled                  |

### Test Summary (S6)

- **Total tests written**: 3 (main-gate: --apply w/o --yes abort; prod-endpoint dry-run rejection; confirmed apply plan-before-mutation)
- **Total tests passing**: 38/38 (full), 17/17 (focused)
- **Layers used**: Unit (3); runtime harness exercised the real CLI via tsx (no Appwrite credentials, no network)
- **Approval tests**: None — no refactoring of existing behavior
- **Pure functions created**: 0 (CLI wiring; gates delegate to existing `parseArgs`/`assertDevTarget`)

### Work Unit Evidence (S6 — revised tasks 3.6–3.9)

| Evidence                                          | Required value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Focused test command and exact result             | `./node_modules/.bin/vitest run tests/permissions.test.ts` (binaries from source worktree `node_modules` via temporary symlink, removed after — no install): SAFETY NET 35/35 full (3 files) / 14/14 focused; RED **3 failed \| 14 passed (17)** (`TypeError: main is not a function`); GREEN **17/17 passed**. Full `vitest run` → **3 files, 38/38 passed** (schemas 2 + guard 19 + permissions 17). `tsc --noEmit` exit 0; `eslint scripts/ tests/` exit 0 (0 problems). Raw staged diff = **255 lines, 188+/10− (198 changed) ≤ 400 ✓**.                                                                                                                                                                                                                                                                                                                    |
| Runtime harness command/scenario and exact result | `strace -f -e trace=connect tsx scripts/migrate-appwrite-permissions.ts --dry-run` under three env scenarios (temp dirs in /tmp/opencode, no Appwrite credentials): (a) no `.env`, no env vars → **exit 1**, silent (Node `process.loadEnvFile()` throws ENOENT; entry guard `.catch(() => process.exit(1))` swallows — fail-closed); (b) prod endpoint `.env` (`https://cloud.appwrite.io/v1`) → **exit 1**, no output printed (assertDevTarget throw swallowed by entry guard; message asserted in unit test #2 via direct `main()`); (c) dev endpoint + missing `APPWRITE_API_KEY` → **exit 1**, prints `Missing required Appwrite environment variables.` + `- APPWRITE_API_KEY`. ALL three: **zero AF_INET/AF_INET6 connect syscalls** — only tsx's AF_UNIX worker pipe (`/tmp/tsx-1000/*.pipe`); fail-closed exit before Client/Databases/network proven. |
| Rollback boundary                                 | Worktree: `git restore --staged <4 paths>` then revert the 4 files to 9a949e1 (`git restore --source=9a949e1 -- <paths>` drops the whole slice): migrate script loses `main()`+entry guard+imports; test file loses the 3 main-gate tests + import changes; setup-appwrite.ts loses the SA-2 hunk; package.json loses the migrate script line. PR5 core files' other content untouched by the revert; PR7/CI/docs files never touched.                                                                                                                                                                                                                                                                                                                                                                                                                          |

### Line Budget (S6 / PR #6)

- **PR #6 raw staged diff vs PR5 base (9a949e1)**: `package.json` 3 (2+/1−) + `scripts/migrate-appwrite-permissions.ts` 82 (81+/1−) + `scripts/setup-appwrite.ts` 9 (2+/7−) + `tests/permissions.test.ts` 104 (103+/1−) = **255 diff lines, 198 changed (188+/10−) ≤ 400 ✓**. Matches the unit-6 estimate (~190). No size exception needed or claimed.
- Staged paths = exactly the 4 PR6 files (`M` ×4); no other index entry touched. PR7 markers (husky/lint-staged/prepare/`:apply`) absent from staged diff (grep 0 matches).

### Status (current, after S6)

19/26 revised tasks complete (1.1–1.4, 2.1–2.3, revised 3.1–3.9). PR #6 candidate staged in `fix/remediate-audit-pr6-cli` (198 changed ≤ 400), 38/38 tests green (35 baseline + 3 new), tsc/eslint clean, runtime harness fail-closed with zero network syscalls. **State: READY for PR #6 construction (parent).** Next unit: PR #7 — Phase 4 dev-only run (S4, task 4.x): runtime unit requiring dev Appwrite + key; preconditions: dev env matching, else STOP; dry-run then `--apply --yes`; capture plan + evidence; no `Role.any()`.

## Correction: PR #6 — Entrypoint Error Sink (authorized; chain base `bb0911e`→PR4→PR5→PR6)

- **Context**: PR6 candidate rebased onto the committed PR3→PR5 chain (HEAD `44e48f7`; `bb0911e` = PR3 build/DB identity, `06116a1` = PR4 dev-target guard, `44e48f7` = PR5 runner core). Post-rebase audit: the entrypoint error sink in `scripts/migrate-appwrite-permissions.ts` swallowed every `main()` rejection — fail-closed but **silent** for loadEnvFile/parseArgs/assertDevTarget failures.
- **Before**:
  ```ts
  if (isMain) {
    main().catch(() => process.exit(1));
  }
  ```
- **After**:
  ```ts
  if (isMain) {
    main().catch((err: unknown) => {
      console.error(err instanceof Error ? err.message : String(err));
      process.exit(1);
    });
  }
  ```
- **Effect**: `process.loadEnvFile()` ENOENT, `parseArgs` unknown-arg throws, and `assertDevTarget` prod/malformed rejections now surface the exact error on stderr before `process.exit(1)`; nonzero exit preserved.
- **Regression decision**: NO new unit test — the catch boundary is reachable only through a real process (`isMain` compares `process.argv[1]` against `import.meta.url`), so vitest cannot hit it without a child-process spawn abstraction (scope widening, rejected). The boundary is exercised instead by the existing 3 direct-`main()` gate tests (all fail-closed paths reject through `main()`) plus the real CLI harness below, which drives the actual entrypoint and asserts stderr + exit 1.
- **Commands/results**: `pnpm exec vitest run` → **3 files, 38/38 passed**; `pnpm exec vitest run tests/permissions.test.ts` → **17/17 passed** (unchanged from S6; fix adds no behavior surface); `tsc --noEmit` exit 0; `eslint scripts/ tests/` exit 0 (0 problems). Real CLI harness (`node --import tsx scripts/migrate-appwrite-permissions.ts`, scratch dir in /tmp/opencode, no Appwrite credentials, `strace -f -e trace=connect`): (a) no `.env` → **exit 1**, stderr `ENOENT: no such file or directory, open '.env'` (**was silent**); (b) prod endpoint `.env` → **exit 1**, stderr `Appwrite endpoint host "cloud.appwrite.io" is not on the development allowlist.` (**was silent**); (c) dev endpoint, missing `APPWRITE_API_KEY` → **exit 1**, stderr `Missing required Appwrite environment variables.` + `- APPWRITE_API_KEY`; (d) dev `.env`, `--bogus` → **exit 1**, stderr `Unknown arguments. Usage: …` (**was silent**). ALL: **zero AF_INET/AF_INET6 connect syscalls** (only tsx AF_UNIX worker pipes) — fail-closed before Client/Databases/network.
- **Line budget**: PR #6 staged diff vs PR5 base `44e48f7` now **201 changed lines (191+/10−)** = S6-recorded 198 + 3 fix lines ≤ 400 ✓ (≤ 220 candidate budget ✓). Staged paths unchanged: exactly `package.json`, `scripts/migrate-appwrite-permissions.ts`, `scripts/setup-appwrite.ts`, `tests/permissions.test.ts` (`M` ×4).
- **Checkboxes**: NO changes — revised 3.6–3.9 remain [x], 19/26 unchanged. tasks.md not edited (state consistent: 38-test count, no `:apply` alias). Cleanup: temp `.env`/scratch dir removed, `tsconfig.tsbuildinfo` deleted; worktree clean outside the 4 staged files.
